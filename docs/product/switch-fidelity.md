---
route: new
stage: framed
---

# Switch fidelity — the handoff records exactly where we are, and nothing agreed is lost

## Value

Tony's requirement, in his words (2026-08-07, stated live when the session
tried to frame this as a token-cost problem and he corrected it):

> When context window is filling, we can switch out to record EXACTLY where
> we are against the agreed stage/ladder/plan to allow us to clear the
> context window and start as if it was the same exact session and no memory
> or agreed points or input had been forgotten or lost. Whatever we need to
> make that happen we should do. If we can do that in a super efficient way
> even better (efficient means faster to switch in/out and fewest tokens
> required to do those steps) but NEVER at the expense of memory or context
> quality.

Two clauses in that statement are load-bearing and easy to lose:

- **"against the agreed stage/ladder/plan"** — the record is *structured
  against the ladder*, not free prose. Position is a location, not a
  narrative.
- **"never at the expense of memory or context quality"** — efficiency is a
  tiebreaker between designs that all preserve fidelity. It is never a
  reason to record less, read less, or skip a step. This wording exists so a
  future session cannot re-derive "light mode" from first principles.

The failure this removes, in Tony's terms: *"it does not understand or
remember what we were working on and drifts or makes new decisions or just
assumes what we were working on, and I have to intervene and course
correct."* Detection is late — *"I sometimes don't notice until we have been
working for a while, but the drift could have come in from the start."*

Three drift signatures, all named by Tony, all of them a claim or action
contradicting a written record:

1. Working on something that was not what we agreed last session
2. Asking questions we had already answered
3. Making a statement that contradicts his input from last session

Value, in units:

- **Course-correction interventions per pickup: 2 observed → 0.** Baseline
  measured on this session's own pickup (2026-08-07): the session planned
  against a premise a shipped feature had already voided, then optimised
  token cost when the goal was fidelity. Both are signature 1.
- **Ladder position available at pickup: no → yes.** `tools/diagram/progress.py`
  derives the exact rung of all 17 slugs from disk. Switch-in does not read it
  (gap 1 below).
- **Agreed points lost across a boundary: unmeasured → 0, and measurable.**
  There is no check today that a pickup restored what the close recorded.
- **Read-set determinism: unbounded → bounded and complete.** The newest
  session log is read "in full" with no bound; today's is 1,005 lines
  (gap 2).

Explicitly *not* value here: fewer tokens. Efficiency gains are welcome and
are ranked last, behind every fidelity property above.

## Grounding

- skills/switch/SKILL.md — the flow under test; the boundary is defined once, here
- docs/state-contract.md — the same-turn rule and artifact ownership
- docs/plans/2026-07-03-context-history-split.md — why state/work/history are three files, and the "CONTEXT.md must never become a diary" edge
- tools/gates/README.md — the ladder vocabulary the record must be structured against
- tools/diagram/progress.py — the derived-from-disk position renderer
- CONTEXT.md — standing decisions bind: derived-from-disk, older logs are archive, a risk without a countermeasure is a blocker

## The gap list

Every row measured 2026-08-07 against the requirement above. Each cites its
evidence; none is inferred.

### Gap 1 — the one artifact stating our exact ladder position is the one the pickup does not read

Switch Out step 4 and Switch In step 8 look for `docs/project/progress.md`,
`progress.md`, "or similar". Neither file exists. The repo's actual position
record is `docs/plans/progress.{excalidraw,svg,html}`, generated by
`tools/diagram/progress.py`, which reports all 17 slugs and their exact rung
on the ladder — and is CI-refused if stale, so it is always current.

Evidence: `ls docs/project/progress.md progress.md docs/progress.md` → all
absent, run at this session's pickup. The board was seen this morning only
because it was run as a *smoke test*, not because the pickup asked for it.

Violates requirement 1 ("record EXACTLY where we are against the agreed
stage/ladder/plan") directly.

### Gap 2 — "read the newest log in full" is unbounded, and was silently truncated at this very pickup

Switch In step 6 says to read the most recent file in `kivna/sessions/` in
full. Logs are per-day and multi-sitting: `kivna/sessions/2026-08-06.md` is
1,005 lines across ten sittings. At this session's pickup the model read
lines 856–1005 — the last sitting only — and reported doing so.

That was an efficiency call traded against fidelity, made by the model,
unprompted, on the same day the requirement forbidding exactly that was
stated. It is the failure mode in miniature: the instruction is unbounded, so
the reader silently invents a bound.

Evidence: this session's transcript; `wc -l kivna/sessions/2026-08-06.md` → 1005.

### Gap 3 — the prune instruction has never fired, in 34 closes

Switch Out step 1 says to prune superseded decisions from CONTEXT.md.
Measured across every revision of the file:

| | first (2026-07-03) | latest (2026-08-06) |
|---|---|---|
| bytes | 5,074 | 54,566 |
| decisions | 12 | 59 |

Monotonic across 34 revisions. One dip in five months, of 183 bytes, with the
decision count unchanged. Key Decisions is now 88% of the file.

Evidence: `git log -- CONTEXT.md` with per-revision size and `^- ` count in
the Key Decisions section.

### Gap 4 — erosion is unguarded, and unexercised only because nothing prunes

Tony's predicted failure (2026-08-07): a long session records deep state,
then a short session's switch-out overwrites CONTEXT.md and rewrites TODO
`## Now`, losing the earlier record. The mechanism permits it — CONTEXT.md is
overwritten in place and no rule requires the new version to be a superset of
the old minus what is genuinely superseded.

Observed incidence: **zero in 34 closes.** The reason is gap 3: nothing ever
prunes, so nothing can erode. The risk is real and untested, not absent.

### Gap 5 — erosion and accretion are the same knob

Gaps 3 and 4 are opposite failures controlled by one dial. Pruning harder
fixes accretion and creates erosion. Pruning never is erosion-safe and is
what produced the 54KB file. Any countermeasure that treats them separately
will trade one for the other.

**Tony's countermeasure, proposed 2026-08-07:** pruning is licensed only at
defined events — a goal record landing (`docs/gates/*-goal.md`), or an
explicit agreement to drop something. Between those moments CONTEXT.md is
append-only. Both triggers are events rather than judgment calls, which is
the property that makes them safe: a short session can never prune, because a
short session is not a goal moment.

This reuses machinery that already exists. Conductor close-out step 6 already
fires the release pass on a goal-record landing (v0.86.0). Seven goal records
exist today.

Open at design: the trigger answers *when*. It does not answer *what* is safe
to remove — that criterion is undecided.

### Gap 6 — even removals add text

Four skills have been cut (capturerequirements v0.73.0, sherpa v0.74.0, mode
v0.75.0, trim v0.87.0). Each cut **added** a decision bullet to CONTEXT.md
recording the cut. The net decision count has never fallen.

Evidence: CONTEXT.md Key Decisions bullets for the four cuts; the git table in
gap 3 shows no decrease at any of those four releases.

### Gap 7 — the cross-day log erosion path

Switch In reads only the newest session log; older logs are archive by
explicit decision. Logs are per-day. A fat day followed by a thin day means
the next pickup reads the thin log and never sees the fat one.

Same-day sittings are safe (they append to one file). Across days it is
unguarded. Nearest historical instance: `2026-03-18.md` (112 lines) followed
by `2026-03-19.md` (20 lines). The recent high-intensity period
(2026-08-03 → 08-06: 301, 921, 289, 1005 lines) has no thin day, so the path
is currently unexercised rather than safe.

Evidence: per-log line counts across all 34 logs.

## Risk ledger

| Risk | Killer? | Impact | Likelihood | Evidence | State | Countermeasure | Review trigger |
|---|---|---|---|---|---|---|---|
| Pruning to fix accretion introduces erosion — a short session's close overwrites CONTEXT.md and drops an agreed point recorded by an earlier, deeper session | yes | a lost agreed point IS the failure this work exists to remove, so impact equals the declared value — fatal by impact alone if left without a countermeasure | medium — mechanism confirmed and unguarded, but incidence is zero across 34 closes because nothing currently prunes at all | gaps 3-5: git history of CONTEXT.md shows monotonic growth 5,074 to 54,566 bytes and 12 to 59 decisions, one 183-byte dip in five months; no rule requires a new version to be a superset minus superseded | countermeasure - permanent | pruning is licensed only at defined events — a goal record landing (docs/gates/*-goal.md) or an explicit agreed drop; CONTEXT.md is append-only between them, so a short session structurally cannot prune (Tony 2026-08-07) | the first prune that removes something a later session needed re-argues this row |
| Reading more at pickup fills the window faster, forcing more boundaries — each boundary is itself a fidelity risk | no | more switches per unit of work, compounding every other risk here | high — CONTEXT.md at 54KB is already a large share of a cold reload, and every gap fix adds to the read set | measured 2026-08-07: Key Decisions is 88% of CONTEXT.md; the newest session log is 1,005 lines | accepted unknown | | design rung — a design that increases the read set without a compensating reduction re-argues this row |
| The fidelity property stays unfalsifiable — no check exists that a pickup restored what the close recorded, so every countermeasure here ships unverified | no | the work cannot be proven to have worked; regressions stay invisible exactly as they are today | high — no such check exists anywhere in the repo today | analysis 2026-08-07: switch has no post-pickup verification step in any mode; the only evidence of continuity is that sessions feel continuous | accepted unknown | | design rung — this must be answered before the build rung, or the goal gate has nothing to measure |
| "Read in full" instructions get silently bounded by the reader when the target is large | no | the pickup reads less than specified and reports it as complete, which is indistinguishable from a faithful read | high — one observed instance on the same day the requirement was stated | gap 2: at this session's own pickup the model read lines 856-1005 of a 1,005-line log and reported doing so | accepted unknown | | design rung — bound the read set explicitly, or restructure logs so the bound is structural |

## Killer risk, read out

Erosion is the killer, and it is qualified rather than merely named: its
impact equals the declared value, which makes it fatal by impact alone at any
likelihood if left uncountered. It is not fatal here only because a permanent
countermeasure exists — Tony's licensed-events rule. Remove that rule and this
work item is dead by its own standard.

The three accepted unknowns all carry the design rung as their review
trigger. None may reach the build rung unanswered.

## What this is not

- Not the `light`/`low` modifier cut. That is queued separately and is a
  consequence of this requirement, not this work item: both modes are specced
  to reduce what the pickup reads, which requirement 5 forbids outright.
- Not "auto-sizing". That row was filed 2026-08-06 against a cost complaint
  that `vault-unhook` (v0.83.0) had already resolved. Premise-dead; closed.
- Not a CONTEXT.md size-reduction exercise. Size is a second-order efficiency
  concern and is explicitly ranked below fidelity.
