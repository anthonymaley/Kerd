# Real-project journey: Seinn

Status, 2026-09-09: real local deliveries and independent reviews demonstrated;
continuation still needed prompting. Not full adoption or a completed Out/In cycle.

The person first named Dinner Tonight, then rejected a proposed small item
because that project needs more fundamental work. They selected Seinn and
confirmed the existing approved-device access visibility item with `y`. After
the proposed screen, data meaning, checks and boundaries were shown, they said
`yes` to implementation. Delivery continued through local builds, fixtures,
native component rendering and a real Claude review without routine “next” turns.

Authoritative work and current question are in the Seinn checkout:
`/Users/anthonymaley/development/product/seinn/docs/work/device-access/work.md`.
Its `results.html` contains the visible result and explicit limits. Seinn's
CONTEXT, TODO and playbook point to that record. Do not duplicate its agreement
or treat this Kerd note as producer acceptance.

Observed: 48 production-decoder/display checks pass; full iOS/tvOS simulator
builds pass; native SwiftUI component snapshots cover 320/390pt and large text.
Claude Opus 5 performed a read-only assessment; its useful reproduction and
evidence-scope findings were handled. Actual prompt, reply, requested/observed
model distinction, usage and contribution are saved in Seinn's work folder.

Still unproved: complete authenticated-screen layout/VoiceOver/action behavior;
Out followed by In; host automatic discovery;
speed/resource improvement over the old flow. Component snapshots are not a
full-app screenshot. No installation of candidate Kerd, live-device deployment,
server change, commit or push occurred. The real-work trial used explicit
candidate instructions in the existing session, not a fresh session-local
plugin launch. No model job remains active.

## User feedback: easy delivery, insufficient visibility

After delivery, the person praised the approval → visual → execution → result
experience, but did not know Claude had reviewed the work and could not clearly
follow the process. This is positive evidence for ease, not a pass for visibility
or final acceptance of the Seinn feature.

They agreed to a live task list like Superpowers: tasks stay visible where the
host supports it, with named model handoffs and actual work unfolding underneath.
The candidate now routes this through its journey guide and delivery/job guidance.
Native task controls are preferred; compact transition updates are the disclosed
fallback, not a claim of a pinned message. No new approval stops or tracker service.

The instruction change still needs a real-use observation: can the person tell
what is done, who is working, what happens next and whether they are needed,
without opening an evidence report? Static validation cannot establish that.

## Subsequent result acceptance

The user answered `yes` to “Does the approved-access summary look right to you?”
after the result was reopened and the remaining evidence limits were repeated.
Seinn's work record now closes its user-review step and local delivery. M4 is
still partial; no full-screen/VoiceOver pass, release or global Kerd adoption is
implied. The updated native task-list experience still needs a user-visible run
on the next genuine work item, not a rerun of this completed feature.

## Second work item: loader fix and handoff

Source: the user's pasted Claude transcript on 2026-09-08. These are reported
observations; this follow-up did not independently inspect the Seinn branch,
rerun its checks or change its files.

- After mount access and task-tool setup were corrected, Claude showed a native
  task list. Setup narration and recommendation reports were still too long.
- The build initially stopped on `dontAsk` permission denials: the requested
  compiler had not run. This was execution setup, not a compiler failure.
- Claude subsequently reported 26 fixture checks, iOS/tvOS builds, reviewer
  corrections and a standalone clean-worktree check. Real playback was not
  verified. Following the user's explicit “commit and push,” it reported
  `47733d2` on `fix/resource-loader-registration-race`, not merged into main.
- A later copied continuation prompt said no commits/pushes. The controller's
  stale prompt contributed to confusion; Claude proposed deleting the branch
  even though no undo had been requested. Then “go” after “leave it and move
  on” was used to start earlier unapproved cleanup items.
- Repeated “everything is done” summaries obscured local documentation changes
  and the difference between a pushed branch and merged work. Shared TODO
  edits also crossed the supposedly isolated commit boundary.

Agreed response: a focused instruction correction, not another review of the
same implementation. Interpret a reply in its current decision context; preserve
earlier authorized history while honoring new limits; use precise delivery facts
and one clear final handoff. Keep unrelated diff hunks out of scoped commits.
The work record carries the pending decision, not only the host task list.

Static reasoning checks for the updated instructions (not model-run passes):

| Situation | Required behavior |
| --- | --- |
| “Yes” to a specific, unchanged local fix | Apply and verify within that authority, without another routine confirmation. |
| “Go” after leaving a completed branch alone; no next item selected | Do not choose an old cleanup; resolve the next outcome if no authorized work remains. |
| Authorized push already happened, followed by “continue; no pushes” | Preserve the push, honor the new limit for future actions; no unsolicited deletion or duplicate build. |
| Pushed fix, uncommitted correction, no merge | Report all three facts and the evidence gap; don't equate pushed with merged or amend published history by default. |
| Completed work, unchanged inputs, repeated “continue” | Give the existing result briefly; no new tests merely to look active, and no new scope. |
| Explicit request to undo a push | Resolve exact targets and effects under that new authority; don't dismiss a real reversal as a stale prompt. |

These cases were checked against the written guidance only. Native task use is
visible in the supplied transcript, but the revised decision/handoff behavior
still needs observation in real use. No broader adoption or user-comprehension
pass is claimed.

## Third work item: date-filter performance change

The user requested a performance pass and then authorized the proposed local
date-filter optimization, tests, both app builds and independent review. The
controller announced implementation but stopped before starting; the person had
to ask “so are you doing it?”. That is a continuation failure, not an approval
requirement. Work subsequently completed without further implementation approval.

Controller-observed result on 2026-09-09: cutoff computed once per batch,
74 date-filter checks passing, 26 loader and 48 device-access regression checks
passing, and successful iOS/tvOS simulator builds. A separate Claude Opus 5
read-only job returned no blocking finding. Its feedback strengthened cutoff-call
counting, consecutive-batch checks, DST cases and source identification. A scratch
mutation that still computed per entry failed the strengthened check. Requested
effort was high; actual effort was not observed. This is code/test evidence, not
measured app latency or playback evidence. Builds included the existing dirty tree.

The authoritative result, prompt and returned review are in Seinn's
`docs/work/performance-pass/` (local checkout
`/Users/anthonymaley/development/product/seinn`). Work is uncommitted; no device,
server, deployment or push occurred. Other uncommitted features were preserved.

The user again asked “okay....next?” after the final result. Do not turn that
into authorization for another Seinn fix or mark the experience fully passed.
Ordinary save/fresh-session pickup remains an adoption evidence gap, but this
does not select Seinn for another test or make its closeout a Kerd task. The user
explicitly separated the repositories: Seinn supplies testing evidence only.
Its future changes or boundary need their own project-specific request. The
prior accepted Switch device trial stays accepted and is not to be repeated.
