# Contribution evidence

## Preparation

The saved agreement authorizes a Claude draft and independent Codex review.
Prompt checked against outcome, audience, success, edit authority and stop point.
No new interview, approval, install, commit or publication is required.

- Draft choice: Claude Code 2.1.261, explicit `claude-fable-5`, native effort
  `medium`. Installed CLI help names this exact model and supports medium.
  Shared Claude guidance `2026-09` applies: direct outcome and descriptive XML
  separate instructions from source context. No matching Fable-5 family profile
  exists; the Fable-5.1-specific effort advice was not applied. Medium is a
  controller judgment for this bounded writing task, not a measured optimum.
- Review choice: Codex CLI 0.153.4, explicit `gpt-5.6-terra`, native effort
  `medium`. Local CLI model catalog lists the model, medium support and medium
  default. Shared OpenAI and GPT-5.6 family guidance `2026-09` apply to the
  outcome-first review brief. Terra is chosen for judgment across prose and
  source code; no comparative quality or cost claim is made. These profiles
  remain unevaluated guidance.
- Transport: candidate's packaged `skills/conductor/scripts/ask.py`, current
  project `/tmp/kerd-job-integration.gKNK7m`. Private transport metadata remains
  in this project's Git metadata; shared evidence omits native session IDs.

## Jobs

Draft request `model-help-draft-1`, local alias `model-help-draft`: completed,
provider exit 0. Provider confirmed `claude-fable-5`; medium effort requested.
Created the guide, which the controller read on disk. Source-based delivery
note covered all requested topics and disclosed deliberately simplified
failure/cancellation detail. Provider-reported estimated cost: USD 0.779129;
this is reported usage, not a verified invoice or an available-account balance.

Review request `model-help-review-1`, fresh alias `model-help-review`: completed,
provider exit 0, without editing authority. Prompt includes exact snapshots of the
agreement, actual draft and named source files. Source inspection established
that the runner adds “do not ... run shell commands” to all read-only jobs.
Supplying snapshots allows Codex review without shell-based file access or
widening authority. This safe workaround is not explained in model-jobs.md and
is a practical usability gap in the candidate's Codex review route.

The requested Codex model was `gpt-5.6-terra`, effort `medium`. The runner parsed
no observed Codex model identity; the requested value is not represented as an
observed identity. [The actual reply](review-result.md) found all required
topics substantially covered, with one spending-claim correction and one
optional update clarification. Both were applied and source-checked. No model
comparison, effort sweep, cancellation experiment or end-user trial was run.

The parent acknowledged the read-only command limitation and plans a separate
candidate correction. This result does not review or certify that later change.
The current work stopped after handling supported findings and saving evidence.
Both completed local aliases were closed successfully; saved results remain
retrievable in the temporary project's private transport metadata.
