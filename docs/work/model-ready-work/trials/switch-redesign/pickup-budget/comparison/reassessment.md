# Restoration reassessment

The paired candidate passed the numeric target. Claude judged that first answer
a conditional restoration pass, with source-identity, playback-evidence and
deeper-reading-route repairs required before affected work. Its complete review
is retained in claude-review.md. Legacy lost substantial restrictions.

The exact final-navigation-wording repeat also passed the numeric target, but
its independent review did not pass the retained answer: add_shares disappeared,
an unresolved privacy experience was obscured by a settled-decision label, and
several operative conditions became labels. These were already in the input.

Conductor accepts the source-backed omissions, not every review inference.
Claude's alleged branch-mismatch issue is not a failure here: this experiment
explicitly uses local branch pickup-comparison and excludes the old device
handoff. Both answers attribute identity to the local snapshot. Historical
handoff branch instructions do not change this measurement's target. Backlog-only
facts outside the selected source set are scope limits, not invented requirements
to reread all history.

Repeated report compression is the observed problem. Do not keep adding
case-specific prohibitions or report the numerical result as completion.
Separate a brief user display from source-backed retained working state.
The In/Out guide now makes that separation explicit and keeps open choices,
operative constraints and deeper-reading routes distinct. Original source records
remain authoritative; no parallel living rules ledger is introduced.

One bounded source-preserving repeat will test this changed representation:
same snapshot, source set, native model/effort and targets; short display plus a
portable working-state annex preserving operative source wording. The annex is
an experiment artifact, not a required file/schema for skill users. Its generated
tokens count in the context measurement. The preceding final control is the
pre-control; a new post-control follows. Retain every earlier result unchanged.
An independent review assesses the returned display AND annex; no preloaded
corrected answers. If meaning still fails, stop the wording-repeat approach and
report the unresolved restoration limitation rather than retrying indefinitely.
