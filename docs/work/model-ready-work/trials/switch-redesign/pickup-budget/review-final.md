## Independent assessment — prepared-input pickup (preserved run)

**Verdict: no blocking findings.** Restoration is faithful to the supplied records; the numeric observations reconcile.

### Fidelity of preparation
`submitted-pickup-preserved.md:255` reproduces `CONTEXT.md` byte-for-byte against `/private/tmp/kerd-pickup-budget.qfKGD3/project/CONTEXT.md:1-111`, and `:260` reproduces `TODO.md:8-39` — `## Now` with both `### Owed` children, complete, no truncation.

### Restoration checked item by item
- **Position** — "nothing in flight, no active Conductor build", Sherpa Build 4/5, missing Build-exit ruling: `CONTEXT.md:14,19-20`. ✓
- **Last outcome** — 4/4 rollout, build 339, agent 2.3.1, explicitly marked saved-not-probed: `CONTEXT.md:15-17`. ✓
- **Next obligation** — Krutho notice re 2.3.1 and the zero-hash-LAST deviation, with `3c9e976`, carried *with* "Do not send a message merely because it is listed here": `CONTEXT.md:22-24`, `TODO.md:14-17`. ✓
- **Next decision** — producer's TV-failure/queue-halt call, both defects open, "Do not implement a reasonable default": `CONTEXT.md:29-34`. ✓
- **Restrictions** — all five Standing protections (`CONTEXT.md:38-52`), all six non-reopenable decisions (`:56-75`), all three evidence caveats (`:79-90`), plus TODO's "wire half is DONE", staged D1 file, and composer-not-drive-by framing. ✓
- **Unresolved** — interim-token migration set, wrong-target deletion cause, all four Owed-tail items, and all four archived questions (`CONTEXT.md:102-105`). ✓
- **Invented fact** — none found. Git identity is attributed to the controller, `synchronized:false` is respected, `reads.supporting_sources_opened` is empty and consistent with `observed_operations: []`, and no token-budget claim is made (the prompt forbade one).

### Two non-blocking gaps, named
1. `CONTEXT.md:26-27` ("only memory/handoff work is authorized — no client, server, device or external communication operations") appears in the answer only inside `authority.current_scope` as request-derived scope, not as a cited record-level prohibition. Effect is identical (execution barred), so it does not change the next action — but it is the same class the prior review flagged.
2. Preparation supplied CONTEXT + TODO `## Now` only. `CONTEXT.md:32` routes the producer decision to "TODO masking row" = `TODO.md:53`, which was **not** loaded; it adds "asked three times on 2026-09-06, unanswered — it gates the plan" and the code sites. A coverage check against loaded records cannot see this. Non-blocking for a report-only next action; it is a limit of prepared-input selection, not of the reader.

Everything else absent (Key-Decisions read routing at `CONTEXT.md:96-101`, on-open gate re-arming, revocation-cascade sequencing) is future-feature detail, not next-action material — archival inventory, not a blocker.

### Arithmetic
18,275 − 13,026 = 5,249 added input (< 8,000). 21,769 − 13,026 = 8,743 = 3.383% of 258,400 (< 5%). Controls identical at 13,026 → zero spread.

### Limits
I ran no sessions; all token figures are controller observations I only re-derived. I read `preserved-post-control.json` but not the pre-control file (evidence-asserted). I could not verify tree cleanliness (no shell). Single corrected local case; no old-Switch baseline, no GitHub pickup, no selector-cost claim.

## Controller disposition

No blocking finding remains for this report-only prepared-reader trial. Both bracketing controls were checked by the controller and match; the isolated tree remains clean at the pinned revision. Accept the scoped numerical and restoration result, not a general Switch In or source-selection claim. Before acting on the TV decision, retrieve its full linked TODO entry; the report-only trial did not need to execute it. A cited record-level restriction and equivalent request restriction have the same effect here, but neither broadens authority.

