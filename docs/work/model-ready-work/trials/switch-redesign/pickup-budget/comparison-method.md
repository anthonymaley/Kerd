# Matched local pickup comparison

## Final wording repeat

After the first pair, the candidate guide gained explicit named-reading-set and
current-position-first navigation. Retain the first pair unchanged, then repeat
only candidate pickup with those exact final instructions and the same raw
sources/model/task. Use the first pair's post-control as the new pre-control and
run another post-control. This is a documented revised-instruction observation,
not permission to replace the first candidate result with a preferred one.

## Original paired protocol

Declared before these runs. Continue the original Switch measure, not a new
device trial. Use one isolated Seinn clone pinned to
2302c2a3f92fb1c094b23b3c83eb67b9d79b8a83 on local branch pickup-comparison.
Do not edit project files, fetch/push, execute product work, probe devices or
load native conversation histories. Private run records live in Git metadata.

Compare two supported local strategies on that identical saved project:

- Legacy: full current live Switch instruction text, with its full-file pickup
  reads. The measurement's read-only/no-network scope overrides pull, smoke
  tests, mode writes and operational actions. This is local pickup, not full
  legacy runtime compliance or a deployment benchmark.
- Candidate: full current Switch and In/Out guide, with raw CONTEXT and complete
  TODO Now section assembled by the existing helper. Those sources were selected
  by the earlier Out/reader work, not by a hidden new model in this measurement.
  Further source reads remain available for genuine gaps. Source preparation
  wall time is measured separately; all supplied source text counts in native input.

Use the existing read-only App Server measurement route and requested
gpt-5.6-sol/high for both, preserving the earlier experiment's model/effort
choice. No new model-optimality claim. Run control, legacy, candidate, control
as fresh sessions. Save exact prompts and sanitized native usage/results.
No correction run may overwrite these first observations.

Use the smaller bracketing control first-input count as estimated host overhead;
report control spread. Subtract it from each pickup's maximum latest-request input
and total context, never cumulative billing. Candidate must meet both original
8,000 added-input and 5% usable-context targets and pass restoration review.
Missing usage, compaction, materially drifting controls or restoration failure
prevents a pass. Report elapsed time, not a promise of general speed/cost savings.

Equivalent restoration questions cover actual position/active work, last result,
next obligation without invented authority, unresolved producer decisions and
all applicable restrictions from loaded current records. A short answer does not
prove complete recall; independent review compares it with source material.

This paired observation can establish the local prepared-entry strategy on one
snapshot, not automatic source selection, end-to-end Git synchronization cost,
every project's memory needs or statistical superiority. These remaining limits
stay explicit even if the two numerical targets pass.
