# Continue the active device trial — not ordinary Seinn pickup

Enter only on the laptop in `/Users/anthonymaley/seinn-test`, origin
`git@github.com:anthonymaley/seinn.git`, branch `kerd-switch-trial-20260906`.
Before updating files, require a clean checkout and fetch the branch. Compare
FETCH_HEAD with the exact revision supplied by the Studio controller. If it
differs, stop without merging, resetting, stashing or substituting another flow.
Then fast-forward only. Check local execution: a session still running on Studio
over SSH is not the second-device test. Missing or conflicting records stop.

The controller's pickup instruction must confirm that its owned source worker
has exited after the verified GitHub save. This file was written before that
release and is not itself proof it occurred. No source process or conversation
is migrated, and this does not end the user's Studio controller conversation.

Read the complete `agreement.md` and `place.json` in this folder first. They
carry the actual active work, measures, limits, evidence and next action. The old
`../device-handoff.md` describes the completed READ-ONLY memory trial; do not run
that old trial again. No installed Switch, Conductor, gate, generic project pickup,
root memory sweep, Seinn readiness check or operational probe is needed here.

Show briefly: **Studio saved → source exited → laptop restored → continuing**,
marking only observed steps complete. State the restored next action and continue
it in this same turn under the agreement. A successful read is not task completion.
Use the original tests as well as new coverage. No further routine “okay” is needed.
Read deeper only when relevant to this folder's task.

Finish with actual computer identity, branch/revision, restored position, changes,
checks run/results, remaining gaps and measures met/not met/unassessed. Distinguish
source-release evidence supplied by the controller from local observations.
Only report token/context cost if actual readings exist; file sizes are not tokens.
Keep the saved source checkpoint immutable; return the new result in conversation.
Do not commit/push or claim final independent acceptance. The user will bring the
result to the controller for the independent assessment.
