# Arrival fidelity: final review and bounded replay

Established Claude partner; unchanged native model, effort and permissions.
Opus 5 2026-09 scope/evidence guidance reused; applied effort unverified.
Codex owns publication as 0.121.1 (bug-fix wording/control flow, no new surface).
Do not edit shared files or start a competing release. Scratch fixtures only.

Review the current diff in Switch SKILL.md and references/in-out.md, Conductor
SKILL.md and references/journey.md, and README's Switch usage. These implement
your evidence reply: validate NOW before rendering; successful complete stdout
is the final message unchanged; correct input and rerender; only actual failure
uses the disclosed fallback. Owner labels contain owners, not dependencies.
Flag a consequential conflict or missing correction before calling this ready.

Then exercise that instruction with the actual Leru case you already inspected.
This is a bounded replay, not an ordinary In, consumer inspection, installation
or human acceptance. No new model jobs, lifecycle, bindings, settings or sends.
Do not re-read native history. Use the existing guide's JSON example as the shape
and the known Leru facts: device check of the installed live-stats panel is the
current action; recording Tony's result is necessary follow-through; heartbeat
is a separate unapproved build. Tests and installations are saved observations,
not reverified. Main was four ahead with no push approval. Keep immediate limits;
do not turn the long TV procedure into NOW. Source document is TODO.md in Leru.

You may create summary.json and rendered.md only under
/tmp/kerd-arrival-fidelity.ACPExB/ (or return a blocker if inaccessible).
Use the repository renderer with --summary and --markdown, as the guide
specifies. Save its complete stdout to rendered.md and inspect it. Do not edit
the rendered output; correct summary input and rerender if necessary.

If review finds a defect, return numbered findings with minimal correction
instead of a dashboard. Otherwise your final reply, inside Agent's ordinary
request markers, must be the complete rendered.md contents unchanged. This
lets Codex independently compare the retrieved assistant text with stdout,
and inspect the summary selection. No review preamble or approval suffix:
the dashboard-only reply means no consequential finding in this bounded review,
not a claim of general compliance or acceptance.

Brief correction after dispatch: the sent request mistakenly named
`--format markdown`; the actual guide and CLI use `--markdown`. The private
request retains the original wording. This correction is not a renderer change.
