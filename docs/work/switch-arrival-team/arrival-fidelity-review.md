# Arrival fidelity: evidence and correction review

Existing Claude partner; keep native model, effort and permissions. Local Opus 5
2026-09 scope/evidence guidance reused; applied effort unknown. Read-only shared
tree. No release, consumer edits, bindings, settings or lifecycle changes.

<objective>
Anthony supplied your Leru 0.121.0 comparison and told Codex: “work with them to
fix this finally then lets release and try again”. Codex owns implementation and
the next release, after your independent review; please flag a competing release
or edit immediately. Do not start a parallel release.
</objective>

<contribution>
First confirm the evidence/root cause against the native Leru material you
already inspected. Return the exact NOW input items, whether heartbeat was
already present before rendering, and the released In instruction you meant by
“show the renderer's Markdown directly”. I find that phrase in Out only (around
in-out.md:573), not In. Confirm or correct me, without rereading whole histories.
Your source/final comparison is reported evidence until we inspect its selected
artifacts. Retain a small sanitized excerpt if needed; no private IDs in shared
records, no new archive or consumer writes.

Proposed correction I am implementing: Conductor composes the summary before
rendering; NOW selects the immediate action and its necessary follow-through,
not a future unrelated build awaiting separate approval. Keep owner labels just
owners, with the dependency in action text. Review summary scope before render.
Switch's final operation renders; final assistant text is that complete stdout,
unchanged, with no second prose pass. If a correction is needed, amend input and
rerender. If output is truncated, retrieve the complete result instead of filling
gaps. If genuinely unavailable, disclose a plain-text fallback. No new schema,
service, hook or semantic parser to guess which work is unrelated.

Is there an overlooked conflicting instruction or narrower effective correction?
Return findings, not an approval of code you have not seen. After implementation
I will send a final focused review request. Do not manufacture a live pickup or
ask Anthony for another dashboard verdict during this work.
</contribution>

<limits>
This first request is read-only evidence/coordination, not implementation or a
fresh model job. Use already-selected relevant material only. No new workers,
commits, pushes, settings, installs, bindings or shared record edits. One reply.
</limits>
