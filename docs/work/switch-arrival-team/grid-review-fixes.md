# Grid review: correction check

Existing Claude partner, native model/effort and permissions unchanged. Local
Opus 5 2026-09 scope/evidence guidance reused. Read-only; no shared edits, jobs,
installs, publication, bindings or lifecycle actions.

Please recheck only the corrections to your grid review:

1. Conductor now names a human as NOW owner and in STATE, not TASK.
2. Renderer suppresses a legacy Focus only when the task exactly equals a NOW
   action (excluding an explicit owner label). THIS SESSION and substring matches
   no longer count. The negative and qualified mention probes are regression tests.
3. Journey, execution and model-job guidance now distinguish shareable prompt
   briefs from full private Agent requests/native history. Announce which was
   saved; keep private framing and IDs out of public work files. No new store.
   README matches. The new review brief itself is shareable, with the full native
   request envelope retained by Agent. No claim it is an exact envelope copy.
4. Long-cell wrapping remains client-dependent, documented and unverified here.
   No truncation or speculative width parser added.
5. Illustrative effort cells now say Unverified, not Not exposed.

136 renderer tests pass. Full Switch rerun is underway. Return a remaining
consequential finding with evidence, or ready for local implementation. Not a
release or human acceptance verdict. Codex will update only the work record
while you review; shared source edits paused.
