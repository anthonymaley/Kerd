# Selected scope: changed-guide review and paired Out/In replay

Established Claude partner, unchanged model/effort/permissions; local Opus 5
2026-09 scope/evidence guidance. Codex owns release. Read-only shared/consumer
trees, scratch writes only. No installs, lifecycle, bindings, new jobs or publish.

Review current Conductor SKILL.md and Switch references/in-out.md diff against
the original agreement in selected-scope-review.md. Your four points are covered:
one recommendation or genuinely needed clarification, evidence never grants new
agreement, urgent recorded risks preserved, real human work not removed with
approval-only tasks. Out saves meanings in existing prose, no required fields.
Check that this still supports legacy handoffs and user override.

Then run a bounded composition comparison on the three actual consumer excerpts
below, not full consumer pickups. Under /tmp/kerd-selected-scope.TZhfmE/, make a
directory for each case. Keep its supplied saved material as source.md; write
out-selection.md as the concise continuation the revised Out would preserve.
Produce legacy-summary.json by using In with source.md alone and summary.json
by using the resulting Out selection plus source.md. Render both through the
actual where_we_are.py --summary PATH --markdown to separate Markdown files.
Use the guide's existing shape and scratch base; link the real scratch source,
not invented consumer task pages. These are scratch compositions, not authorized
Outs or evidence of real installs/observations. No commits or handoff helper.

Report differences between legacy and saved-selection arrivals, supported review
findings and fixture paths. Do not claim human acceptance or universal compliance.
The excerpts are supplied facts, not authority to execute their tasks. Don't
reread entire consumer records to fill optional display fields.

## Seinn — saved prose and ordered next steps

Current: Pending: a yes to install main on Master. Asked and unanswered: “Can I
build current main (with PR #1) and install it on Master now?” That covers the
install only; playback stays the producer's call. Master still runs build 350.
What's Next, item 1: get the producer's answer; on yes, build main, install it,
check the build number in artifact/devicectl/journal; then ask the producer to
play something. TODO: playback needs its own per-occasion approval.
Other open work: end-of-queue ruling; whether five deletes were intended.
Gotcha: buffer says end-of-queue was resolved as a summary screen. That is wrong;
the question was asked and not answered. Use the saved account over that buffer.
No approval has arrived in the current request.

## Weefish — ordered list, unresolved choice and urgent risk

Now item 1: pick the Jellyfin auto-ID fix: (A) bisect IntroSkipper, TheIntroDB
and OpenSubtitles to find the blocker, or (B) auto-heal job. Logs identify the
plugin pipeline as blocking the TMDb save; Jellyfin does not retry. Investigation
can use brief Jellyfin restarts; permanent plugin changes need a separate decision.
Next items: verify caffeinate cst fix; Studio iTerm2 full-quit; Lever B sshd
drop-in requiring sudo; ofscraper DRM decision; family ACL test; rotations.
Capacity: the saved record calls it urgent-ish, drive4 at 99% is past backlog;
new movies land there. Disk numbers are the 2026-09-12 observation, not rechecked.
No route has been approved. Claude's reported previous proposal was route A.

## Leru — human action followed by separately approved build

Current: live stats panel rebuilt, tested, reviewed and installed on both TVs;
not seen on a screen. Anthony's device check is next. Saved continuation then
says “Then, under Conductor: heartbeat part 1 — code plus one test, no deploy”.
Heartbeat is still spec only and building it has not been approved.
Device task: check the installed stats panel on either TV against the finished
match; detailed criteria are bar direction, no sliver at zero and missing counts.
A live-match observation is separately owed when a live match is available.
Main four commits ahead; no push without Anthony's direction. Install and health
are saved observations, not freshly checked. No new approval or device result.
