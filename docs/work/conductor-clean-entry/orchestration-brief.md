# Model-aware Conductor startup and release

Anthony extended the agreed clean-entry work: without choosing Conductor, work
stays ordinary host work on the current model/effort. Choosing Conductor assesses
every task for composer work, contributors and controller ownership, before work
and as new tasks emerge. Small work gets considered too, not a compulsory worker.
Prompt preparation must use the applicable model guidance before dispatch.
Anthony authorized implementation, collaboration with Claude, then release/test.
Codex owns the only release, planned 0.124.0. No plugin installation is requested.

Anthony clarified the invitation boundary: offer Conductor for substantial
build/design/workflow work whether new or existing, not only new projects. Small
fixes can stay direct. Explicit requests open it and established guided work
continues without a repeat offer. No host implicit-invocation metadata is changed.
The initial explicit-only design wording in orchestration-review.md is superseded
by this clarification, not an instruction to hide Conductor from existing work.

## Implementation contribution brief

Create only skills/conductor/references/orchestration.md. Keep it concise and
self-contained for the startup assessment. Conductor SKILL.md and existing
execution/journey/model-jobs guides are available sources; do not edit them.
Cover the current-session model/effort evidence and suitability recommendation;
unknown/configured/requested/observed distinctions; all-task assessment with
tiny inline cases and reuse on resume; composer as a function (possibly delegated
when useful), not a required extra model or approval authority; visible startup
grid even when no worker launches; model-specific prompt preparation and actual
preparation/dispatch/result transitions, linking to existing detailed guides.
No mandatory model tier, optimum-effort claim, settings edits, native transcript
survey or provider capability invention. Unknown settings don't automatically stop
safe work. Host permissions, role choices and approved boundaries remain binding.
No other edits, commits, installs, dispatch or consumer access.

Native worker model/effort inherited and not independently observed. No matching
model-specific tuning is claimed; this is the clear outcome-contract fallback.
The exact prompt is in native history; this is its shareable brief.

## Established Claude review brief

Review the whole changed clean-entry tree plus the new model/composer work when
ready. Check opt-in boundaries, task coverage (including tiny and emerged work),
startup settings evidence, no automatic mode/permission changes, useful independent
assignments, model-guide consultation and prepared prompts before send, actual
returned results, and preservation of managed continuity. Codex integrates and
releases; Claude is read-only. No competing release or new model jobs.
Existing Claude session/effort retained; observed model last review claude-opus-5.
Prompt guidance uses shared Claude/Opus 5 2026-09 and its narrow-scope advice.

Current official Codex configuration documentation distinguishes configured
model/effort defaults and overrides. It doesn't establish this session's current
effective pair. See [configuration reference](https://learn.chatgpt.com/docs/config-file/config-reference),
read 2026-09-14. No provider profile ranking or default has been changed here.
