# Composer restoration — prompts and dispatch record

## Assignment

Anthony authorized implementing, cross-reviewing and releasing the Conductor
composer/spec/player restoration together with the completed interactive-
orientation follow-up. He subsequently required comparison with v0.105.0.

Controller evidence: current host identifies the controller only as GPT-5 family;
effort is unavailable. It is retained for agreement, integration and release.
Composer request: `gpt-6-astra`, high effort, fresh context. No local model
profile explicitly applies to Astra, so the prompt uses the outcome-first fallback
from OpenAI shared guidance 2026-09 and makes that limitation explicit.

## Composer pass 1 — exact message

```text
Act as the bounded top-tier composer for Kerd's Conductor restoration. Work in /Users/anthonymaley/development/product/Kerd, but this first pass is read-only and must return only the smallest explicit list of named files you need for a safe execution score. Outcome: restore the effective v0.105.0 composer/spec/per-step delegation contract while preserving the valuable current safeguards, integrate cleanly with the already-uncommitted interactive-orientation follow-up, and define an end comparison against v0.105.0. Read root CLAUDE.md, current skills/conductor/SKILL.md, current references/orchestration.md and execution.md, and the historical v0.105.0 Conductor at git object 54c28cd:skills/conductor/SKILL.md. Also read docs/work/conductor-clean-entry/work.md only for current scope/authority. Do not read consumer repositories or transcripts. Preserve: honest requested/observed model and effort labels; current Agent/Codex/established-partner routes; no invented results; managed Conductor and permissions; no worker quotas. Design constraints already agreed by controller and Claude: command-verifiable is necessary but not sufficient; mostly-delegate is a bias for a well-factored multi-step score, not a quota; use capability tiers mapped through current guidance rather than frozen provider names; the score step can be the player brief but transport-specific missing facts remain; re-dispatch never changes agreed semantics, and score defects return to composer immediately with three failed player attempts as a ceiling; compare the next two real multi-step builds and final source/fixture behavior to v0.105.0 without claiming efficiency from counts. No edits, commits, release, install, dispatch or lifecycle actions in pass 1. Return only named repository paths plus one short reason each. Requested native controls: gpt-6-astra/high, fork none; effective runtime is not independently observed. There is no Astra-specific local prompt profile, so use the clear outcome-contract fallback rather than GPT-5.6-specific tuning.
```

The private native event retains transport framing. Later exact messages and
the composer's score path will be appended here before release.

## Composer pass 1 result

The composer requested these named files: model-jobs.md, guidance/README.md,
guidance/model-choice.md, journey.md, work-record.md, managed-conductor.md,
managed-decision.md, the interactive-orientation brief and fixture, test_ask.py,
and README.md. The controller accepted that bounded set.

## Composer pass 2 — exact message

```text
Pass 1 scope accepted. Read exactly the named files you returned, plus the already-authorized core sources from pass 1 (root CLAUDE.md; current skills/conductor/SKILL.md, references/orchestration.md and execution.md; historical git object 54c28cd:skills/conductor/SKILL.md; docs/work/conductor-clean-entry/work.md). Then write one execution score only at /Users/anthonymaley/development/product/Kerd/docs/work/conductor-clean-entry/composer-restoration-spec.md using apply_patch. Do not edit any other file. The score must be sufficient for fresh players: start with the agreed outcome, preserved safeguards and evidence limits; define each substantive step completely with What / Why / exact Files owned / Authority and boundaries / Verify with expected evidence, then tag it [delegate: route, capability tier/model, effort evidence] or [keep] only after the body. Bias a well-factored multi-step score toward delegation without a quota; give a concrete reason for each keep. Include: restored four-role definitions; composer two-pass call for non-trivial specs and disclosed controller fallback; spec-as-contract and per-step test for delegation; capability-based sizing mapped through current guidance rather than frozen provider brands; task-based session advice down and up; score step as reusable worker brief with transport additions only when needed; semantic re-dispatch/score hand-back rule with three failures as ceiling; integration with current Agent, managed Conductor, journey grid, work record and interactive-orientation changes; public README/trigger surface; meaningful validation; independent Claude review; one release step respecting CLAUDE.md; and an end comparison against git object 54c28cd/v0.105.0 plus two future real multi-step build observations. Do not rewrite published history. Separate tests we can run now from future acceptance. Avoid wording-only regex tests unless they bind a real contract. No version number is predetermined; the controller will choose semver at release. No commits, pushes, installs, consumer access, model dispatch or lifecycle changes. Return a short score summary and any unresolved design risk. Requested controls remain GPT-6 Astra/high; effective runtime is not independently observed, and no Astra-specific profile exists.
```
