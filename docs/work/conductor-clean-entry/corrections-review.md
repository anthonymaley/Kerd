# Entry review corrections

Shareable follow-up to the established Claude reviewer; existing model/effort
unchanged. Read-only. No new jobs, edits, release, installs or session changes.

All four findings from your implementation review are corrected:

1. Restored the cut status clause before the post-arrival action handling in
   Conductor SKILL.md.
2. Restored combined clarification/approval handling in Switch's composition
   section. A supplied fact alone remains insufficient.
3. Conductor entry now explicitly orders relevant Kerd guide reads, then entry
   frame and split, then substantive project work. The invocation example has
   that order too. Bounded local orientation remains allowed if actual stage/job
   boundaries are missing; no guessed agreement or a global reread requirement.
4. No selected authorized task means no contributor/model jobs merely to choose
   one. An explicit research request can authorize its useful contributions
   without a second staffing approval.

Please read those changed passages and confirm whether any consequential gap
remains. The managed runtime, renderer and saved-scope rules are unchanged by
these corrections. Tests before correction: Switch 379, Conductor transport 37,
packaging 9; validators pass. The relevant renderer/example tests and validators
are being rerun on the corrected text. No source test is model acceptance.

Independent source-guided forward simulation of three new turns (no consumer
operations): excluded key task opened direction-setting and asked only for the
new capacity operation; design yes entered Shape with a delegated constraints
survey; branching diagnosis assigned safe captured-evidence analysis without SSH.
This is proposed behavior, not a claim dispatch or installed behavior occurred.

Hold the worktree read-only; Codex will retrieve this request once. No release
is authorized by this review.
