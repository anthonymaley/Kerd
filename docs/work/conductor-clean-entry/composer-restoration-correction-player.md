# Composer restoration — repaired-score player prompt

Claude found the missing collateral gate; the Composer repaired the affected
score passage first. This is the exact follow-up sent to the original Step 2
owner. Requested controls remain Sol/medium; effective runtime is unverified.

```text
The composer repaired the score passage after Claude's review. Implement only that repaired collateral gate in your owned skills/conductor/references/execution.md using apply_patch; no other file. Require Conductor to inspect every returned Player edit's actual diff against the score step's owned boundaries, not only its verification output. Bulk deletions, renames and pattern-driven edits always receive that diff read. Require stating how any defect—including one caught in Conductor's own work—was caught. Preserve independent/seam review as additional, the existing failure-routing changes, and all current authority/model/managed semantics. Run the owned diff check and inspect the complete resulting hunk. Return the exact source location and evidence. No commit, push, release, install, consumer access, dispatch, settings/binding or lifecycle action. Requested controls remain Sol/medium; effective runtime unverified.
```

## Final execution consistency — exact message

```text
Claude confirmed the repaired score and five findings, then found two small consistency gaps in your owned execution.md; it says no further review round is needed. Correct only that file using apply_patch. (A) The mandatory full-diff rule for bulk deletions, renames and pattern-driven edits must explicitly include Conductor's own inline edits, not only returned Player edits; keep the existing how-caught rule. (B) The earlier inline retain-reason list still has unqualified briefing/coordination cost. State that a finished score step's remaining transport supplement is not itself such a cost, linking or pointing to orchestration as appropriate. Preserve all failure routing, score reuse and authority semantics. Run git diff --check on execution.md and inspect both resulting hunks. No other edit, commit, push, release, install, consumer access, dispatch or lifecycle action. Requested controls remain Sol/medium; effective runtime unverified.
```
