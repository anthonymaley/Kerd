# Ordinary entry implementation and honest Roll boundary

Established Claude partner; keep current model/effort/permissions. Read-only,
no shared edits, installs, consumer checks, native lifecycle or new model jobs.
Local Opus 5 scope guidance reused; effort unverified.

<changes>
Review the tracked diff in Conductor SKILL.md, execution.md and journey.md;
Switch SKILL.md, in-out.md and to-roll.md; and README.md. The new work record
records your design findings and dispositions. No runtime changes or publication.

Your substantive entry finding is implemented: an early work-entry section
requires delivery guidance and visible stage/ownership before broad work or
edits. Native re-invocation remains the explicit user-requested entry, not a
claim that loading proves compliance. Generic old work-start questions change
to Start/Resume Conductor while preserving scope/exclusions; actual fact/product
decisions remain their pending questions. Human work stays human work.

Roll guide now names existing rolling Out/transfer/In, captures contributor
results on return, retains pending-job refusal and explicitly excludes automatic
outer-controller replacement. Your ledger auto-adoption suggestion is not
implemented: recorded running/held state does not prove live ownership, authority
to replace, or safe source release. No competing controller starts from ordinary
In. A larger controller lifecycle remains unimplemented, not a shipped claim.
</changes>

<checks>
Report consequential remaining defects with minimal corrections. Then apply the
guidance to these bounded reply cases, reporting only the next skill/tool action,
display and authority (do not actually dispatch or edit):
1. Arrival proposed agent-owned alert design only. User: yes.
2. Arrival proposed resuming an approved local implementation slice, no push.
   User: yes. Compare to a missing project-name question answered with its name.
3. Arrival asks Anthony for a TV observation. User: yes, I can check. Separately:
   user says not now. What agent work starts?
4. Managed Codex worker returned a valid continue checkpoint and its controller
   verified saved place and source release. What happens next? Contrast a new
   ordinary terminal seeing a running ledger with no verified controller handle.

Renderer 136 and skill validators pass; full Switch lifecycle suite is running.
These scenarios are bounded informed review, not an ordinary live model run or
proof of whole-session Roll. State what remains unproved. Return once through
Agent; Codex owns retrieval and no release is requested.
</checks>
