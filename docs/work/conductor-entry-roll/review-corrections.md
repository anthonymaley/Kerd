# Final local review: corrections only

Established Claude partner; same model/effort/permissions. Read-only, no consumer
work, lifecycle actions, jobs or publication. Scoped local Opus guidance reused.

All four points from your implementation review are addressed:
1. Switch in-out.md checks an existing worktree Roll ledger before fast-forward
   or a competing work proposal. It reads roll_status.py only if the ledger
   exists. Recorded active, uncertain or released ownership stays local/read-only
   and proposes inspection, never adoption or duplicate work. Unknown state is
   unresolved. roll_status.py now says running is recorded, liveness unchecked;
   its display test is updated. No process discovery, kill or binding changes.
2. Conductor frontmatter restores the four natural request examples.
3. to-roll.md names long-running host handles, Claude background Bash, the live
   stdin requirement for control, and unverified child survival after timeout.
4. Recording the shown human result is allowed once the actual report arrives;
   availability alone grants neither a result nor build authority.

Review only these corrections and their interaction with the already-reviewed
entry. Confirm ready for local use or report a consequential remaining defect.
Apply the new normal-In guard to a recorded running Roll in another terminal:
what checkout mutations, skill start and user question are allowed? Don't execute
that scenario. This remains a bounded interpretation check, not behavior proof.

Full Switch tests are rerunning. Automatic outer-controller/TUI rollover remains
explicitly unimplemented; this does not silently satisfy Anthony's whole-session
requirement. No release or install is authorized. Codex retrieves your reply.
