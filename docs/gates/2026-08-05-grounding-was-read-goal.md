---
route: new
stage: done
---

# Goal record — grounding-was-read slice 1 (AU5, the reachability audit), 2026-08-05

Both keys given: cold eyes (opus reviewer, work order + change only,
verdict could block) and the expert-user pass (Tony). Two BLOCKs raised
and resolved by composer-keyed amendment before this record.

## Done condition

Assembled from upstream declarations, every item a conformance check:

- **Met the contract** — 5/5 Pieces landed, 7/7 steps verified
  (`docs/plans/2026-08-05-grounding-was-read-spec.md`; commits d4f5d27 ·
  bad2003 · 005bed9 · bb64bfe).
- **Met the design** — cold-eyes layers 1–3 all PASS: kit.py byte-matches
  the spec's verbatim blocks (em dashes included), the spec carries the
  design's semantics exactly, and the built slice delivers the frame's
  claim with no slice-2 import; the reviewer independently re-ran the
  both-ways demonstration on the live tree.
- **Proof layers pass** — `selftest: 18 cases passed` locally and in CI;
  all seven CI steps green on every ship SHA (bad2003, bb64bfe),
  headSha-verified.
- **Product measurement met as amended** — a broken grounding reference
  at a pushed tip goes red within one CI run, never silent, per declared
  section (both-ways demonstration; fixture T16 asserts the message
  verbatim; the refusal's `problem:` grammar confirmed live).

## The blocks, and their resolution

Cold-eyes layer 4 BLOCKED: the README quantified AU5 over "every
top-level list line" while the code (per the design's own precise words)
checks only `- ` bullets. Layer 5 BLOCKED: the frame still promised the
`kit.GATES` grounding-slot landing site the design had proven fictional,
and its "0 broken references at any pushed tip" unit was unenforceable
(no branch protection; CI runs post-landing) and non-discriminating
(declaring is optional — the count read 0 before the build too).
Composer chose amendment (the push-wiring precedent): README reach
stated exactly, the dead landing site excised, the unit restated as
detection-at-tip per declared section (005bed9, CI green). The
looser-resolution finding (an absolute path or directory ref resolves
clean) became a new accepted ledger row with a review trigger — the
first real grounding line that is not a plain relative file path
re-argues it.

A note for the record: the staleness refuser fired on the amendment
commit itself (the new ledger row moved the derived page) — its fourth
genuine catch, guarding the goal-gate paperwork of its sibling feature.

## The expert-user pass

Tony's verdict, verbatim in kind: trust given on the machine surface —
and the finding that matters: **"I don't know that I will ever manually
add a grounding line."** The feature's *author* is the machine: grounding
sections are written by the model during framing and design (this
build's dogfood section was — the model declared its own reading list
unprompted by any human step). The human meets AU5 only as its
beneficiary: the red check naming a reference that stopped resolving.
Tony read that refusal message and raised no objection — the human half
of the surface passed. Consequence carried forward: slice 2's receipts
are therefore an almost entirely model-facing instrument, which moves
the hollow-stamping ledger row from a caveat to the central design
problem when that slice is ever framed.

## Hands to

LOOP — nothing further for this slice; AU5 now guards every push.
Slice 2 (receipts) stays deliberately unqueued behind its ledger rows.
