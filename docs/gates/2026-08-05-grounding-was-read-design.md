---
route: new
stage: designed
---

# Design GO — grounding-was-read, 2026-08-05

Slice 1 (declarations + the reachability audit) of
`docs/product/grounding-was-read.md`.

## The package

- `docs/design/grounding-was-read.md` — the doc half: the `## Grounding`
  section shape, the AU5 audit rule with both refusal messages verbatim,
  the granularity decision (marks — per-item section wins, static table
  dies on both M-criteria, hybrid deferred to slice 2), why optional
  beats required, testing strategy, the stage-1 measurement answered by
  name.
- `docs/design/grounding-was-read.excalidraw` (+ `.svg`) — the drawn
  half, seven movements, generated by
  `tools/diagram/gen_grounding_was_read.py`, driven onto the live canvas
  and reviewed there.

## The two keys

- **Human (Tony):** GO given on the drawn package, nothing left to
  annotate — a zero-correction round.
- **Machine:** the stage-1 measurement carries a named answer in the
  package (`docs/design/grounding-was-read.md`, "Named answers"): broken
  grounding references at a pushed tip → 0 via AU5 inside `gate.py
  audit` — CI step two, no new CI step; refusal names the doc and the
  reference verbatim.

## Found at design time

A8's sketched landing site does not exist: `check_rung` is inline code
per rung — there is no `kit.GATES` data table to hold a `grounding`
slot. The granularity decision therefore chose the per-item
`## Grounding` section over inventing a static structure whose contents
would only duplicate the gate's existing existence checks.

## Hands to

CONTRACT — the slice's spec, sized and assigned, measurable against this
package.
