---
route: new
stage: designed
---

# Design GO — vault-unhook, 2026-08-06

Slice 1 (the vault becomes opt-in everywhere) of
`docs/product/vault-unhook.md`.

## The package

- `docs/design/vault-unhook.md` — the doc half: the coverage table
  (every vault artifact's fate — the killer risk's countermeasure,
  with `people/` named as the genuine vault-only residue), the
  four-file edit map (switch bears the bulk; kivna gets one sentence
  by design; tend's nag softens; vault-spec gains the opt-in
  sentence), the delete-and-renumber decision as marks, three
  diff-scoped stage-1 measurements with the prompt-layer limit named
  honestly, and the mvp rigor disposition (measured = three diff
  checks + release sweep; waived-by-name = live-boundary observation,
  recorded at the first real switch-out after ship).
- `docs/design/vault-unhook.excalidraw` (+ `.svg`) — the drawn half,
  seven movements, generated by `tools/diagram/gen_vault_unhook.py`,
  driven onto the live canvas and reviewed there.

## The two keys

- **Human (Tony):** GO given on the drawn package ("works for me"),
  nothing left to annotate — a zero-correction round (53/53 elements
  generator-marked, zero foreign at read-back).
- **Machine:** all three stage-1 measurements carry named answers:
  writes-per-boundary 1 → 0 by the step's deletion (grep lands at
  exactly two deliberate-path pointers); on-demand unchanged (kivna
  diff = one added sentence, zero removals); killer feature
  byte-for-byte (zero diff hunks inside `## Switch In` — any hunk
  there is a build refusal).

## Found at design time

The frame's grounding was incomplete against its own amended slice:
the tend softening joined at the slice amendment but tend's SKILL.md
was never declared or read — healed in the same sitting (grounding
line added, Category 3 read; a second nag surfaced in Category 2's
required-files example, now in the edit map). The release sweep also
refused this design doc's first draft on a bare `/kivna` reference —
R3's no-waiver rule catching its own author.

## Hands to

CONTRACT — the slice's spec, sized and assigned, measurable against
this package.
