---
route: new
stage: designed
---

# Design GO — risk-state-split, 2026-09-03

**Clock:** 2026-09-03 16:12 EDT

Design approved — Tony's key, 2026-09-03 afternoon sitting, granted
conditionally and discharged the same sitting. His key, verbatim: *"With
those two claim corrections, the lifecycle and all three views carry my
key."* The two conditions, both landed before sealing: the planned form
got one exact grammar — `planned — <what will exist> · <expected
repo-relative location>` — used identically in the doc, all three views,
and both refusal strings, with `gate-reachability`'s migrated value naming
its expected fixture location (`tools/gates/kit.py`); and the matrix
legend's "truthfully treated" overclaim became "treatment declared before
acceptance, verified at it".

Package: `docs/design/risk-state-split.md` with three views sealed on the
key — `two-axis-vocabulary` (matrix, fp:065cb1c38dd7) · `migration-map`
(flowchart, fp:aef214c7ae05) · `hollow-treatment` (flowchart,
fp:c2a0be490625).

The design took two producer refusals before the key, and both are
recorded in the doc's What we ruled out because they are the record's
substance:

- **Revision 1 required resolving `Treatment evidence` at the viability
  parse — a circular dependency.** It would have blocked `gate-reachability`
  permanently: unable to advance to the loop that builds the very fixtures
  the gate demanded. His replacement shape: treatment assurance is a
  lifecycle — a fatal risk advances from viability on a *planned*
  (declared, never called proven) treatment; *acceptance* is the gate that
  demands the resolving citation (*verified*). The machine distinguishes
  the two by form.
- **Revision 1 blanket-refused fatal + temporary countermeasure.** His
  correction: temporary means *carries a return condition*, not *expires
  before protecting this increment* — the combination passes with its
  return condition named (Review trigger, machine-checked).

Consequence stated at the gate so nobody rediscovers it: under this design
`gate-reachability` **unblocks at the migration commit** — planned evidence
passes viability, it proceeds to its stated scope, and verified evidence
comes due at its acceptance. No other rung position is claimed stable until
the 79 severity reviews are keyed.

Hands to HANDOFF: the work specification for the checker change, the
21-record migration under the review worksheet, and the producer's five
fixtures plus the added sixth (fatal + temporary without a return
condition refused — the model's addition, marked in the design).
