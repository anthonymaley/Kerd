---
route: new
stage: designed
---

# Design GO — push-wiring, 2026-08-04

The ladder's first design gate record. Slice 1 (the staleness refuser) of
`docs/product/push-wiring.md`.

## The package

- `docs/design/push-wiring.md` — the doc half: stale-check semantics,
  the ship flow under the gate, CI wiring, determinism evidence, testing
  strategy, both stage-1 measurements answered by name.
- `docs/design/push-wiring.excalidraw` (+ `.svg`) — the drawn half,
  seven movements, generated by `tools/diagram/gen_push_wiring.py`,
  driven onto the live canvas and reviewed there.

## The two keys

- **Human (Tony):** GO given on the drawn package, nothing left to
  annotate. One correction in the round, acted on before GO: the package
  reads in Nunito, not the kit's hand-drawn default — applied in the gen
  script so the committed artifact is the reviewed artifact.
- **Machine:** both stage-1 measurements carry a named answer in the
  package (`docs/design/push-wiring.md`, "Named answers"): staleness at
  a push tip → 0 via the seventh CI step's byte-compare refusal;
  remember-steps per ship → 0 via enforcement-outside-the-model with the
  fix carried verbatim in the refusal message.

## Found at design time

`actions/checkout@v4` defaults to a shallow clone; a fresh render in CI
would derive from one commit of history and refuse every push. The fix
(`fetch-depth: 0`) ships with the slice. Recorded in the package, step 4.

## Hands to

CONTRACT — the slice's spec, sized and assigned, measurable against this
package.
