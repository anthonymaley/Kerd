# TODO

## Now

**Requirements is the live subject**, and the producer settled its shape on the
night of 2026-08-07: **requirements → features → releases**. The middle layer
partly exists — **13** product docs, all gate-enforced and populated. (A first
pass said 20; that was the board's slug universe, 22, of which 9 exist only as
contract specs with no product doc.) The repo has no concept called a
"feature": the frame's own chain table calls `docs/product/<slug>.md` the
**Product Requirement** link and grades it *"prose, no identity"*. Both other
ends are empty.

**GOAL 2026-08-07 23:44 — _perform a requirements management and traceability
evaluation_ — is DONE (2026-08-08 00:20).** Verdict: **BUILD**; adopt none of
the five. Recorded in CONTEXT.md; the matrix is
`docs/design/requirements-traceability.md`, the repo's **first** — `tools/design/`
has been CI-enforced since v0.77.0 and had never been used.

- 6 options × 19 criteria, marks-only, `matrix.py check` clean; rendered to
  `docs/design/requirements-traceability-matrix.svg`.
- Six architecture overviews generated by `tools/diagram/gen_reqtools.py` →
  `docs/design/reqtools-{build,strictdoc,doorstop,sphinxneeds,openfasttrace,reqflow}.svg`,
  all layouts clean. Every one is required: the matrix refuses an option row
  whose overview file does not exist.
- **Open for the producer, and only him:** *install burden* is the criterion
  doing most of the killing. Declared **M** at 23:52 before any evidence landed.
  Relax it to **D** and StrictDoc becomes a living option — verified write-back,
  ReqIF and Excel export, a validator that refuses. It would still lose *the
  same files* and still have no release board.

**BUILT 2026-08-08 13:20 — `docs/requirements/` now exists.** Three files, one
declared location:

- `catalog.md` — the schema: fields, twenty codes, five states **and what each
  owes**, five link roles with their reverses, four named views. Carries the
  named return condition for reoccupying `capturerequirements`' cut path: these
  are living files with no date in any filename, so the defect that caused the
  cut is not reproduced.
- `categories.md` — the disposition, all twenty rows. **Drafted by the model,
  NOT yet keyed** — the design says this file is the producer's. Eleven
  categories are `applies` and unfilled, which is now visible instead of
  indistinguishable from the four that are correctly `n/a`.
- `register.md` — **48 requirements as blocks**, seeded from the frame's
  gap-2 evidence table, grouped by category, with an empty `## Archive`
  section at the foot.

**Proven, not asserted:** a stdlib-only parser reads all 48 with no third-party
import, and the `Approved` hash refuses a tampered statement. The first run of
that parser reported six false divergences — every one the last block in its
category, absorbing the next `## ` heading into its statement. The hash check
caught a parser bug it was not written to find.

**Next, in order:** rework the blocked design package (below) against the
BUILD verdict · land `gate.py --root` (hard dependency, and its script-location
half is undesigned) · the release grouping artifact · then the board.

**Then: rework the blocked design package** —
`docs/design/requirements-traceability.md`. Task framing agreed 23:14; now
downstream of the evaluation's verdict.

**Hook research is DONE (2026-08-07 23:48) and its findings must survive into
that rework:**

- **`PreToolUse` + `permissionDecision: "ask"` is the only mechanism** that
  warns *and* requires approval (verified against the live hooks reference,
  shasum-matched by an independent refuter). Kerd's four hooks sit on
  `UserPromptSubmit`, `Stop`, `SessionStart`, `PostToolUse` — **none can prompt
  a human.** Not unconditional either: it degrades to silent deny under
  `dontAsk`, has no prompt under non-interactive `-p`, and the docs do not
  state it survives `bypassPermissions`.
- **Killer false negative, measured:** on 2026-08-07 the session produced 25
  requirements while `## Key Decisions` grew by **7** — the other 18 landed in a
  product-doc table. A decisions-list detector would have caught two-fifths of
  the session it was designed for, so gap 1's premise is only partly true.
- **The obvious implementation is refuted:** `git diff HEAD -- CONTEXT.md` is
  blind at the boundary turn, because switch-out writes *and* commits inside one
  turn. Name it rejected so it is not re-proposed.
- **Doctrine constraint:** *"advisory output is not a check"* — "warn" is the
  disqualified half by name, and all four shipped hooks are `echo … exit 0`,
  so there is **zero blocking precedent** in this repo.
- **The honest ceiling:** a hook cannot observe that a durable requirement was
  *stated* — that is semantic judgment over the producer's words. It can only
  observe that the register did not move while the written record did, and hand
  the judgment back via `additionalContext`.
- Best candidate if built: set-diff of Key Decisions entry identities against a
  **SessionStart-captured baseline** (matcher-scoped to `startup`/`resume`, or
  compaction silently resets it), evaluated at `Stop`, gated to the boundary
  turn and to opt-in.

- Adopt the producer's **universal requirement row** verbatim
  (`docs/product/requirements-project-type-templates.md:162`) — the design
  invented a five-column row and is missing ten of its fifteen fields,
  `Trace Links` among them. His file was written two hours *after* the design
  was blocked; no review lens re-read it.
- The promotion beat is **two beats**: capture on the fly as `proposed`; ruled
  to `final` or `dropped` on the board. Not at the boundary.
- Record the **requirement → feature** link; the feature layer is already on
  disk.
- Fix the five mechanical blocks: false premise (FUN-001 did not delete the
  plan gate), the aiming fix's undesigned script half, the ID regex rejecting
  `UX-001`, the edit map missing `tools/gates/README.md`, the killer risk's
  absent countermeasure.
- Themes 7 and 8 are **dead, not fixed** — G1 and the project-type floors
  settle disposition; `Trace Links` subsumes dependency.

**Out of this slice, named:** the release grouping artifact, the board itself,
and browser write-back. The producer loses the board tonight and gains the data
model it stands on.

**Next, in order:** the release grouping artifact (5 days without one), then the
board — `shared-memory` gap 8, whose "source does not exist" blocker dies with
this slice. Write-back is its one genuinely new mechanism.

**Also queued:** `requirements-project-type-templates` needs a `## Release
slice`; the three-declaration merge (`route` + `Rigor level` → derived from
`Project Type`) is drawn and annotated but unframed.

**Standing conduct (2026-08-07):** Tony's rungs are frame, viability, slice and
design, plus evaluation at goal; contract and build are the model's. Do not
narrate build mechanics. The plan-approval gate is deleted — his key lands on
the design.

**Working default:** per-piece evidence check, no blanket license.

## Backlog

*Ranked by consequence x value. See `docs/plans/2026-08-03-choose-what-matters-view.excalidraw`.*

**High consequence**

- **`gate.py --root` — promoted from Medium.** No longer a nice-to-have: it is
  a hard dependency of requirements-traceability slice 1. `tools/gates/kit.py:24`
  derives `ROOT` from the tool's own path, so a consuming project would audit
  the plugin cache. The pattern already exists in all four hooks
  (`${CLAUDE_PLUGIN_ROOT}` finds the script, `$CLAUDE_PROJECT_DIR` finds the
  state) and the gate library already takes `root` as a parameter everywhere —
  only the CLI pins it.
- **Hookify — promoted to a dependency.** `OPS-001` (funnel interaction requires
  a conductor session, everything else stays open) is a **refusal**, and skill
  text cannot enforce it on itself. A hook fires outside the model, which is the
  only layer that can. `hookify` is installed and unused. Its
  `conversation-analyzer` also reads a transcript for behaviours worth
  preventing — the superpowers pull is one, since the v0.77.0 "tie is cut"
  countermeasure aims one layer below the cause.
- **Requirement archaeology over CONTEXT.md's 74 standing decisions.** Framed
  work, never ad-hoc, and **never from session logs** (see the decision in
  CONTEXT.md). Provenance must be marked permanently or the register becomes
  untrustworthy with no way back.
- **The release-planning artifact.** Gained two inputs today: project type ==
  release type for the twelve that ship, and `TECH-006` requirement dependency —
  which is the missing artifact behind the 2026-08-03 decision's first deciding
  factor, *"dependency forbids (hard constraint)"*.
- **Frame switch-fidelity slice 2 — capture human input.** Gaps 10/11/12. Note
  the overlap: requirements-traceability is arguably this slice's mechanism.
- **The fidelity check** (accepted unknown; review trigger already fired).
  Nothing verifies a pickup restored what the close recorded. It proves *file*
  reachability, never *finding* reachability.
- **boundary-cycle, in-half** — the reset ritual's automation. Killer
  feasibility question first, verified against harness docs at frame.
- **Plugin cache repin debt.** Reopened by v0.95.0: the cache was current at
  0.94.0 this afternoon and the repo has since shipped. Structural — the only
  session running current cache text is one where nothing shipped.
- **Stashes and local-equals-remote are unchecked at the boundary.**
- **The playbook's `## Current Status` duplicates CONTEXT.md.** Its stale
  content was fixed this session (v0.90.0 → v0.95.0, three hooks → four); the
  duplication itself remains. Kill it or make it a pointer.
- **Out-of-repo artifacts have no home** — PRs, URLs, decks, external docs.
- **Stop-hook over-prescription**: distinguish work-dirty from
  session-state-dirty at a real stopping point.

**Medium**

- **The refusal surface does not travel with the plugin — the return condition
  FIRED 2026-08-07.** It was accepted "for now" with the trigger *"the first
  time Kerd's ladder is run in a repo that isn't Kerd"*; declaring requirements
  traceability a capability for consuming projects is that moment. Now
  interlocked with the `--root` row above.
- **`docs/vault-spec.md` contradicts itself** (found by tend this session): line
  39 says Weekly is "the one append-style file in the vault", line 88 describes
  the decisions file as accumulating entries. `Kerd Architecture Decisions.md`
  (6 dated sections) and `Kerd Skill Lessons.md` (5) sit in the gap. Not drift —
  a genuine unresolved rule.
- **`Kerd.md` MOC has one broken wikilink** — `[[eloas/Eolas]]`, a typo for
  `eolas`. 16 of 17 resolve. Vault write, so kivna's.
- **Revisit the journey view when more data exists** (parked 2026-08-05, shape
  agreed on mock v4).
- Clean krutho-strategy's stray `sessions-of-record/`.
- AGENTS.md needs its own verdict: gitignored, machine-local, stale Codex-era fork.
- Regenerate the choose-what-matters view before its next use.
- Hook version staleness check in `/kerd:tend`.
- PR-event edge in the stale CI step (unexercised; no PR flow).
- Guard switch-in step 3 smoke test against context bloat.
- **lorg-cut candidate** — evidence check per the rip discipline before any
  license. **Interrogate rides the same review** — and note it now has a second
  caller: requirement qualification is the same shape as risk qualification.
- **kivna verdict** — same zero-usage smell as the vault; import/export
  confirmed unused.
- **CI rule for the single-definition law** — nothing machine-enforces
  "conductor never re-describes a Switch Out step".
- **Close-out double-write** — conductor step 1 writes CONTEXT/TODO, step 6's
  invoked flow overwrites both.
- Derive the rigor refusal messages from `RIGOR_LEVELS` via join.
- Gate records can only say GO: a refused gate has no dated home.

**Low — genuinely ignorable, and you can see what ignoring costs**

- `CHANGELOG.md` stale at 0.14.0 while the repo is at 0.95.0, and absent from
  the release checklist. Revive or delete.
- Stale `Kerd.md` MOC version field (says 0.31.0).
- Consider promoting the refined question-formation rule from the pair hook into
  global `~/.claude/CLAUDE.md`.

**Blocked — not candidates at any consequence**

- skriv voice profile wiring — needs non-founder-genre samples.
