# Session 2026-09-01 (requirements-success-measurement: design correction + work spec, 10:20–12:17 EDT)

**Machine:** AnthonyacStudio.attlocal.net (the Mac Studio; thin-client host, tmux over SSH from the MacBook)
**Branch:** main
**Tracking:** origin/main (up to date)

> **The heading's open side is the `execute` stamp and under-reports this sitting
> by about two and a half hours.** Switch-in ran at **07:57 EDT**; conductor
> oriented 08:04 and planned 08:15, and `execute` only stamped **10:20** because
> the whole first stretch was orient and plan — reading the design, then two
> composer dispatches. **Eleventh recorded instance of the conductor marker
> holding one line and therefore only ever reporting the LAST phase, while the
> open time is a property of the FIRST.** Written honestly rather than smoothed;
> the row is in TODO's Backlog with the running tally. The measured spread there
> is now 14 min · 26 min · 66 min · 157 min · ~143 min.

## What Was Done

**Per-task actuals:**

```
design correction: sweep, correct and reseal three views  — started 10:20 (marker) · landed 10:55 (78411bf)
work spec: compose, cold-review, amend, copy-edit         — started 10:55 · landed 12:07 (654f6fe)
```

**`requirements-success-measurement` reached `loop`.** The work specification is
`docs/plans/2026-09-01-requirements-success-measurement-spec.md` — 19 steps, 19
Pieces, a `**Verify:**` command with expected output on every step. `gate.py
route` reports `enters at: loop`; `audit: clean (1 finding)`, that one the
pre-existing trace gap. **Nothing was built:** `tools/` untouched, `catalog.md`
unedited, `LEGAL_STATES` still five, `rigor-level` slice 2 not absorbed, Kerd
still **v0.104.0**. The producer set that boundary at the top of the session —
*"Keep this session to composing and reviewing the spec — no implementation"* —
and it held.

**The session's real finding: the sealed design's `KEYED` freeze was hollow.**
Converting the design into a contract exposed that the register's `Approved`
recipe hashes the statement alone, so an `MSC`'s Measure, Baseline and Target sat
outside the hash — editable after the producer's key with nothing diverging.
Verified in three places before it was reported: `kit.py:1191`,
`catalog.md:178`, and `fingerprint.py`'s docstring. The producer ruled one
versioned `approval_fingerprint(category, fields)`; full rationale in CONTEXT.md.

**All three sealed views were corrected and re-keyed, not just the one.** His
instruction to sweep the other two before resealing is what turned a fix into a
sweep, and it found the assumption in every view — twice in `assurance-boundary`,
where it had *classified* a row as machine-checked, and in `condition-anatomy` as
a claim that was accurate when sealed and superseded by the new ruling. Executed
per the 2026-08-25 precedent: seals downgraded together, route regression to
`scope` accepted as honest, prose and views corrected as one deliverable, one
combined producer eye with three explicit approvals, resealed from final content.
`condition-anatomy` `fp:fefa90380fe3 → fp:67ff11391faf` · `condition-lifecycle`
`fp:0a91dbcac981 → fp:22651ad0d921` · `assurance-boundary` `fp:c9b8d06ebfb6 →
fp:544e73328c06`. **The dated GO record was left untouched** and still carries
the pre-ruling seals, which is correct — it records what was believed on
2026-08-31.

**The artifacts, named by path so a pickup can reach them.** The design package
is `docs/design/requirements-success-measurement.md`; the work record carrying
the concerns block and the three reseals is
`docs/product/requirements-success-measurement.md`. The three sealed views — the
fingerprint covers the `.html` only — are
`docs/design/requirements-success-measurement/condition-anatomy.html`,
`docs/design/requirements-success-measurement/condition-lifecycle.html` and
`docs/design/requirements-success-measurement/assurance-boundary.html`. Each was
re-rendered for the producer's combined eye to
`docs/design/requirements-success-measurement/condition-anatomy.png`,
`docs/design/requirements-success-measurement/condition-lifecycle.png` and
`docs/design/requirements-success-measurement/assurance-boundary.png`; **a render
is never the view**. The work specification is
`docs/plans/2026-09-01-requirements-success-measurement-spec.md`, and the design
GO record left deliberately untouched is
`docs/gates/2026-08-31-requirements-success-measurement-design.md`.

**The assurance tally was recounted by tense on the producer's ruling.** Thirteen
questions, not fourteen lines; the design-gate row is dual-marked because its
answer differs by tense. Today five machine-checked / two producer-agreed / six
unenforced (eight of thirteen on agreement or nothing); once the MSC fingerprint
is built, six / two / five (seven of thirteen). Applied in the visible footer,
the `<desc>` and the design prose.

**Three composer dispatches, each bounded by the producer.** Pass 1 composed the
score. Pass 2 was a **fresh** agent cold-reviewing it — it landed twelve
corrections, the most serious being that Step 17 had invented a way for the pilot
to close on its failure path (writing an acceptance record carrying `Outcome: NOT
ASSESSABLE`, which `kit.py:892` would have read as the terminal). Pass 3 was a
bounded amendment absorbing the fingerprint ruling into steps, verifies,
fixtures, Piece boundaries and later dependencies — 18 steps became 19.

**The producer chose the pilot.** Phases 2–4 prove the capability end-to-end
against `stage-route-consistency` — the filed stage-versus-derived-route
consistency guard — because it is real, narrow, currently unframed (so its
measurement can be declared before design) and naturally measurable: fixtures
must prove a legal-but-overclaimed and a legal-but-underclaimed `stage:` are both
refused. **This item's acceptance therefore stays open across multiple sessions**,
his ruling: the declared value is end-to-end proof, so closing earlier would
weaken the scope after the fact.

**TODO closure review** (this boundary):

```
  ✓ done   — "the work specification for requirements-success-measurement"  (654f6fe; route reports enters at: loop)
  ✓ done   — "MSC needs a categories.md disposition" -> scheduled as spec Steps 1 and 5, producer-gated
  ✓ done   — "where the Observed result lives"       -> scheduled as spec Step 2, producer gate
  ✓ done   — "the comparison is unenforced"          -> scheduled as spec Step 4, producer gate
  ✓ done   — "the catalog supersession is recorded, not executed" -> scheduled as spec Step 6
  · open   — "reciprocal link stamping owed"         (still owed; now spec Step 3's gate, explicitly NOT folded in)
  · open   — "funnel-driver acceptance gate"         (untouched; still the largest open piece)
  · open   — "gate-visuals slices 2 and 3"           (untouched)
  · open   — standards spike x4 · archaeology · retire register
  → moved  — "the suspect-link stamp has no slot in the format" -> Backlog, narrowed by today's ruling
  + new    — the composer emits hard-coded absolute paths and cold review misses them
  + new    — grep -c in a fail-fast verify chain fails exactly when it should pass
  ! note   — the Backlog's stage/route row IS the pilot now, and is still unframed
```

## What's Next

- **Spec Steps 1–4 are all producer gates and lead deliberately** — the category
  code and its `categories.md` disposition · where the `Observed result` lives ·
  reciprocal stamping (build or keep owed) · what mechanism compares the reading
  against the frozen target. Nothing downstream should start before these.
- **Then Step 7**, the approval-fingerprint build the producer required when he
  ruled it. Its preservation constraint was already verified against live data:
  47/47 shipped `Approved` values reproduce, 51/51 statements match
  `req_statement_hash`, 15/15 live link stamps still verify.
- **The dependency that is still invisible to every machine surface:**
  `stage-route-consistency` has no product doc, no slug on the board, and no gate
  demanding anything of it. Spec Step 12 frames it. Until then, the thing this
  item's acceptance depends on is exactly as unnoticeable as `funnel-driver` was
  for the three days that became this repo's canonical example.
- **Position:** `requirements-success-measurement` at **loop** (19 pieces
  remaining, acceptance deliberately open) · `gate-visuals` at
  **ready-to-release** · `funnel-driver` at **acceptance** · Kerd at **v0.104.0**.

## Key Decisions

Full rationale in CONTEXT.md. **The approval fingerprint is ONE versioned
mechanism with artifact-specific canonical payloads** — existing requirements keep
the legacy statement-only payload byte-for-byte, `MSC` v1 hashes exactly
Statement · Measure · Baseline · Target, the byte stream is fixed centrally so an
alternate implementation cannot invent a different one, and **link stamps
targeting an `MSC` use the same full fingerprint**, never `req_statement_hash` ·
**reciprocal stamping stays separately owed**, because a richer target
fingerprint does not make protection bidirectional · **a ruling that changes a
mechanism invalidates every view describing it**, not only the view that was
wrong · **an assurance tally is counted by tense**, because counting a
dual-marked row as two lines makes proposed assurance read as present assurance ·
**the adjacent-wording rule binds on trees already keyed, not on the one being
keyed** — which is why the count correction was in scope rather than creep.

Nothing was pruned from CONTEXT.md: no acceptance record landed and no drop was
agreed, so this sitting was structurally not a licensed prune event.

## Commits

Three, all pushed to origin/main, CI green on both pushes (`33522539258`,
`33529922171`): `78411bf` (the design correction and three reseals) · `654f6fe`
(the work specification) · `4c86a5f` (render refresh) · this boundary.

## Gotchas

- **A verification that inspects nothing reports success.** Three probes in this
  session printed `0/0` and read as clean passes; each was a lookup miss, not a
  result — `block["meta"]` where the parser returns `block["fields"]`, a stamp
  filter testing `startswith("sha256:")` when the parser stores bare hex, and a
  `grep -o` regex that exceeded ugrep's complexity limit so the `|| echo
  "(clean)"` fallback fired. **A denominator of zero is never evidence.**
  Mirrored to `docs/playbook.md`.
- **Sequential substitutions collide when a shift moves a value onto another's
  key.** Shifting SVG y-coordinates, `634 → 700` ran before `700 → 766`, so the
  line just moved to 700 moved again and landed on top of the line that belonged
  there. Invisible in markup, obvious in the PNG. The repo already had this rule
  from the 2026-08-25 rename and it was still broken. Mirrored.
- **Downgrading a seal demotes the item, and the board must not record it.**
  `kit.py:815-823` fails the design rung for any view row that is not `ok`/`na`,
  so stripping `· fp:` drops the item to `scope`. Refresh the render only after
  the gate is restored. Mirrored.
- **A ruling that changes a mechanism invalidates every view describing it.**
  Mirrored.

## Insights

- **Both composer defects that recurred this session were already fixed once, on
  2026-08-28, in the artifact rather than in the process.** Hard-coded absolute
  repository paths and `grep -c` exiting 1 on zero matches were two of three
  sibling score defects handed back that day. Both came back. Nothing in the
  composer dispatch forbids absolute paths and nothing in the cold-review brief
  names them as a hunt target, so the correction lived only in one document. **A
  finding fixed in the artifact and not in the generator is a finding that will
  arrive again**, and this is the measured proof.
- **The cold-review pass earned its cost on exactly the failure it was dispatched
  to catch, and missed a whole class beside it.** It caught pass 1 asserting that
  `check_stage_schema` does not exist (it does, at `gen_journey.py:385`) and
  caught the acceptance-record failure path — both judgment findings. It read the
  entire spec and did not notice 24 hard-coded paths. **A reviewer hunting
  meaning does not see form**, which argues the two hunts are separate briefs
  rather than one thorough reviewer.
- **The producer's entry conditions did the work again, twice.** Requiring
  `concerns:` before the design gate made yesterday's PASS mean something;
  requiring the other two views be swept before resealing turned a one-view fix
  into a three-view finding. Both were cheap sentences said before work started,
  and both changed what the work found rather than how it was checked.
- **The seals on this item have now been wrong twice, in eight days, about
  mechanisms rather than about layout.** Every blocking finding across both
  sittings was a *claim*; the visual system was never the problem. That is the
  third consecutive item where drawings land and the sentences about the drawings
  are where the defects live — worth treating as the standing expectation rather
  than a surprise, and it argues for verifying a view's claims against the code
  as a named beat before sealing, not after.

---

# Session 2026-09-01 (2nd sitting — the producer-gate pass, 13:05–15:46 EDT)

**Machine:** AnthonyacStudio.attlocal.net (the Mac Studio; thin-client host, tmux over SSH from the MacBook)
**Branch:** main
**Tracking:** origin/main (up to date)

> **The heading's open side is the `execute` stamp and under-reports this sitting
> by about half an hour.** Switch-in ran ~12:34 EDT; conductor oriented 12:38 and
> planned 12:56, and `execute` only stamped **13:05**. **Twelfth recorded instance
> of the conductor marker holding one line and therefore only ever reporting the
> LAST phase, while the open time is a property of the FIRST.** The row is in
> TODO's Backlog with the running tally; the measured spread there is now
> 14 min · 26 min · 27 min · 66 min · 157 min · ~143 min.

## What Was Done

**Per-task actuals:**

```
Step 1 — the category code            — started 13:05 (marker) · landed 13:22 (93b1215)
Step 2 — the Observed result's home   — started 13:22 · landed 13:27 (5101645)
Step 3 — reciprocal stamping          — started 13:27 · landed 13:46 (7ed7fd1)
Step 4 — the comparison               — started 13:46 · landed 13:57 (838a5fa)
```

**All four of `requirements-success-measurement`'s producer gates were keyed in
one sitting**, recorded per D1 in a new `## Rulings after the GO` section at the
end of `docs/design/requirements-success-measurement.md`, each with its
does-not-settle bullet suffixed, its Pieces box ticked, and its own commit. The
item still routes at **loop** with **15 unchecked Pieces**. **Nothing was built:**
`tools/` untouched, `catalog.md` and `categories.md` unedited, `LEGAL_STATES`
still five, Kerd still **v0.104.0**. The producer set that boundary when he
scoped the session and it held.

**The four rulings.**

1. **The category code is `MSC`**, reader-facing name Measurable Success
   Condition, disposition `applies`. The vocabulary review the design deferred
   was run as the gate: all twenty deciding questions in `catalog.md` were
   applied to an `MSC` statement, and none returns yes. The decisive argument
   turned out to be coherence rather than semantics — D2's obligation
   (*Measure, Baseline or Target on any non-`MSC` block is a refusal*) is written
   **in terms of the category**, so folding conditions into `TST` makes that
   refusal unwritable without a sub-type discriminator inside `TST`.
2. **The `Observed result` lives in the immutable acceptance record**, not the
   register; **no `OBS` code is added**. Ruled on the repo's own 2026-08-03 law
   rather than on the two costs the design listed: a reading is an event, and
   rewriting it tomorrow would falsify the record. Seven fields are required on
   the entry so the comparison can be bound. The producer refused the cheap path
   explicitly — `evidenced-by` is not to be forced through today's ID-only link
   grammar, and the suspect-link stamp is not to be described as supporting a
   file reference.
3. **Reciprocal stamping is BUILT NOW**, scoped narrowly to Requirement <->
   `MSC`. Both edges stored, each with its own payload; the audit requires both
   directions; severity stays a **finding**, explicitly not inflated into a
   refusal. Existing role pairs are not retrofitted.
4. **The comparison stays producer-performed.** The parser enforces structure,
   never semantics; the machine may not recompute `PROVEN` vs `NOT MET` until
   `Target` has a typed comparison grammar. A return condition is recorded.

**Then the reassessment sent the item BACK TO DESIGN, not forward to the
composer.** Five of the six remaining Phase 1 steps are blocked, and Step 8 is
**enlarged** — ruling 4's structural acceptance-record parser has no home step in
the spec at all. Step 7 (`approval_fingerprint`) was verified as the only step
surviving all four rulings intact, and is deliberately **not** built.

## What's Next

- **The design amendment pass, in the producer's order.** Freeze the spec with
  the four rulings recorded (**done** — pushed at `0f46ba4`) · amend the design
  package to settle seven things · sweep all three views and the prose,
  especially `assurance-boundary` and its counts · re-render, producer approval
  on every changed view, reseal from final content, dated GO record untouched ·
  **only then** re-dispatch the composer, explicitly encoding cross-step
  dependencies.
- **Open the amendment with the seal downgrade and reserve the runway.** Per the
  2026-08-25/2026-09-01 precedent the downgrade is the first act of a
  **continuous** deliverable — downgrade -> amend -> render -> producer eye ->
  reseal -> board refresh. Starting it and pausing would strand a demoted item
  across a session boundary and force the committed render to either memorialize
  the demotion or misstate it. The clean restart point is the pushed tip
  `0f46ba4`.
- **Do not build Step 7 first**, although it is technically clear. The producer's
  reason: building it before the governing design is restored would invert the
  ladder.
- **Position:** `requirements-success-measurement` at **loop** (15 of 19 Pieces
  remaining, 4 landed; acceptance still deliberately open) · `gate-visuals` at
  **ready-to-release** · `funnel-driver` at **acceptance** · the pilot
  `stage-route-consistency` still at **frame**, unframed, 4 inputs missing for
  viability · Kerd at **v0.104.0**.

## Key Decisions

Full rationale in CONTEXT.md. **The design package is amended before the spec is
revised — a work specification must never be the first authoritative statement of
a design.** The producer's reason: the four rulings changed architecture and
assurance rather than execution order, so letting the composer encode them
directly into Steps 5–10 would put the decision in the wrong artifact. **The
transferable rule: D1-style hand-back is right for a wrong PASSAGE and wrong for
a ruling that MOVES the architecture** — the first is a contract defect, the
second is a design change wearing a contract's clothes.

Nothing was pruned from CONTEXT.md: no acceptance record landed and no drop was
agreed, so this sitting was structurally not a licensed prune event.

## Commits

Five, all pushed to origin/main: `93b1215` (Step 1, the category code) ·
`5101645` (Step 2, the Observed result's home) · `7ed7fd1` (Step 3, reciprocal
stamping) · `838a5fa` (Step 4, the comparison) · `0f46ba4` (render refresh, no
trailer, per the ship flow) · this boundary.

## Gotchas

- **Ticking Pieces boxes moves the derived model, so per-task pushing goes red.**
  Conductor says push each verified task immediately; CI's `progress.py stale`
  byte-compares the committed render at every pushed tip. Four box-ticks with no
  render refresh would have refused. The documented ship flow already resolves
  it — work commits, then refresh, then the render commit, then **one** push —
  but the two rules read as contradictory until you notice the push is what the
  render gates, not the commit. Mirrored to `docs/playbook.md`.
- **A spec's own override clause can be narrower than it looks.** Step 5 carries
  *"where the ruling's recorded wording differs from **item 1's** template, the
  ruling's words win"* — so ruling 1 corrected the disposition row and left
  items 2 and 3 standing in contradiction to it. A step that anticipates being
  overridden is not thereby safe from the ruling. Mirrored.

## Insights

- **The coherence test beat the semantic test, and was cheaper to run.** Step 1
  was framed as *does any existing code already mean this?* — a judgment about
  meaning across twenty definitions. The argument that actually settled it was
  *can the refusal be written in terms of the category?*, which has one answer.
  Where a category system has per-category checks, the checks themselves become
  the taxonomy's test. That generalises past this gate.
- **Two spec sentences went stale inside the session executing them, by the same
  mechanism.** Step 5's framing died under ruling 1; Step 4's feasibility premise
  died under ruling 2 — and the second was already wrong on its own terms, since
  `kit.py:892` globs and parses acceptance records, so the machine option was
  relocated rather than eliminated. Both wrote a *consequence of an open question*
  as settled fact. A reader following either literally would have shipped the
  producer's ruling's opposite. Filed as a Backlog row.
- **Two of the four gates took the more expensive option, and both were argued
  from the pilot's purpose.** The spec was written so every gate could be
  answered without revision; the honest answers revised it anyway. Worth stating
  because it is the gates working rather than failing — a gate that can only be
  answered in the direction the spec anticipated is not a gate.
- **The session's largest finding was a question, not an edit.** Asking where the
  hand-back belonged — composer or design — is what surfaced that D1's mechanism
  had never distinguished a wrong passage from a moved architecture. That
  distinction is now a standing decision, and it came from a gate question rather
  than from any artifact being examined.

---

# Session 2026-09-01 (3rd sitting — the design amendment, 19:27–20:09 EDT)

**Machine:** AnthonyacStudio (the Mac Studio; thin-client host, tmux over SSH from the MacBook)
**Branch:** main
**Tracking:** origin/main (up to date)

> **The heading's open side is the `execute` stamp and under-reports this sitting
> by about eight minutes.** Switch-in ran ~19:18 EDT; conductor oriented 19:19
> and planned 19:25, and `execute` stamped **19:27**. **Thirteenth recorded
> instance of the conductor marker holding one line and therefore only ever
> reporting the LAST phase, while the open time is a property of the FIRST.**
> The eight minutes are the smallest gap yet recorded, because the plan gate
> resolved in one exchange. The row is in TODO's Backlog with the running tally;
> the measured spread there is now 8 min · 14 · 26 · 27 · 66 · 157 · ~143.

## What Was Done

**Per-task actuals:**

```
design amendment: downgrade · amend · sweep · render · eye · reseal — started 19:27 (marker) · landed 20:05 (02b3d7d)
board refresh + the single push                                     — started 20:05 · landed 20:05 (9de267a)
```

**`requirements-success-measurement`'s design gate is RESTORED and the whole
chain ran in one continuous sitting**, exactly as the 2nd sitting required:
downgrade -> amend -> sweep -> render -> combined producer eye -> reseal ->
board refresh, landing as **one push**. The item routes at **loop** again with
15 unchecked Pieces, 4 landed. **Nothing was built:** `tools/` untouched,
`catalog.md` and `categories.md` unedited, Kerd still **v0.104.0**.

**All seven things the producer sent back to design are settled**, and five of
them left `## What this design does not settle`. The design doc gained two new
sections — `## The observed-result entry` and `## Register artifact extensions`
— and lost the third edge from `## The edges`.

**Three producer rulings landed, two of them at the eye rather than the gate.**

1. **The binding direction.** It runs from the immutable observed-result entry
   **to** the `MSC`, never the reverse — *"The living MSC must not be edited
   after acceptance merely to point at a dated record."* This resolved an
   apparent conflict inside his own Step 2 ruling, raised at the plan gate: the
   hand-back asked design to define *"how an `MSC` references that immutable
   record"* (outbound) while the seven required fields put the `MSC` ID and its
   frozen fingerprint **on the record entry** (inbound). Inbound wins.
   `evidenced-by`/`evidences` is **removed as a stored register link role**; the
   anatomy arrow **reverses**; discovery is **derived** by scanning immutable
   acceptance records. Verified before the edit: `evidenced-by` was never
   declared in `catalog.md` (only `verified-by`, at `:204`) and no link role
   appears anywhere under `tools/` — so the catalog stayed untouched.
2. **The evidence digest — mandatory, no escape.** `Evidence: <repo-relative
   path>` plus `Evidence-SHA256: <64 lowercase hex>` over raw bytes, recomputed
   by the parser. A missing file or a mismatch makes the result invalid and
   therefore `NOT ASSESSABLE`, never `PROVEN` or `NOT MET`. External evidence
   must be **snapshotted locally before acceptance**; a bare URL or mutable
   CI-run location is insufficient in this slice, and provider-native
   immutability is deferred to the out-of-repo-artifacts problem. **This
   overruled the session's own proposal**, which offered an optional digest with
   a `digest: none — <reason>` escape on the argument that a fabricated digest is
   worse than none. He refused the escape and closed the gap the other way.
3. **The assurance taxonomy splits four ways.** `machine-refused` ·
   `machine-detected` · `producer-agreed` · `no enforcement`.

**THE COMBINED EYE BLOCKED TWICE, and both blocks were claim defects rather
than layout** — his words: *"The renders are visually clean, but two correctness
issues prevent my key."*

- **`Taken` was a declared required field the qualification contract never
  checked.** The entry showed seven fields, the prose claimed eight required
  facts, and the box then listed seven conditions with no date validation. His
  count is the correct one: **nine** required facts, **eight** qualification
  conditions (the value and its unit are two facts checked by one condition).
  `Taken` became condition 7. His rule: *"The prose and parser contract must
  match."*
- **The assurance legend promised refusal while one of its own rows only
  finds.** The legend defined machine-checked as *"something on disk refuses"*,
  and reciprocal stamping explicitly emits a **finding**. *"Those claims cannot
  both stand… Do not count reciprocal stamping among refusal-backed machine
  checks."* Hence the four-way split, and reciprocal stamping is drawn as a
  **hollow** dark square — same family, visibly weaker.

**The assurance tally was re-derived from the drawing's rows, twice.** Thirteen
questions at the GO became **fifteen questions carrying nineteen markers**, four
dual-marked. Today **5 machine-refused / 0 machine-detected / 3 producer-agreed
/ 7 unenforced** — ten of fifteen on agreement or nothing. Once the designed
machinery is built: **8 / 1 / 3 / 3**. Two questions were added (reciprocal
stamping, the evidence digest) and two moved (the entry check to
machine-once-built; the comparison from unenforced to **producer-agreed**, on his
ruling that *"the comparison is not assured by nothing"*).

**Resealed on his key, from final content:** `condition-anatomy`
`fp:67ff11391faf → fp:a75179033850` · `condition-lifecycle` `fp:22651ad0d921 →
fp:da2a068e7a94` · `assurance-boundary` `fp:544e73328c06 → fp:73ba8986c228`.
**The dated GO record was left untouched** and still carries the pre-ruling
seals and the pre-ruling four-object model, which is correct — it records what
was believed on 2026-08-31.

**Prose swept, with two supersessions executed in place rather than deleted.**
`CONTEXT.md` carried two standing decisions falsified by the rulings — the old
assurance tally, and the `evidenced-by` edge in the four-objects decision — both
struck in place with their replacements named, per Law 4. The design doc's own
`## Proposed supersession` block said *"observed results are linked evidence"*,
which the binding ruling falsified; corrected, and the correction labelled,
because amending a *proposed* edit inside a living design doc is not rewriting
history. A third strike landed inside the rulings section itself: the row-13
ruling recorded `nine, three, three — six of fifteen` an hour before the
taxonomy split superseded that figure.

**The artifacts, named by path so a pickup can reach them.** Design package:
`docs/design/requirements-success-measurement.md`. Work record with the
concerns block and the three reseals:
`docs/product/requirements-success-measurement.md`. The three sealed views — the
fingerprint covers the `.html` only —
`docs/design/requirements-success-measurement/condition-anatomy.html`,
`docs/design/requirements-success-measurement/condition-lifecycle.html`,
`docs/design/requirements-success-measurement/assurance-boundary.html`, each
re-rendered for the producer's combined eye to
`docs/design/requirements-success-measurement/condition-anatomy.png`,
`docs/design/requirements-success-measurement/condition-lifecycle.png` and
`docs/design/requirements-success-measurement/assurance-boundary.png`; **a
render is never the view**. The frozen work specification, deliberately
untouched all sitting:
`docs/plans/2026-09-01-requirements-success-measurement-spec.md`. The untouched
GO record: `docs/gates/2026-08-31-requirements-success-measurement-design.md`.
The session's plan visual was written to the scratchpad and is not committed.

**TODO closure review** (this boundary):

```
  ✓ done   — "the design amendment pass, seven things"     (02b3d7d; route restored to loop, check design PASS)
  ✓ done   — "downgrade -> amend -> render -> eye -> reseal -> board refresh, one chain"  (one push, CI 33573854422)
  · open   — "the suspect-link stamp has no slot in the format"  (narrowed again: the design now specifies TWO
             stored edges with different payloads; the BLOCK SHAPE — where each stamp is written — is still unwritten)
  · open   — "out-of-repo artifacts have no home"          (untouched, but GAINED an input: provider-native
             immutable evidence was deferred to it by ruling 2)
  · open   — "composer emits hard-coded absolute paths" · "grep -c in a fail-fast chain" · "a spec writes the
             consequence of an open question as settled fact"  (all three become clauses in the composer brief)
  · open   — "funnel-driver acceptance gate" · "gate-visuals slices 2 and 3" · standards spike x4 · archaeology
  ! note   — the pilot `stage-route-consistency` is STILL unframed and still invisible to every machine surface
```

## What's Next

- **The composer dispatch, and the next sitting opens with it.** The producer
  closed this sitting rather than starting it: *"The composer revision is
  substantial enough to deserve a fresh context and its own review window —
  especially because it must remove an obsolete design path across 16 sites, not
  merely incorporate wording."* The **eight-item brief is recorded verbatim in
  TODO's `## Now`** so the next sitting does not re-derive it.
- **Item 1 of that brief is a removal, not a rewording.** `evidenced-by` appears
  in the frozen spec **16 times** — a `## Link roles` table row (`:386`), fixture
  `T60` (`:1012`), the register citation form (`:1369`, `:1390`, `:1393`), and
  two steps written entirely around the ruling that was **not** taken
  (`:559`–`:570`) — plus a stale *"thirteen assurance questions"* at `:629`.
- **Three clauses the brief must carry that exist nowhere today**: forbid
  absolute repository paths outright · name hard-coded paths as an explicit
  cold-review hunt target · require cross-step dependencies to be marked, so
  keying a gate names what it invalidates.
- **Position:** `requirements-success-measurement` at **loop** (15 of 19 Pieces
  remaining, 4 landed; acceptance deliberately open) · `gate-visuals` at
  **ready-to-release** · `funnel-driver` at **acceptance** · the pilot
  `stage-route-consistency` at **frame**, unframed, 4 inputs missing for
  viability · Kerd at **v0.104.0**.

## Key Decisions

Full rationale in CONTEXT.md. **The binding runs from the immutable record to
the living condition, never the reverse** — the condition does not reach forward
into history, so `evidenced-by` dies as a stored link role and discovery becomes
derived · **the evidence location carries a mandatory content digest**, because
a location answers *where* the evidence was found while only a digest answers
*what* was judged · **a check that FINDS is not a check that REFUSES**, so the
assurance taxonomy splits four ways and a non-blocking check is never counted
among refusal-backed ones · **a required fact the parser does not check is not a
requirement**, it is a comment wearing a requirement's clothes.

Nothing was pruned from CONTEXT.md: no acceptance record landed and no drop was
agreed, so this sitting was structurally not a licensed prune event. Two
standing decisions were **struck in place with replacements named**, which is
supersession rather than pruning.

## Commits

Two work commits plus this boundary, all pushed to origin/main in **one push**,
CI green (`33573854422`, 12s): `02b3d7d` (the design correction and three
reseals) · `9de267a` (render refresh, no trailer, per the ship flow). Neither
carries a `Piece:` trailer — the amendment is not a spec Piece.

## Gotchas

- **The progress render can be owed when the board has not visibly moved.**
  `docs/plans/progress.html` carries a `state` digest over derived content, and
  the concerns block's three new fingerprints moved it while the board itself
  was byte-identical (same rung, same 4 landed / 15 remaining). `progress.svg`
  and `progress.excalidraw` did not change at all. Skipping the render commit
  because "nothing looks different" would have failed CI's stale check on the
  next push. **Run the renderer and read `git status`; do not decide from the
  board.** Mirrored to `docs/playbook.md`.
- **A drawing that declares N required fields and then writes an N-minus-one
  contract passes every visual review.** The anatomy view listed `Taken` as a
  required field and the qualification box never checked it; three renders were
  looked at, by the author, before the producer caught it. **When a drawing
  states a count, count it against the contract beside it.** Mirrored.
- **A legend is a claim about every row it labels.** The assurance legend
  promised *"something on disk refuses"* while one of its rows only emits a
  finding — the contradiction was created by adding a row, not by editing the
  legend, so nothing drew attention to it. **Adding a row to a categorised
  drawing re-tests the category definitions.** Mirrored.

## Insights

- **The producer's eye caught two defects in a pass that had already been
  swept.** Both were arithmetic-or-definition mismatches inside artifacts whose
  visual layout was correct, and both were introduced by *this* session rather
  than inherited. That is now the **third consecutive item** where the drawings
  land and the sentences about the drawings carry the defects — it should be
  treated as the standing expectation rather than a surprise, which argues for
  a named beat that checks a view's claims against its own contract and legend
  before it goes to the eye, not after.
- **The sharpest question of the sitting came from reading two of his own
  rulings against each other.** Step 2's hand-back asked design to define how an
  `MSC` references the record, while Step 2's required-field list put the
  reference on the record. Neither was wrong in isolation; the conflict was only
  visible when both were held at once, and surfacing it at the plan gate cost
  one exchange and settled four of the seven amendment items. **A hand-back list
  and a field list are two specifications of the same relationship, and they
  should be diffed on arrival.**
- **The producer asked for visuals mid-turn and was right to.** *"youve also
  stopped visualizing things for me"* — the session had spent two long prose
  messages arguing a structural point (the edge direction) that a drawing settles
  at a glance. The repo's own 2026-08-23 rule already says diagram and prose are
  one deliverable produced together, and the default had quietly reverted. The
  plan visual that followed made the demotion dip and the single-push constraint
  legible in one image. **The standing Backlog row — flip the default in the
  skills — has now measured its own absence.**
- **Two rulings this sitting closed gaps by making the CHEAP state impossible
  rather than by adding a check.** The digest has no `none` escape (snapshot the
  evidence instead), and a non-qualifying entry cannot report `PROVEN` (it
  reports `NOT ASSESSABLE`). Both follow the repo's own *the cheap state is the
  one that must be argued for* pattern, and both were the producer choosing the
  more expensive option — the fourth and fifth times he has done so on this item.


---

# Session 2026-09-01 (4th sitting — the spec revision and a corrected build picture, closed 22:17 EDT)

**Machine:** Anthonys-Mac-Studio.local (the Mac Studio; thin-client host, tmux over SSH from the MacBook)
**Branch:** main
**Tracking:** origin/main (up to date)

> **No conductor ran this sitting, so there is no honest open time and the
> heading carries a close time only**, per switch's rule. The work was
> producer-directed turn by turn. For orientation only, not as a derived range:
> the session's own `UserPromptSubmit` hook stamps put switch-in at **~20:12
> EDT**, making this roughly a two-hour sitting.

## What Was Done

**The composer revised the work specification against the resealed design, and
it landed.** `528ca88` (the revision) and `f0fcd79` (render refresh), one push,
CI green `33580251351` on the exact head SHA. The item routes at **loop** with
**16 unchecked Pieces**, 4 landed. Kerd still **v0.104.0**; nothing was built.

**`evidenced-by` is gone from all 16 sites**, the acceptance-record entry
contract (D6 — seven fields, nine required facts, eight qualification checks) is
in with evidence-digest recomputation and both `NOT ASSESSABLE` paths, reciprocal
Requirement <-> `MSC` stamping is specified as two stored edges at finding
severity, and the stale *"thirteen assurance questions"* is corrected. 19 steps
became **20** — Step 9, the observed-result entry parser, is new machinery, which
is why open Pieces went UP from 15 to 16 rather than down.

**The producer's three acceptance conditions were carried as REJECTION criteria,
not style notes**, and all three verified in the returned score: zero absolute
paths (21 `rev-parse --show-toplevel` derivations), zero-expected checks made
exit-safe and tested in both directions, and open-gate consequences expressed as
dependencies rather than settled fact.

**One producer ruling at the gate, and it went the expensive way again.** The
composer raised that D6 check 6 validated `Method` as *shape* while check 1
required register *existence*. His ruling: a reference that parses is not a
reference that resolves; a phantom method is `NOT ASSESSABLE`. Corrected as a
bounded hand-back — re-dispatch, never re-specify — with an explicit boundary
against adding a method fingerprint, and the version-binding question **filed**
rather than absorbed.

**Three defects the composer found and fixed unprompted:** the spec's preamble
carried stale pre-reseal fingerprints (now matching the work record exactly),
Step 4's Verify grep double-matched after last night's ruling, and a latent
fixture bug in T58 demanded an empty `blocks` while editing a `final` block.

**Then the sitting turned into a measurement of where the build actually is, and
a standing claim was refuted.** The session reported that Drive had shipped,
reading `funnel-driver` at `acceptance` with 6/6 Pieces. The producer refused it
in one line — *"drive has not shipped - its not possible"* — and he was right.
Drive's own text says *"Viability, scope, design, work handoff, loop and
acceptance have no question set yet."* **Drive drives one gate of seven.**

**The effort ratio was measured rather than felt:** over 14 days, **2,392 lines
of capability against 107,569 lines of documents — 1:45**; the last three days
were 7 capability changes to 111 document changes; `requirements-success-measurement`
was framed 2026-08-28 and has produced one commit to `tools/` in five days.

**Switch-in cost was measured, diagnosed and deferred.** The pickup spent ~17% of
the window. `CONTEXT.md` is 177KB of a 250KB read set, and `## Key Decisions` is
97.9% of it. **The decisive finding: old bullets do not accrete** — of the 48
standing on 2026-08-04, 44 survive and grew 1.01x — while the 87 added since
average 1,696B against the survivors' 672B. **Pruning is aimed at the wrong
variable**, which is why both licensed prune events ended with the file bigger.
The producer's call: *"do nothing for now"*, with a return condition filed.

**A visual of the process build-out was asked for and delivered** as a dated
snapshot artifact, every number derived from disk at `f0fcd79`. It names its own
defect: nothing checks it for staleness, and the durable version is a generator
under `tools/diagram/` that does not exist.

## What's Next

- **The viability question set, as a SPIKE.** One rung, answering one question:
  can a set be **derived** from gate needs plus decisions already
  standards-anchored, or does each rung need fresh Law 4 research? That is the
  producer's stated fear and it is a sitting to settle.
- **The shape is settled:** per rung, work-type-neutral, six small files. The
  working example is six questions and 24 lines, and every question is a gate
  requirement said in plain language.
- **Two items need a record and a key, not work** — `funnel-driver` and
  `progress-html` sit at `acceptance` with zero Pieces remaining. Two records
  take the board from 9 to 11 of 22 done.
- **Deliberately parked:** `requirements-success-measurement` at loop with 16
  Pieces and a freshly correct spec. Its context goes cold; that is the price.
- **Position:** `requirements-success-measurement` at **loop** (16 of 20 Pieces
  remaining) · `gate-visuals` at **ready-to-release** · `funnel-driver` and
  `progress-html` at **acceptance**, 0 Pieces · the pilot
  `stage-route-consistency` still unframed at **frame** · Kerd at **v0.104.0**.

## Key Decisions

Full rationale in CONTEXT.md. **A reference that parses is not a reference that
resolves** — and resolution is not versioning, so the frozen-method-version
question is filed rather than absorbed · **Drive drives one gate of seven, and a
rung position cannot show capability completeness** — it measures the slices that
were framed, so most of Drive is not in the denominator · **the ceremony has no
working proportionality dial**, measured at 1:45, because `rigor-level` slice 1
shipped the declaration and slice 2's floors were never built · **a question set
is per rung and work-type-neutral**, on the evidence that the one existing set
cannot justify its own filename.

Nothing was pruned from CONTEXT.md: no acceptance record landed and no drop was
agreed, so this sitting was structurally not a licensed prune event.

## Commits

Two work commits plus this boundary, pushed to origin/main in one push, CI green
(`33580251351`): `528ca88` (the spec revision) · `f0fcd79` (render refresh, no
trailer, per the ship flow). Neither carries a `Piece:` trailer — a spec revision
is not a Piece.

## Gotchas

- **A rung position measures the slices that were FRAMED, never the capability
  the slug names.** `funnel-driver` at `acceptance` with 6/6 Pieces reads as
  "the umbrella is built" and means "the two slices that were framed are
  complete". Unframed work is not in the denominator, so a percentage derived
  from the board flatters anything whose scope was never written down. Mirrored
  to `docs/playbook.md`.
- **A grep that appears to confirm a claim may be matching a different
  mechanism.** `grep -i floor tools/gates/*.py` returns three hits and they are
  the killer-risk floor, not `rigor-level`'s per-level floors; `purity` returns
  two and they are a selftest membership assertion, not the fold closing check.
  Both original claims survived — but the greps that look like proof are not.
  **Read the hit, never the count.** Mirrored.
- **Announcing an action in the same breath as a tool call that does something
  weaker leaves contradictory evidence in the record.** This session wrote
  "actually executing the new Step 9 probe" as a preamble to a call that only
  located and read it, then correctly said later that the probe was never run.
  The producer caught the contradiction and required it reconciled before the
  commit. **Name what the command does, not what it is for.** Mirrored.

## Insights

- **The producer's one-line refusal beat the model's derived evidence, and the
  evidence was on screen.** *"drive has not shipped - its not possible"*
  overturned a claim built from a board reading, a Pieces count and a file
  existing. The correction took one grep of Drive's own text. **A skill file
  being 104 lines against conductor's 394 was visible before the claim was made
  and was not weighed** — the model had a size signal and used a status signal.
- **The measurement that changed the conversation was a RATIO, not a total.**
  "107,569 lines of documents" is inert; "1 line of capability to 45 of
  documents" is a diagnosis. Both came from the same command.
- **Tonight's own CONTEXT.md bullets were written at ~800B against the 1,696B
  average this session measured.** Whether that holds is the only real test of
  the finding, and it is checkable at the next boundary.
- **The producer's fear about research cost was the right question aimed at the
  wrong artifact.** Six question sets sound like six Law 4 investigations; the
  one that exists is 24 lines of gate requirements rephrased, and the standards
  work for the rung placement was already fetched and paid for in August. **The
  spike exists to test that, not to assume it.**
