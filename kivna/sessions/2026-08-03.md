# Session 2026-08-03

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

*Long session, spanning 2026-07-31 → 08-03.*

## What Was Done

**Shipped three conductor versions, none of them run.**

- **v0.66.0 — conductor re-seated into four roles.** From Tony's observation that conductor works well with Fable driving but burns too many Fable tokens *managing* subagents. The seating went through three framings before landing: my "sub-conductor under Fable" → Tony's inversion (Fable composes, Opus conducts) → the final one where **Tony is the composer**, a top-tier model is the **orchestrator** (called as a subagent, writes the score, leaves), the session model is the **conductor**, and cheap subagents are **players**. Dominant saving is *context size*, not task allocation. Consequences: model advisory now recommends the conductor model and never the top tier for difficulty alone; the orchestrator is called in two passes and writes the spec to disk returning only a summary; tags assigned *after* the step body is written; blast radius answered by a `[keep]` review step, not by keeping the risky edit; **re-dispatch, never re-specify**, with a three-failure hand-back.
- **v0.67.0 — conductor commits its own work.** Tony: having to switch out just to commit forced early session ends. Investigation found conductor already contradicted itself — principles banned pull/push in one line and pull/push/commit in the next, while "docs travel with code, in the same commit" required a commit that could never happen. Split: *work commits* are conductor's (per verified task, staged by name, pushed); the *session-state commit* is switch's; pull stays switch-only. Swept five stale "switch owns all git operations" claims from switch, conductor, tend, README and the playbook.
- **v0.68.0 — say it in the user's terms.** From a dinner-tonight transcript where three rounds of file names and migration IDs produced no decision, and one plain restatement resolved two product questions at once. Shape: *now / the change / what it means*; a removed capability must be **named as a loss**; question test is *could they answer it without reading the code?*

**Tested the orchestrator mechanic.** `model: fable` was accepted by the Agent tool (failed on quota, not validation — Tony is out of Fable credits). Re-ran on Opus: **1,601 words written to disk, ~180 words returned — 9:1.** The subagent also followed the after-the-body tagging rule unprompted and produced 3 delegate / 1 keep, where the keep was a diff-review step. Its three factual claims about the repo all checked out.

**Ran a whole-system design pass**, which became the session's centre of gravity. Findings, all measured:

- superpowers **plans** are inert: 10,177 lines across 17 docs in 3of3, **0 of 472 checkboxes ticked**. Its **specs** are alive (19 docs, referenced from 5+ session logs, survived every trim pass).
- conductor **already replaced `writing-plans` in July** — superpowers artifacts run 20 Jun / 17 Jul / **0 Aug**; Kerd's `docs/plans` run 0 / 7 / 3. Nothing announced it.
- `capturerequirements` has **zero artifacts in any repo, ever**. mode, sherpa and interrogate are also never used.
- **sherpa is a routing orphan** — the cross-skill reference graph shows nothing points at it, and conductor's only routes are kivna/slainte/switch, all lateral or downstream.
- **0 CI workflows and 0 pre-commit hooks across every repo.**
- Design docs are never retrieved: a 6 Jul design doc held the exact answer to a 1 Aug question and went unread while the session read a stale code comment and got it wrong twice.

**Built a living design diagram** at `docs/plans/2026-08-02-product-to-build.excalidraw` (+ `.svg`), generated from `tools/diagram/`. Ten movements: four altitudes; actual skill usage; the routing finding; bets; trim's exit; a **17-function map** with inputs / outputs / evidence; build sequence (MVP / SPIKE / v1 / SOMEDAY); cross-cutting constraints; **first-cut requirements**; and a **tooling catalogue** (tool → caller → requirement → met?).

**Established a shared-whiteboard working loop.** Excalidraw's clipboard format is `excalidraw/clipboard` JSON, so `pbcopy`/`pbpaste` is a bidirectional channel needing no install; the Chrome extension lets Claude drive the canvas directly. Proven both ways in one round trip. Tony's annotations are preserved to `docs/plans/annotations/` and merged back on every regeneration.

## What's Next

- **Agree the 18 first-cut requirements, one at a time.** Tony's sequencing: requirements → agree → *then* HOW and WHAT, which will dictate what happens to the five scattered communication statements and to every other function. Two are marked `(?)` because they are drafted rather than read: *Design the interface MUST output machine-checkable values*, and *How we talk to each other needs an enforcement point*.
- **Do not use `/kerd:capturerequirements` for this.** Tony's explicit instruction — it is under review, and its dated-snapshot output shape is the thing we suspect is wrong.
- **Nothing gets ripped until the design is approved.**
- The SPIKE, when we get there: route ONE dead skill cheaply and watch whether it gets used. Beats route-vs-rip as a binary, since neither side has evidence.

## Key Decisions

- **Release is the time axis, not a fifth rung.** MVP / v1 / v1.2 each cut through all four altitudes. This resolved current-truth-vs-intent for a product spec: the spec holds what is true now, each release is a delta — OpenSpec's model, reached independently.
- **Don't adopt OpenSpec / Spec Kit / BMAD, don't clone superpowers, don't remove it.** Its two halves had opposite fates; removing the plugin would discard the design step that survived along with the plan step that died.
- **Design is agreed in diagrams, not prose.** Tony thinks in high-level diagrams — that is what "too much noise" has meant every time. Grammar taken from his DDIL deck; mermaid cannot express it.
- **UI/UX design and its approval sit BEFORE the loop**, in product planning and design — corrected from my placing final approval after the build.
- **Post-build conformance is its own step**, half machine and half human: check the built thing against everything declared upstream — code, logic, architecture, pixel-perfect UI, and the product measurements. This gave the evidence column a consumer.
- **Entry gate at every rung: "do we have what we need?"** — check the declared inputs, and push back naming what is missing rather than proceeding on assumption.

## Commits

Fourteen this session, from `2b3e4db` through `9cbdc06` plus the switch-out commit. Three feature releases (v0.66.0, v0.67.0, v0.68.0) and eleven documentation/diagram commits.

## Gotchas

- **Excalidraw reassigns every element id on paste.** An ownership scheme based on id patterns fails on the first round trip — after one paste my own 209 elements read as foreign. Use `customData` instead; it survives the clipboard. Mirrored to `docs/playbook.md`.
- **zsh does not word-split unquoted variables.** A `for x in $VAR` loop over a space-separated string runs once with the whole string. Use an array. Cost a bogus "every skill is an orphan" result before it was caught. Mirrored to `docs/playbook.md`.
- **`find ~ -name ...` without a depth limit will hang past the command timeout.** Scope searches to specific directories. Mirrored to `docs/playbook.md`.

## Insights

- **The useful mechanisms already existed, aimed at a different problem.** The `low` switch modifier (built for token budget, wanted for context hygiene), the self-correction reporting rule (built to distinguish "guard worked" from "got lucky", and it is the nearest thing to a degradation signal), conductor's pre-flight inventory (an entry gate that exists at exactly one rung). Finding the second caller for an existing mechanism is cheaper than building a new one — and the tooling catalogue exists to make those second callers visible.
- **Generating artifacts unseen is a defect, not a caveat.** Two diagrams shipped and validated before anyone looked at them; the first render immediately exposed a caption sitting on top of a box. The checker had a blind spot shaped exactly like the thing I could not see. Both a bound-text and a collision check now run on every generation, and the collision check has caught two faults since.
- **Almost every correction Tony made was a *relationship* between functions, not a property of one** — design approval gates the loop, the loop requires CI, the evidence column needs a consumer, the conformance step reads what PRODUCT declared, testing is decided once and applied many times. None would surface from gathering requirements function-by-function in isolation, which is the argument for a full shallow pass over a deep one on three.

---

# Session 2026-08-03 (second)

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

*The requirements walk begins. Started at the top of the board, function by function.*

## What Was Done

**Captured four cross-cutting concerns Tony raised, none of which were on the board.** He opened by asking whether a progress visualisation idea from the previous session had survived. My first answer was wrong — I grepped and reported "not captured" when it *was* captured, on the entry-gate requirement, phrased as `RENDERED, not prose: have / need / progress`; my pattern just didn't match that wording. What was genuinely missing was the wider framing (invocable at any time, produced at every phase-gate close). Added as functions: **Show where we are**, **Size work to a model**, **Where the work is written down**, plus communication expanded from one MUST to five (diagram grammar, round-trip, story format, enforcement point).

**Restructured the requirement row, which was the session's real turn.** Tony asked why evidence sat after outputs. It shouldn't have — but not for the reason he assumed. One word was carrying three distinct ideas: what you must read *before* producing (grounding), what proves the output good *after* (acceptance), and whether the function runs at all (a diagnostic of the current system). Movement 6 became `in / grounding / out / acceptance` in execution order; the diagnostic moved to a new movement 11. That movement 11 is the first concrete instance of *Show where we are*, so the function acquired a working caller the same day it was drafted.

**Interviewed `Frame the intent` in full — 1 of 25.** Triage is a branch with three exits, and the third (a *question about the product*) leaves the stage entirely, answered from product truth. The routes diverge at grounding and rejoin at acceptance. Output is **two documents**, not one shape with optional sections — an idea brief and a problem statement, both `.md` + diagram, differing in sections. Acceptance is two keys, machine and human, neither sufficient alone. Both routes hand off to Test viability.

**Built the stage-flow generator.** `tools/diagram/gen_flow.py` plus `kit.py`, which extracts the drawing primitives so the board and the flows cannot drift into two dialects. Arrows added to the toolkit and to the SVG renderer — used in flows only, since sequence is the point of a flow and containment cannot express order.

**Agreed `Where the work is written down`.** Date records of events, never date living documents. Homes declared for the two product documents, gate records, sessions and the vault.

**Agreed the colour grammar and made annotations a queue.** Green is Tony's input (I first mis-applied it to "steps where both act" and reverted). Blue is changed-since-reviewed, with an explicit `mark_reviewed` baseline. Comments are now read, acted on, and deleted, with disposition logged.

## What's Next

- **`Test viability`, function 2 of 5.** Its first-cut MUST is *"name the assumption that kills it, and record which risks were tested versus carried forward — carried is fine, silent is not"*; today's work established that it receives a viability **signal**, never a verdict, and that the killer assumption differs by route (idea: is the need real and can we win; problem: is the cause correctly located and is closing it worth the cost).
- Two loose ends: the route-specific acceptance checklists for function 1 are named but not written, and the *reachable* clause of the naming requirement is still open.
- The `docs/plans/` rename implied by the naming rule is deliberately not done — nothing gets ripped until the design is approved.

## Key Decisions

- **in / grounding / out / acceptance** — four fields, execution order. Grounding is the entry gate's real job.
- **Date records of events, never living documents.** Test: would rewriting it tomorrow be correct, or falsifying?
- **Red = cost, green = Tony's input, blue = changed since reviewed.** Blue not red, because red is load-bearing and everything is new once.
- **Annotations are a queue, not an archive.**
- **A tool declares the route it serves and is invoked on match** — sensei's trigger is a problem that survived a few attempts to fix it.
- **Sensei is proven elsewhere, untested in Kerd** — Tony's correction. The bet is transfer, not the method.
- **Triage is a branch inside one function, not a fork between two.** Both routes hand off to Test viability; a problem going straight to design is the jump-to-countermeasure failure, and the risk is highest on that route.

## Commits

`f9741d4` board restructure + function 1 interview · `a976e11` colour grammar, blue deltas, sensei correction · `ca97cfb` annotations as a queue. Plus the switch-out commit.

## Gotchas

- **`customData` does not solve Excalidraw ownership either — this corrects yesterday's gotcha.** Ids under-claim (everything reads foreign after a paste); customData over-claims, because Excalidraw propagates it onto newly drawn elements. The round trip reported `total 91 | kerd 91 | tony 0` while two were plainly Tony's. What works is diffing the clipboard against the generated file by text. Mirrored to `docs/playbook.md`.
- **A checker built on your own renderer is blind to the target renderer's interpretation.** The review SVG passed all three layout checks while native Excalidraw drew the same arrows as curved loops — `roundness: {"type": 2}` curve-fits an orthogonal path into a blob. Three headless renders missed it; one paste to the canvas showed it instantly. Mirrored to `docs/playbook.md`.
- **A layout checker only catches the collision class it was written to imagine.** The box-collision check compared text against rectangles, so nine-line requirement text printing through two rows below it never flagged. Mirrored to `docs/playbook.md`.

## Insights

- **Three defects today, each found by a different mechanism, none reachable by the other two:** fixed row spacing (found by looking at the render), text-on-text (found by a new checker), curved arrows (found only on the real canvas). The checking loop needs all three, and the canvas pass is the one that was missing — it exists only because Tony asked "did you put it on the chrome session?".
- **Every substantive correction came from drawing, not discussing.** The question-exit, the true width of the triage branch, and the two-document split were all invisible in prose and obvious once boxed. That is the measured case for "design is agreed in diagrams", arrived at by running it rather than asserting it.
- **The cheaper policy dissolved the problem the expensive mechanism was for.** I was about to build annotation anchoring — capture each comment's nearest element, re-place on regeneration — when Tony said to delete comments once dealt with. A comment that lives one cycle cannot drift, so none of the machinery was needed.
- **Restructuring forces definition in a way naming never does.** The three-concepts-in-one-word collision surfaced only because Tony tried to *reorder* the columns. Every row looked filled in, so nothing flagged; reordering forced the question of what each column was for.

---

# Session 2026-08-03 (third)

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

*PRODUCT rung completed. Five functions walked, one cut, one added.*

## What Was Done

**Walked `Test viability` (function 2).** Tony corrected the diagnostic I had drafted: risks are not unnamed. They are unmitigated, **unqualified**, or accepted unknowns — and unqualified is the dangerous state precisely because it does not look dangerous. A named, unsized risk reads as managed. **Evidence qualifies a risk, and it is a test OR an analysis** — analysis is evidence, usually the cheapest, and "run the cheapest test" had been quietly excluding it. Qualified means proven AND measured: impact in the value's units, likelihood recorded separately and never multiplied, because expected value is the wrong maths for a bet taken once. **Fatal = impact ≥ the value framing declared**, which makes it mechanically checkable against function 1's output. Then Tony's two structural moves: **a risk without a countermeasure is a BLOCKER** (flipping the default so silence stops work rather than passing it), and **high impact + high likelihood + no countermeasure = dead project** — the one blocker that cannot be accepted by name, which stops "accept by name" becoming an escape hatch.

**Cut `Hold product truth` (function 3).** It failed its own test: asked whether any question had ever needed answering that the code could not answer, the answer was no. I had been describing this function as established when it rested on a single data point which, read properly, argued the other way — the 6 July design doc that held 1 August's answer **existed**. That is evidence retrieval failed, not that a document was missing. Kept on the board struck through with a return condition rather than deleted.

**Walked `Slice a release · Set the goal` (function 4).** A release is a **grouping**, not a time axis — correcting yesterday's record. Five factors decide it and they work three different ways: dependency forbids, user comprehension caps, effort/risk/opportunity shape. The comprehension ceiling is the unusual one — the bound comes from the receiving side, which is why "we shipped everything we had" is a real failure. **The DONE condition is ASSEMBLED, never authored**: every item a conformance check against an upstream declaration. That dissolved the circular draft and resolved two orphans without new functions.

**Added `What we ruled out, and why` (cross-cutting).** Four functions independently demanded this output and none had a home for it. Tony collapsed my two-tier design in one line — *"a fix that failed is the same as a rejected approach no?"* — which was correct, and my split was smuggling back a distinction already dissolved at function 2. The unit is the **concept**, not the attempt and not the code.

**Walked `Choose what matters next` (function 5).** Built as a working view over the real Kerd backlog rather than a spec, because axes are easier to judge against real items than to derive. Tony rejected effort and chose **value**; the swap changed the ranking, which is the test that it mattered.

**Drew five diagrams**, extracted `kit.Flow` so all stages share one layout, and marked four of them reviewed — the first real use of the blue-delta baseline.

## What's Next

- **Start the DESIGN rung — 4 functions, beginning with `Shape the solution`.** It is served today by superpowers brainstorming, marked PARTIAL: gives the capability, captures the plan phase, routes to `writing-plans`, never returns. That behaviour is what to interview against.
- Debt carried: the route-specific acceptance checklists for `Frame the intent` are named but not written.
- Two things still open and neither is small: the **reachable** clause, and **accepted risks age with nothing to bring them back** (temporary countermeasures are self-expiring; accepted unknowns and low-likelihood fatals are not).

## Key Decisions

- **A risk without a countermeasure is a blocker.** Silence stops work rather than passing it.
- **The limit on acceptance:** high impact + high likelihood + no countermeasure = dead project.
- **Impact and likelihood are recorded separately, never multiplied.**
- **A release is a grouping, not a time axis.** Ordering and scheduling are separable.
- **DONE is assembled from upstream declarations.** Nothing in DONE that nothing declared.
- **Choose what matters next: consequence × value.** Effort is not an axis — an input measure beside outcome measures.
- **Hold product truth is cut**, with a return condition.

## Commits

`60de710` Test viability · `8661154` cut Hold product truth · `8de542c` Slice a release + What we ruled out · `6716e48` two flows drawn · `1c9bace` reviewed baselines · `08d1742` Choose what matters next. Plus the switch-out commit.

## Gotchas

- **An Excalidraw paste silently does nothing without canvas keyboard focus.** After `cmd+A` then `Delete`, focus leaves the canvas and the following `cmd+V` is swallowed with no error — and `shift+1` on an empty canvas leaves you at 10% zoom on blank space, so it looks exactly like a paste that worked then vanished. Click the canvas first. Mirrored to `docs/playbook.md`.
- **I attributed the first occurrence of that to colliding element `index` values, and shipped the claim in a commit message.** The retry both re-indexed and clicked; the re-indexing got the credit. Re-indexing is still required when *merging* two generated diagrams, but it was not what unblocked the paste. Correction mirrored to the playbook alongside the entry it corrects.

## Insights

- **The vocabulary started blocking work, which is the first sign it is not decorative.** The maintenance risk on *What we ruled out* ran through function 2's machinery and came out high impact + high likelihood + no countermeasure = dead — so byproduct capture became the condition of the artifact existing rather than an optimisation. The same machinery was then applied to the decision to cut a function, as a temporary countermeasure with a return condition.
- **Swapping an axis changed the answer, which is how you know the axis mattered.** Under effort × consequence, "repin three repos" rose as a cheap win. Under value it fell correctly to pure hygiene. Effort is an input measure sitting beside outcome measures — incoherent, and it systematically flatters cheap work.
- **Three separate things acquired a return condition today** — a temporary countermeasure, an accepted risk, and an eliminated option. Three is enough to suspect a general rule rather than three coincidences.
- **Kerd already performs function 5 badly, and I demonstrated it repeatedly.** Switch's pick-list is a numbered menu ordered by position in TODO.md, title-only, no consequence, no value, no loss. I handed one over at switch-in and again after switch-out, on the same day we specified why that shape fails.
- **My own "unqualified risk" moment:** one observation, a plausible mechanism, written into a commit as established — three commits after building the machinery designed to stop exactly that.

---

# Session 2026-08-03 (fourth)

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

*DESIGN rung complete — and it collapsed to one function under interview. First live run of the v0.66–0.68 conductor session protocol.*

## What Was Done

**Ran conductor v0.66–0.68 live for the first time.** Orient (warm path), model
advisory (skipped correctly — already on Fable), plan gate, lean/inline execute,
two per-task work commits pushed mid-session, close-out. The v0.67 commit split
held in practice: work commits carried code + travelling docs, session state
stayed for switch. **The four-role delegation still has not fired** — the task
was all-judgment, so the lean path was correct and no orchestrator or players
were summoned. That machinery remains tested only by the 08-02 disk-write trial.

**Interviewed the DESIGN rung — it collapsed 4→1.** Opened by asking Tony where
"Shape the solution" ends and "Agree the shape" begins; his answer — *"i dont
know what agree the shape is or shape the solution - they are the superpowers
skill"* — exposed that the four-way split was my decomposition of the
brainstorming checklist, never his shape. The `(?)` marker existed for exactly
this and I asked him to arbitrate my own invention anyway. Re-run in his terms,
the rung is ONE function, **Design the solution**: one conversation producing
one package — detailed specs, architecture plans, testing strategy, and
diagrams for as many aspects as we can. Two-key GO (nothing left to annotate ·
every stage-1 measurement has a named answer in the package; measuring itself is
post-build). Home chosen A-over-B: `docs/design/<slug>.md` + `.excalidraw`,
living, undated, same slug as the product doc; GO writes a dated gate record
and hands to CONTRACT — never to `writing-plans`, which is where the working
half of brainstorming exits today. Grounding gained two entries from measured
failures: standing decisions, and the living design docs of whatever the work
touches (reachability's second caller). Captured in `DETAIL` + `REQUIREMENTS`,
walk table updated, stage flow drawn via new `gen_flow_design.py`.

**Found and fixed the board silently dropping a walked function.** `DETAIL`
keys are single-line, `FUNCTIONS` names wrap with `\n` — so `Slice a release ·
Set the goal`'s interview never rendered and a *reviewed* board showed it as
not yet walked. Lookup normalized. Found by a membership check, not by anyone
noticing the absence.

**Cleared the annotation-queue backlog.** Six 2026-08-02 board comments
predated the queue policy and were being re-merged onto every regeneration —
one had drifted onto board text. All six were long since acted on; dispositions
logged, both lingering capture files deleted, merge block removed from the
generator.

**Hit and survived the canvas race.** Tony's open Excalidraw tab wrote last
session's scene back over the freshly pasted flow; he reviewed the OLD diagram
believing it was new ("they look identical to previous ones" — it literally
was) and his two comments landed on the choose-what-matters view. Both
dispositioned: the Bree dogfood item is REMOVED from TODO (his words: not
working on that right now), and a standing correction accepted — **no skill
names in candidate views during the requirements walk**; requirements and
outcomes only. Re-pasted the real flow; his verdict: "yes! way way better".
Reviewed baseline set.

**Board hygiene:** movement-6 status tally now computed from the data instead
of hardcoded (was already stale).

## What's Next

- **CONTRACT rung: `Write the contract · Size and assign` (function 10).** The
  interview target: conductor v0.66–0.68 serves it — the session protocol now
  has one live run, but the orchestrator call and sized players have never
  fired. That untested half is exactly what this function's requirements
  should be interviewed against.
- Debt unchanged: route-specific acceptance checklists for `Frame the intent`;
  the `reachable` clause; accepted risks age with nothing to bring them back.
- Backlog gained: regenerate the choose view (Bree gone, no skill names) before
  its next use.

## Key Decisions

- **The DESIGN rung is one function — Design the solution.** One conversation,
  one package. The four-way split was solution-vocabulary from the tool's
  checklist, not Tony's shape.
- **Two-key GO for design:** nothing left to annotate + every stage-1
  measurement answered by name. Measuring is post-build conformance.
- **Design home:** `docs/design/<slug>` living pair beside the product doc;
  dated gate record on GO; handoff to CONTRACT, never a plan-writing skill.
- **No solution vocabulary during the requirements walk** (Tony). Candidate
  views name requirements and outcomes, never skills. Bree item removed.

## Commits

`b1a2b63` DESIGN collapse + interview capture + key-mismatch fix + annotation
backlog · `2ab1289` two-key review closure + canvas-race gotcha. Plus the
switch-out commit.

## Gotchas

- **Two Excalidraw tabs fight over one saved scene** — the idle tab clobbers
  the paste when it regains focus, and the review lands on the wrong artifact
  with no error anywhere. One tab per review cycle; refresh, don't switch;
  diff canvas texts against the generated file before any clear-and-paste.
  Mirrored to `docs/playbook.md`.
- **A dict keyed on display names silently drops entries when the name wraps.**
  The miss-path rendered identically to the legitimate "not yet walked" state.
  Normalize lookups at the boundary. Mirrored to `docs/playbook.md`.

## Insights

- **The rung's own shape was solution vocabulary.** The DESIGN split I drafted
  came from the serving tool's checklist — the same failure Tony then flagged
  on the choose view (skills as candidates). Twice in one session, the
  correction was "you are describing the tool, not the requirement". Worth
  watching for on the remaining rungs, all of which are served by Kerd's own
  skills — the pull toward describing conductor instead of the contract
  requirement will be stronger, not weaker.
- **The interview beat the diagnostic.** Every field of the agreed function
  (package, two keys, home, handoff) came from five short answers in Tony's
  terms; the skill-text diagnostic only confirmed what was broken. Reading the
  installed skill was still worth it — the "terminal state is writing-plans"
  line is now cited evidence rather than a summary claim.
- **The checking loop caught two silent failures before they shipped and one
  after.** Membership check → key mismatch; text-overlap checker → note
  collisions; and the canvas race was caught only because the review verdict
  ("identical to previous") contradicted what I knew was pasted. The third
  class has no automated check yet — a scene hash comparison before inviting
  review would close it.
