# Session 2026-07-06

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

- **First natural switch-in on the new 3-file read path** (from the cached v0.60.0 skill) — pulled, verified handoff, ran the hook smoke test (26/26 green), read CONTEXT.md + TODO.md + latest log only. Behaved as written.
- **Added a switch-in TODO pick-list** (v0.61.0): switch-in's Summarize step now ends with a full **numbered** menu of every `## Now` + `## Backlog` item, one terse line each — no truncation (TODO is lean by design), no reply-instructions. `skills/switch/SKILL.md` + README. Shipped under the 0.61.0 bump.
- **Refined focus's question rule from a hard multiple-choice ban to a quality bar.** Multiple-choice is allowed *only* when it clarifies a real choice that's the user's to make (2-4 crisp, distinct options) — never a lazy binary that offloads a call Claude should just make, never vague or verbose. Wired into `hooks/focus.sh` (re-injected every prompt while focus is on), `skills/focus/SKILL.md` rule #3, and README. Updated the hook test assertion (was hard-coded to the old "speech-bubble" wording); 26/26 green.
- **Left a ready-to-drop CLAUDE.md phrase in the backlog** to consider promoting the question rule into global `~/.claude/CLAUDE.md` (replacing the existing hard ban in the Question-formation gate). Not done — Tony's personal file, his call.
- **Backlogged: guard switch-in step 3 smoke test against context bloat** — sparked by `~/3of3` running build+smoke on every switch-in; raw build output floods the deliberately-lean pickup. Fix (delegate to a build subagent or tail/grep) noted for next time switch is touched.
- **Interactive backlog triage** via `AskUserQuestion` 3-option buttons (keep / done / no-longer-needed) across all 14 items → trimmed to **7 keeps**. Done: skriv voice (later reverted — see below), interrogate smoke test, PPS marketplace fix. Dropped: slainte auto-trigger, Path B hooks, Spike retro, community contributions.

## What's Next

- **Dogfood sherpa on `~/Bree`** — in that repo, fresh session, after updating the plugin cache to 0.61.0. Decide first: mid-lifecycle pickup vs fresh feature.
- **The v0.61.0 pick-list is written-but-not-verified-live** — this session ran from the cached 0.60.0 skill. The first switch-in after the cache updates to 0.61.0 is the real test that the numbered pick-list fires as written.
- Consider promoting the question rule to global `~/.claude/CLAUDE.md` (exact phrase parked in TODO backlog).

## Key Decisions

- **Multiple-choice questions = quality bar, not ban** — the failure mode is *pre-narrowing an open space*, not "menu vs no menu"; a genuine small either/or (like this session's Form A/B render choice) clarifies rather than narrows. Enforced in the focus hook.
- **Switch-in pick-list shows the full TODO, numbered, no truncation, no reply-hint** — the user just types a number or says what they want.
- **skriv voice reverted to backlog (Keep), not Done** — the triage only offered keep/done/drop; "Done" was the closest button for what Tony actually meant ("let's do it"). The CONTEXT.md HELD decision stands unchanged.

## Commits

- (this handoff commit) feat(switch,focus): switch-in TODO pick-list + multiple-choice quality bar (v0.61.0)

## Insights

- **The triage buttons exposed a missing bucket.** keep / done / no-longer-needed can't express "pull this into now" — Tony hit it immediately (skriv). A 4th "let's do it → promote to `## Now`" option is the fix for any future interactive backlog groom.
- **Dogfooded the new rule in the same session that wrote it** — the `AskUserQuestion` 3-way triage is exactly the "multiple-choice that clarifies" the new quality bar permits: genuine, distinct, crisp, the user's call. Good confirmation the rule isn't a blanket ban.
