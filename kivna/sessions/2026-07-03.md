# Session 2026-07-03

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

- **Assessed RDF** as a candidate standard for document/data holding/sharing over markdown — **rejected**: an LLM has no query engine at load time, it pays per token of serialized text; triples run ~2× the tokens of prose for the same decision (measured on the Validate-principle example: ~19 vs ~45 tokens) and drop the causal "why". Prose is the compressed format for the LLM reader.
- **Found the real switch-in cost**: switch-out writes the same session narrative three times (TODO `## Current Session`, the session log, vault Status.md) and switch-in reads all three — ~1,700 of ~4,300 tokens at the 2026-07-03 switch-in were duplicate copies. Built an artifact diagram (before/after read sets) to make it visible.
- **Co-signed + committed the context/history-split design** (`docs/plans/2026-07-03-context-history-split.md`): state (CONTEXT.md, root, overwritten) / work (TODO.md, lean) / history (kivna/sessions/, immutable). Switch-in reads state + work + latest log only (~2.3k tokens); vault Status.md becomes write-only; older logs become pure archive. Answers Tony's Q0 (stop reading older logs) and Q1 (CONTEXT.md + TODO.md as first-class root files).
- **Ran Test 1 — A/B cold-pickup quiz: PASS.** Two isolated agents simulating a next-session pickup, one on the full read set, one on the lean set (hand-drafted CONTEXT.md + lean TODO + latest log), 12 questions, several derived adversarially from the removed content. 11/12 substantively identical, zero NOT-FOUNDs; Status.md-derived questions fully answered by the lean set.
- **The one delta exposed a pre-existing hole**: the 2026-06-28 Edit-tool gotcha was never mirrored to `docs/playbook.md` (switch-out step 5 slipped silently; only the older-log skim ever surfaced it, and that net only reaches back a session or two). Healed the instance (gotcha now in the playbook) and hardened the design: switch-out verifies this session's gotchas have playbook counterparts before committing.
- **Wrote this handoff in the lean shape by hand** (CONTEXT.md at root + lean TODO.md; skill unchanged at 0.59.0) — this is Test 2's setup.

## What's Next

- **Test 2 verdict at the next switch-in**: read ONLY `CONTEXT.md` + `TODO.md` + this log (skip vault Status, MOC, older-log skims — instruction is at the top of TODO.md). Tony judges the acceptance criterion no quiz can: does it still feel like the same session? Pass → build the split as a conductor session (3 slices: switch SKILL.md rewrite, cross-cutting grep of every TODO-shape consumer, live migration; plan in the design doc). Fail → the miss names exactly what to pull back into the lean set.
- **Then: dogfood sherpa on `~/Bree`** end-to-end (in that repo, fresh session, after the plugin cache is updated from 0.45.0) — decide first whether to pick up mid-lifecycle or take a fresh feature through the full climb.

## Key Decisions

- **RDF rejected** for Kerd's storage/handoff — see What Was Done for grounds; recorded in the design doc's origin section.
- **State/work/history split**: one kind of information per file. Completeness without degradation = full-fidelity session logs kept forever + git history archiving every pruned CONTEXT.md version; efficiency = reading less, not storing less. The sharp edge: CONTEXT.md must never become a diary.
- **Older session logs: archive, never per-session load** — safe because forward-only discipline carries live items and the playbook is the durable gotcha net (now verified at switch-out, per the Test 1 finding).
- **Validate before building**: A/B quiz first (done, pass), one manual lean switch cycle second (in flight), implementation only after both.

## Commits

- `05fd666` docs(plans): context/history split — lean switch handoff design
- `a738e7c` docs: heal unmirrored 06-28 gotcha + record Test 1 pass in split design
- (this handoff commit) chore(switch): session 2026-07-03 handoff — lean-shape Test 2 setup

## Gotchas

- **Switch-out's gotcha-mirror step (step 5) can silently slip** — the 2026-06-28 Edit-tool gotcha lived only in the session log for five days; nothing verified the playbook mirror, and once older logs stop being skimmed an unmirrored gotcha is effectively lost. Now in the playbook (with the meta-gotcha documented alongside); the split design adds a switch-out verification check.

## Insights

- **The A/B cold-pickup quiz is a cheap, reusable harness for read-set changes**: derive the questions adversarially from the content you're removing, give each set to a fresh subagent with a strict "NOT FOUND, don't guess" rule, and grade against ground truth. Minutes of work, and it names exactly which removed content wasn't a duplicate.
- **Validating the new design found a bug in the current one.** The quiz's single miss wasn't a lean-set failure — it was a five-day-old silent slip in the existing switch-out. Validation pays even when the proposal passes.
- **"Efficient but complete" dissolves once state, work, and history stop sharing files.** The RDF question was really a dedup question: the format was never the cost — the copies were.

---

# Session 2026-07-03 (2)

**Machine:** Anthonys-MacBook-Pro.local
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

- **Test 2 verdict: PASS.** Tony picked up from the lean read set (CONTEXT.md + TODO.md + latest log) and confirmed it felt like the same session. Both validation gates cleared → build approved.
- **Built the context/history split as a conductor session (v0.60.0), all four slices:**
  - Slice 1: `skills/switch/SKILL.md` rewritten end-to-end — switch-out writes CONTEXT.md (state) + lean TODO (`## Now`/`## Backlog`), self-migrating heal, gotcha-mirror verification, prompt-free vault save; switch-in reads exactly three files, no vault read, no older-log skims; modifier table rebuilt.
  - Slice 2: every consumer reconciled — conductor (plan → `## Now`, decisions → CONTEXT.md), kivna (save approval gate removed), tend (CONTEXT.md required + TODO drift check), trim (lean shape + CONTEXT.md pruning), modes/spike.md, docs/state-contract.md (new CONTEXT.md contract), docs/playbook.md living sections, README (switch/kivna/day-to-day), `hooks/session-start.sh` + `tests/hooks_test.sh`.
  - Slice 3: this repo migrated — TODO slimmed to `## Now`/`## Backlog`, CONTEXT.md reconciled to the contract (Test-2-artifact banner removed, new decisions recorded).
  - Slice 4: release checklist — 0.60.0 in all three manifest locations; capability lists unchanged (byte-identical pair preserved); switch trigger description updated.
- **Added two behaviors Tony requested at Test 2 verdict:** switch-out closure inference (done/open/unsure verdict list over open TODOs — readable list, never a prompt; "done" requires session evidence; unsure tagged `(done? — confirm)` for one switch-in question) and prompt-free `kivna save` (do-not-save markers remain the privacy control; scaffold keeps its approvals).
- **Retargeted the SessionStart hook**: last-session date now read from the newest `kivna/sessions/` filename instead of TODO's Current Session block (shape-agnostic, pre/post-split). Tests updated; 26/26 green, shellcheck-clean.
- Closed the vault-spec cold-LLM-read question (answered by the split: LLM reads CONTEXT.md, human reads the vault) and updated project memory (MEMORY.md) to the new save/read contracts.

## What's Next

- **Dogfood sherpa on `~/Bree`** — in that repo, fresh session, after updating the plugin cache to 0.60.0 (`claude plugins install kerd`). Decide first: mid-lifecycle pickup vs fresh feature.
- First natural switch-in on this repo exercises the new 3-file read path from the cache-installed 0.60.0 skill — watch that it behaves as written.

## Key Decisions

- **Closure inference is a readable list, never a prompt**: switch-out infers and shows verdicts; only unsure items become a question, asked once at next switch-in. (Tony: "as a list user can read not be asked".)
- **kivna save drops the approval gate entirely**, not just when switch calls it — switch-out is the single vault write path, a special-case flag would be dead complexity. Scaffold keeps approvals (creates from an interview; save reflects the session).
- **Conductor's plan writes into TODO `## Now`; its mid-session decisions into CONTEXT.md** — settled the design doc's open contract question, no separate conductor line needed.
- **SessionStart hook date source = newest session-log filename** — the only shape-agnostic, always-true signal; TODO no longer carries a date.

## Commits

- (this handoff commit) feat(switch)!: context/history split — CONTEXT.md/lean-TODO/immutable-logs, closure inference, prompt-free vault save (v0.60.0)

## Insights

- **The mandatory final grep caught what the plan missed, again**: `hooks/session-start.sh` grepped `## Current Session` and appeared in no plan slice. The "cross-cutting changes need final grep" pattern (playbook) has now paid out on consecutive cross-cutting changes — it is the load-bearing step, not a formality.
- **First live closure-inference run produced an honest all-open list** (2 done, 14 open, 0 unsure) — the verdict list surfaced two legitimate closures that silent TODO rewriting would have buried without evidence.
