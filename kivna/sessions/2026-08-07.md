# Session 2026-08-07 (morning sitting, 08:28–10:13 EDT) — the boundary gets fixed for code, and the real problem turns out to be human input

**Machine:** Anthonys-MacBook-Pro.local
**Branch:** main
**Tracking:** origin/main

## What Was Done

Per-task actuals:

- Frame switch-fidelity (product doc, 7 gaps) — started 09:25 (marker) · landed 09:29 (`565f165`)
- Slice 1 build — v0.90.0, four gaps closed — started 09:29 · landed 09:44 (`47b30ad`)
- Gaps 8-14, the thinking-layer diagnosis, first ruled-out record — started 09:44 · landed 10:11 (`04d561e`)

**The session started on a dead premise and Tony caught it in two turns.** The
pickup offered "boundary auto-sizing" as the top pick, straight from a TODO
row. The row had been filed 2026-08-06 against a cost complaint that
`vault-unhook` v0.83.0 had already removed. What surfaced it was the frame
gate: `gate.py route` demanded a `## Value` section, no honest one could be
written, and asking why produced Tony's "I think we fixed closing already by
removing vault?" A machine refusing on a missing declaration caught a stale
premise at turn 2 instead of turn 40.

**Tony then restated the actual requirement**, which reframed everything from a
cost problem to a fidelity one: the boundary must record exactly where we are
against the agreed stage/ladder/plan so the window can be cleared and the next
session resumes as the same session, with nothing agreed, decided or
contributed forgotten — and efficiency is a tiebreaker between designs that all
preserve that, never a reason to record or read less. That statement is now the
declared truth in `docs/product/switch-fidelity.md` `## Value`, quoted verbatim.

**Slice 1 shipped (v0.90.0).** Ladder position joins both halves of the
boundary — the steps had named `docs/project/progress.md` and `progress.md`,
neither of which ever existed here, so they silently no-opped while the derived
board went unread. "Read in full" gained a no-silent-truncation rule.
CONTEXT.md became append-only between licensed prune events. Closure inference
gained a fourth verdict, `dead`. The `light` and `low` modifiers were removed
and the loss named as a loss in the README. Switch went 400 → 353 lines while
gaining four rules, because deleting the modifiers deleted every "skip if light
or low" clause.

**Then Tony asked for the plain-English version of switch out and switch in,
and that is what broke the session open.** Writing it plainly exposed a
circularity: switch-out verifies every gotcha reaches `docs/playbook.md`, and
switch-in declares the playbook unread while citing it as the reason old logs
are skipped. Seven more gaps followed, and behind them a root cause.

**The root cause, in Tony's words: "we solve for code level but all the context
is not code — much of it is human input that we lose."** Derived-from-disk
works because code leaves artifacts that cannot lie. Human input leaves no
artifact at all; it is undeclared until a model chooses to record it, and the
model least likely to record it is one already drifting. Measured the same day:
of the seven things Tony named as unlosable, three have no populated home — 0
evaluation matrices against built and CI-enforced machinery, 0 ruled-out
artifacts despite the 2026-08-03 decision, and 27 dormant review triggers and
return conditions that nothing fires. Reading that table he said: **"all of the
x's are where I see the problem regularly — I drive input and direction and we
revisit next session."**

## What's Next

- **Frame slice 2: capture human input.** Gaps 10/11/12 first. The constraint
  from the root cause is the whole design problem — derivation cannot fix
  these, so capture has to become a declared artifact with a gate, the way
  `## Value` already is. That is the one piece of human input that reliably
  survives, and it survives only because `gate.py` refuses without it.
- **The release-planning artifact**, which interlocks with slice 2: roadmap →
  release plan → review the someday/maybe pile for what is now feasible or
  relevant → pull in → focus until shipped. A phase, never a notification.
- **The fidelity check.** Ran by hand this session because Tony asked how we
  can be sure this session survives. That manual run is the spec.
- Position: `switch-fidelity` sits at the **design** rung; contract needs
  `docs/design/switch-fidelity.md` and a design GO record.

## Key Decisions

- **Tony's rungs are frame, viability, slice and design, plus evaluation at
  goal; contract and build are the model's.** Stated directly: "I don't want to
  see the code being built." The ladder already encoded this; what changes is
  conduct — build mechanics go in commits, not into the conversation. This
  session violated it repeatedly before he said so.
- **The someday/maybe pile wakes as a phase, not a notification.** Reviewed at
  roadmap and release planning; once a release is sliced the pile is invisible
  until it ships. Same event-driven shape as the licensed-prune rule.
- **A gotcha surfaces when you are about to touch the thing it is about.** So
  the read policy is right and the justification is wrong; always-known gotchas
  belong in CLAUDE.md, area gotchas stay in the playbook and must be greppable.
- **Slice 1 skipped the design rung on Tony's call** ("as many as we can do,
  lets fill the gaps before we lose the window"), flagged before proceeding.
  First work item to do so. The goal gate is kept.
- **An external review's handoff-manifest proposal was rejected** on
  derived-from-disk: git cannot lie, a written hash rots. Two parts survived
  because they are genuinely not derivable — stashes and an explicit
  local-equals-remote check. Recorded in the product doc's `## What we ruled
  out`, which is the first instance of an artifact decided on 2026-08-03 and
  never before written.

## Commits

`565f165` frame · `b9f064f` render · `47b30ad` slice 1 v0.90.0 · `3796a5c`
render · `04d561e` gaps 8-14 and the ruled-out record. Plus this switch-out.

## Gotchas

- **A `## Risk ledger` section must be table-only.** The parser treats every
  non-blank line in the section as a row, so a closing paragraph comes back as
  "rows 5-9: expected 8 columns, found 1". Same family as the v0.83.1
  fence-awareness fix. Put commentary in its own section after the table.
- **`unmitigated` is not a legal ledger state.** The closed set is
  `countermeasure - permanent`, `countermeasure - temporary`, `accepted`,
  `accepted unknown`, `fatal` — the machine enforcing "a risk without a
  countermeasure is a BLOCKER". An honestly-unresolved risk is `accepted
  unknown` with a review trigger, not `unmitigated`.
- **Grounding references must not be wrapped in backticks.** AU5 reports "does
  not resolve" for `` `path` `` even when the path exists. Plain paths only.
- **The sitting's open time is not always the `execute` stamp.** The v0.89.0
  rule says the execute stamp is the sitting's open time; this sitting opened
  at 08:28 (orient marker) and did not reach execute until 09:25 — nearly an
  hour of framing. Using the execute stamp would have labelled a 105-minute
  sitting as 48. Both stamps are machine-produced and same-turn, so the honest
  range uses the earliest marker written this sitting, not the execute one.

## Insights

- **The frame gate caught a stale premise by refusing a missing section.** No
  cleverness involved: `## Value` was required, could not be written honestly,
  and the attempt to write it surfaced the problem. The cheapest drift detector
  in the system is a required declaration that someone must fill in.
- **Asking for the plain-English version found more than any audit did.** Four
  gaps came from tooling and measurement; seven came from describing the flow
  in ordinary words to someone who would notice if it did not make sense. The
  circularity had been in the file for weeks and every grep-based check passed
  over it.
- **The fix for gaps 1-7 was pointing at artifacts that already existed. That
  approach cannot work for gaps 10-12**, because there is nothing to point at —
  which is the difference between losing code context and losing human context,
  and the reason slice 2 is a harder problem than slice 1.
