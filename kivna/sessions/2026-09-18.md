# 2026-09-18 — Switch In stops repeating chores; 0.135.0

**Contributions captured:** Claude `kerd-b5-review` (owner — arrival, the byte-identity
reading, the 0.135.0 direction, build, release and records) + one Claude review subagent
(Opus 5 requested at `kerd:effort-high`; findings in the work record). Codex `codex-tui`
received the arrival notice only; it had no tokens until 2026-09-19. Missing: none.

## What shipped

- **`7d374ba` — Kerd 0.135.0**, pushed, entry-gate CI success.

## How the sitting turned

The 09:58 Switch In put "take the assistant-side byte-identity reading" in NOW, because
the previous Out had saved it. Anthony said yes to Conductor; the reading was taken from
this session's transcript: identical, 1,991 bytes. Then, 10:07: "i dont care about the
screen being unchnaged or not - that makes no sense. switch in need to tell me what
happens next and why". The check was dropped, and the real defect named: In repeated a
saved chore with no reason, while the product work sat behind a link.

A plain-English list of open work with one recommendation ("yes this is better", 10:08)
became the 0.135.0 direction, agreed 10:11 with a before → after view
(`docs/work/switch-in-open-work/before-after.html`), including the new closing question
"What do you want this session to move forward?".

## Two things I got wrong

- I asked permission to read the transcript, citing Conductor's "do not mine native
  transcripts". That rule is scoped "to fill the display"; it never covered this check.
  Then I invented a motive for the rule. Anthony asked "eh what is this?". Both corrected
  in the record.
- The morning's own arrival led with that chore. That is the defect 0.135.0 fixes; the
  fix is judgment, so the next arrival is its first real test.

## Review

A fresh Claude reviewer stood in for Codex: no authority holes; one real bug (a
`null`/`null` nothing-open arrival fell back to the old grid, fixed, test fails on the old
condition), stale "approval line" wording in three places, small width fixes. Detail:
`docs/work/switch-in-open-work/work.md`.

## Open at close

**Recommended next, proposed not agreed: sign off the risk-state-split migration** —
write the evidence-backed acceptance record at
`docs/gates/2026-09-03-risk-state-split-acceptance.md`. Why: it is the first step of the
launch sequence (0 of 5) and every later step waits on it; the migration shipped
2026-09-03, only the acceptance is missing. Owner Claude drafts, Anthony gives the
expert-user pass. Stops at the record; no release.

Also open: observe the first real 0.135.0 arrival (Claude's cache is 0.134.0 until the
plugin updates); 0.134.0's diagram clauses unmeasured; 0.132.0 rules beyond one scenario;
Archify's Socket alerts; `diagram-design` palette drift upstream; the `codex-plugin`
behavioural scenario; the 2026-09-13 "prove Kerd first" hold, Anthony's call.

## What this commit touched

`git show --name-only 7d374ba`.

---

# 2026-09-18 (second sitting, 10:39–15:50 EDT) — two launch steps done, both Switch screens rebuilt

## What shipped

- **`59cbd49` Kerd 0.136.0**: Switch In gets its grid back (PROJECT · PHASE ·
  NEXT · TEAM), speaks product English, drops its footer and END marker, and ends
  on "Start a Conductor session?" with a Yes — <work> / Something else picker.
- **`4ba70ca` 0.136.1**: risk-state-split accepted, ready to release (launch
  sequence step 1), with the measurement gap as an exception; T68b added.
- **`cc71cce`, `04df355`**: Conductor replaces Drive (ruling); gate-reachability
  reframed to Conductor, scoped, designed.
- **`8a72c0f` 0.137.0 + `d770f18`**: Conductor's step check in the user's own
  project. The first push was red on the stale-render trap; the render commit fixed it.
- **`4cbbeda` 0.137.1 + `7d3d8c3`**: gate-reachability accepted (launch sequence
  step 2), with the reverse-direction test repaired before the key.
- **`e83eb5d` 0.138.0**: the Switch Out screen mirrors Switch In.

CI green at every tip; `8a72c0f` alone was red.

## How the sitting turned

The first real 0.135.0 arrival (10:40) weighed the work correctly, but Anthony
found it unreadable: "nothing is being built" was agent state, the grid was gone,
and the open question could route around Conductor. Five corrections became
0.136.0, agreed on a before → after view.

The risk-state-split sign-off found the item's fifth required test half-covered;
repairing it (T68b) before the key followed the gate-visuals precedent.

Shaping gate-reachability, two proposals drilled into how `${CLAUDE_PLUGIN_ROOT}`
resolves. Anthony: "lets not get too deep into protocol level", then "i want to
focus on capability and product level then we can fine tune protocols" (saved as
a working preference). One headless probe answered the item's unknown: the
placeholder resolves in SKILL.md text only, not in reference files or the shell.
The frame named Drive, and Anthony said Drive had been dropped for Conductor;
nothing recorded that, so it was ruled and recorded (13:08).

Anthony asked for the same tightening on Switch Out (15:35), agreed 15:37 with
his closing line.

## Two things I got wrong

- **"Launch 2 of 5 done"** said several times, including in the proposed closing
  view. The launch plan's five *outcomes* are still 0 of 5; what is done is steps
  1–2 of the TODO's launch sequence, prerequisites on the critical path. Corrected
  in the records at this Out.
- **The render trap again** on `8a72c0f`, although the playbook records it. The
  ordering was right one release later (`4cbbeda` → `7d3d8c3`, one push).

## Review

Every release had a fresh Claude reviewer (Codex was not asked; it had no tokens
the day before). Findings that changed the work: the arrival question's leftover
bypass route (0.136.0); the push history in the risk-state-split record; the
missing go-ahead file and slug rule in the step check (0.137.0); the
`CLAUDE_PROJECT_DIR` fallback that let a test pass without `--root`, and the
unstated "real refusal" half of the frame (gate-reachability record).

## Open at close

**Recommended next, proposed not agreed: start the diagnostic pilot**: refresh
the Kerd plugin to 0.138.0, then create the separate `agent-request` repository
and run the first real work item there, Kerd frozen. Why: it is the first time
Kerd works for someone else's project, and launch outcome 2 is its findings.

Also open: the four fatal risk rows (launch sequence step 3, each in its own item);
removing Drive; tend/slainte naming the plugin placeholder literally; the
measurement countermeasure, now three exceptions deep; observe 0.136.0's arrival
and 0.138.0's closing box in real use.

## What this sitting touched

- **Switch In and Out screens (0.136.0, 0.138.0):** the renderer
  `skills/switch/scripts/where_we_are.py` and its tests
  `skills/switch/scripts/tests/test_where_we_are.py`; the guide
  `skills/switch/references/in-out.md`; the arrival question in
  `skills/conductor/references/journey.md` and
  `skills/conductor/scripts/tests/test_question_form.py`; the closing-box
  designation line in `skills/agent/references/session-succession.md`.
- **risk-state-split acceptance (0.136.1):** the item's stage in
  `docs/product/risk-state-split.md`; the proposed shape
  `docs/work/risk-state-split-acceptance/shape.html`.
- **gate-reachability (0.137.0–0.137.1):** the scope and design views
  `docs/work/gate-reachability/scope.html` and `design.html`; the build spec
  `docs/plans/2026-09-18-gate-reachability-spec.md`; the fixture
  `skills/conductor/scripts/tests/test_step_check.py`.

Everything else is named in `TODO.md` `## Now` or in CONTEXT.md; the full inventory
is `git log 3b02274..HEAD --name-only`.
