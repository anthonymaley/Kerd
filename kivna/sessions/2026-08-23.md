# Session 2026-08-23 (design sitting, closed 12:33 EDT)

**Machine:** Anthonys-MacBook-Pro.local
**Branch:** main
**Tracking:** origin/main (up to date)

> **No open time on this heading, and the reason is now a structural finding
> rather than an edge case.** The conductor marker read `execute @ 12:17` at
> close-out — fourteen minutes before the end — because this sitting lived almost
> entirely inside the `plan` phase: it was a design conversation carried by
> drawings, and only the last quarter-hour was execution. Handing 12:17 over as
> the open time would have labelled a four-hour sitting as fourteen minutes. The
> rule forbids substituting, so none is written. The sitting ran from roughly
> 08:44 (first message of the day) but that is not a machine-written phase stamp
> and is deliberately not promoted into the heading. Second occurrence; the
> playbook gotcha and the Backlog row were both broadened today.

## What Was Done

Switch-in at ~18:16 the previous evening, conductor orient 20:00, plan 22:40,
resumed 08:44, execute 12:17. **Almost the whole sitting was the plan phase** —
the work was deciding what to build, not building it.

**Per-task actuals, handed over by close-out:**

```
funnel-driver frame amendment   — started 12:17 (marker) · landed 12:19 (1530f61)
concerns declared, gate refuses  — started 12:19 · landed 12:20 (5f30ddc)
seal two design views            — started 12:20 · landed 12:27 (953f829)
```

- **The session opened on the `gate-visuals` goal gate and never reached it.**
  The first finding — that the goal record's `Product measurements met` row has
  no upstream declaration to read from — was drawn rather than argued, and the
  drawing turned up something larger: **five of five prior design packages carry
  a `## Named answers — the stage-1 measurements` section and `gate-visuals` is
  the first without one, while nothing in `tools/gates/` has ever checked for it**
  (`grep -i measurement tools/gates/*.py` returns nothing). The design rung's
  declared machine key has been carried by discipline alone for five items.

- **Tony corrected the altitude and the correction reframed everything.** *"i
  think we are working both ends here and its causing confusion… what we are
  building is the skil to use to build things."* Adding numbers to Kerd's own 38
  requirements is dogfood housekeeping; the capability is Kerd asking the *user*
  for the right kind of number. Counted and confirmed: **52 requirement blocks in
  the register, not one carrying a number or target**, and the need was recorded
  on 2026-08-14 (*"what carries the acceptance criterion?"*) and never built.

- **He set the guidance order, and it reorders the board:** diagrams first,
  standards-anchored names throughout, numbers last. Recorded verbatim in
  CONTEXT.md with his amended value statement.

- **A claim recorded at 09:08 was struck by its author at 09:18** — the "naming
  anchored on standards" decision had me record that Kerd owes a
  self-explaining reference artifact. He killed it: the rule is **currency, not
  conformance** (use the name people actually say; a standard confined to
  government and banking is evidence *against* using it), and the lookup is a web
  search. Struck in place with the replacement named.

- **`funnel-driver` unparked, and its mechanism changed.** His proposal: an
  **umbrella above conductor** that forces the stages and calls conductor without
  changing it, rather than graduating functions into conductor. That **retires
  the item's first killer risk by construction** — no edit to conductor, nothing
  to break — while still satisfying the opposing killer risk, because an umbrella
  that forces the stages obliges something. The frame's "Killer risk, read out"
  section says those two opposing pulls are what made the slicing hard; one move
  answered both.

- **The gate loop was designed by walking a drawing.** Tony corrected five of its
  eight steps: input includes the person's existing docs/links/pictures; the
  question set is short and does double duty (judge the idea, seed the
  requirements); the browser show-and-comment loop moves *inside* the package
  step and **is** the gate; the completeness check reads the same question list;
  and the output is **not locked**, because the idea is still shaping.

- **Two design views sealed** — `why-an-umbrella.html` (`fp:54f84887b8b8`) and
  `gate-loop.html` (`fp:47883502cf4b`), Tony 2026-08-23. Declaring the concerns
  dropped the item from *enters at design* to *enters at slice*; sealing returned
  it. **Second live firing of the view lock, and the first on an item that did
  not build it.**

- **TODO closure review** (this boundary):

```
  · open   — "The gate-visuals goal gate"            (untouched; session never reached it)
  · open   — "The four kept items from the spike"    (untouched)
  · open   — "The archaeology batches"               (untouched)
  · open   — "Retire the old register"               (untouched)
  · open   — "The suspect-link stamp"                (untouched)
  ~ amended — "conductor marker loses the open time when a session plans twice"
             (second instance today, worse; diagnosis broadened past re-entry —
              the marker holds one line so it can only report the LAST phase,
              while the open time is a property of the FIRST)
```

Nothing closed. This was a decision sitting, not a delivery sitting, and the
verdict list says so rather than manufacturing a completion.

## Artifacts produced

Named here so a pickup can reach them — the fidelity check refuses when
something produced is pointed at by nothing a later session reads.

- `docs/design/funnel-driver/why-an-umbrella.html` — why the driver sits above
  conductor rather than inside it. Sealed `fp:54f84887b8b8`.
- `docs/design/funnel-driver/why-an-umbrella.png` — the same, rendered, so a
  pickup can look at it without a browser. A render, never approved.
- `docs/design/funnel-driver/gate-loop.html` — how one gate behaves, end to end.
  Sealed `fp:47883502cf4b`.
- `docs/design/funnel-driver/gate-loop.png` — the same, rendered.
- `docs/product/funnel-driver.md` — the frame, rewritten: new mechanism section,
  the gate-behaviour section, the question-set section, killer risk 1 retired,
  slices re-cut.
- `docs/playbook.md` — the marker gotcha broadened, two new gotchas added.

## What's Next

- **`docs/design/funnel-driver.md` and its GO record** — the only two things
  between the item and contract. The frame is currently carrying design-level
  detail because that is where the conversation landed; splitting it is real work
  and was deliberately not started at the tail of a long sitting.
- **Name the umbrella.** `/kerd:product` is the standing recommendation under his
  own currency rule; `concert` refused as an invented name. His call, and it
  blocks the design doc's vocabulary.
- **Then slice 2: one gate's question set**, hand-written, used once for real.
- **Position:** `funnel-driver` sits at **design** (was design, dropped to slice
  on declaration, returned on seal). `gate-visuals` sits at **goal**, untouched
  today. `standards-grounding` is an answered spike. Nothing else moved.

## Key Decisions

Full rationale in CONTEXT.md. **The funnel driver is an umbrella ABOVE conductor,
not a graduation into it** — retires the killer risk by construction, and carries
the rule it is built under: *the umbrella may call conductor but must never
require conductor to change* · **the guidance order is diagrams → standards-
anchored names → numbers**, his priority ruling on work already on the board ·
**naming is currency, not conformance**, and Kerd builds no glossary · **the
question set is data in the user's repo, editable before starting, and
declared-never-inferred** on the 33.8% misclassification evidence already in the
register · **skipping questions withdrawn by its proposer**.

## Commits

Three, all pushed to origin/main: `1530f61` (frame amendment) · `5f30ddc`
(concerns declared, gate refuses) · `953f829` (both views sealed), plus this
boundary.

## Gotchas

- **The conductor marker can never carry a sitting's open time.** It holds one
  line, so it reports the LAST phase, while the open time is a property of the
  FIRST. A design-heavy session reproduces this whether it plans once or five
  times. The test before handing over an open time is not *"did we plan twice?"*
  but *"is the `execute` stamp plausibly when this sitting began?"*
- **`cd` inside one Bash call leaks into the next one.** Working directory
  persists across tool calls even though shell state does not; a later relative
  path resolved against the wrong root and failed naming a path nobody wrote.
- **A rendered diagram must be looked at, because nothing else checks it.** SVG
  text does not wrap and does not visibly clip — it runs past its box and out of
  the drawing, with no error anywhere. Four diagrams today produced three
  text-overflow defects and two edge-crossing defects, every one invisible until
  the PNG was opened.

## Insights

- **Prose was read backwards; the redrawn version landed first time.** The
  clearest measurement this repo has produced for its own *"design is agreed in
  diagrams, not prose"* decision. A structural risk — *conductor is the only
  thing that works, which is why opening it is dangerous* — was read by its
  reader as *conductor is broken and so is everything else*, the exact inversion.
  Redrawn: *"love the diagram, made it instantly clear."* Then again on the gate
  loop: *"again this diagram made this easy for me."* **The prose was not
  ambiguous; it was unreadable at the altitude it was written for.**
- **The producer struck his own recorded claim within ten minutes of making it.**
  Third recorded instance of him applying the supersession rule to himself, and
  the fastest. Worth noting *because the model had already written it into
  CONTEXT.md as a decision* — a recorded decision is not thereby a settled one.
- **A drawing found something arguing would not have.** The measurement-gap
  picture was drawn to explain one missing number; laying the four done-check
  rows against their sources made the *pattern* visible, which is what prompted
  counting the other five design packages and finding the checker had never
  existed. The finding came from the layout, not from the intention.
- **Declaring a concern cost the item a rung, immediately and correctly.** The
  moment `funnel-driver` declared what its design was answerable for, the gate
  moved it *backwards* from design to slice and named the two unapproved
  drawings. The machinery penalising the item that used it honestly is the
  behaviour working, not a defect — but it is worth watching whether that
  backwards move discourages declaring concerns at all.
- **Almost the entire sitting was the plan phase, and that is not a failure.**
  Four hours of framing and one quarter-hour of building, producing three commits
  and two sealed drawings. The conductor protocol assumes execute dominates —
  the marker's design encodes that assumption, and it broke here for exactly that
  reason.

---

# Session 2026-08-23 (evening design sitting, 14:14–17:25 EDT)

**Machine:** Anthonys-MacBook-Pro.local
**Branch:** main
**Tracking:** origin/main (up to date)

> **The heading's open time is the `execute` stamp handed over by close-out, and
> it understates this sitting by about 66 minutes — the THIRD instance of the
> marker defect, now measured in three sizes.** Switch-in ran at 13:08 and
> conductor's `orient` marker was written at 13:08 from a same-turn `date`; `plan`
> restamped at 13:18; `execute` at 14:14. The marker holds one line, so the
> orient stamp was overwritten twice and only the last phase survived to
> close-out. Yesterday the same defect understated a four-hour sitting as
> fourteen minutes; today it costs an hour. **No substitution was made** — the
> rule forbids it and 14:14 is the honest machine-written value that reached the
> boundary. The Backlog row already carries both candidate fixes.

## What Was Done

**Per-task actuals, handed over by close-out:**

```
funnel-driver design package  — started 14:14 (marker) · landed 14:18 (b3a6b6a)
seal the third design view    — started 14:18 · landed 17:06 (0d8b45e)
design GO record              — started 17:06 · landed 17:15 (d4c4e62)
```

Task 2's span is mostly conversation, not sealing work — the diagram request,
two renders, and the diagram-and-prose ruling all sit inside it. The line is
correct as the protocol defines start (the previous task's commit time); the
narrative is here rather than in the number.

- **`funnel-driver` cleared the design rung and hands to CONTRACT.** The oldest
  stalled item on the board — framed 2026-08-07, at design across three sittings
  — now reports `enters at: contract`. `docs/design/funnel-driver.md` is 236
  lines, living and undated; the frame shrank 512 → 481 as design-altitude
  detail moved out of it.

- **The umbrella is named DRIVE**, Tony's call, after the model offered
  `stagegate`, `product`, `gate` and `ship` under the currency rule. His
  reasoning: *"It names the active thing… It keeps the useful part of
  `funnel-driver` without the bad `funnel` baggage."* The slug stays
  `funnel-driver` because slugs name work items, not skills.

- **He refused `docs/product/` as the canonical category name, and it was the
  same error one level deeper than the folder.** *"The whole point of Drive is
  that the thing may be a product, enhancement, content plan, business plan,
  document, repair, or just a rough idea."* Canonical language is now **work
  item** / **work record** / `docs/work/<slug>.md`, with `docs/product/` named as
  current physical storage rather than the concept.

- **`project type` is SUPERSEDED and RETIRED as a field name.** Checking the
  fifteen written types before writing the design found the field was three
  things wearing one name: **eight are ordered and advance** (Ideation → Spike →
  MVP → Pilot → Beta → Full Release → Maintenance → Decommission), **seven do
  not advance into anything** (Security Review, Experiment, Hotfix, Migration,
  Platform Change, Compliance Release, Internal Tooling) — a Hotfix does not
  become a Migration — and **no entry accommodates a content plan or a business
  plan at all**. His ruling: *"Don't preserve `project type`; split it and retire
  the name."* Three axes replace it: work type, route, lifecycle position.

- **The model proposed collapsing `route` into work type and was refused.**
  *"Route is how the work enters the ladder. Work type is what kind of thing it
  is… Those are separate axes."* Recorded because the collapse was written into a
  message as an insight before he caught it.

- **A third view was drawn AT the design gate, and the GO waited on it.** Asked
  to approve, his answer was *"hmmm i need one of your diagrams, this is
  important"* — which confirmed a flag raised in the same turn, that
  `why-an-umbrella.html` was sealed before the item-versus-session formulation
  existed. `span-vs-slice.html` puts a work item's span and a session's slice on
  one grid. Sealed `fp:5adeb340c7ee` on *"great diagram. yes agree"*. **Third
  live firing of the view lock**: declaring the concern dropped the item design →
  slice, sealing returned it.

- **Diagram and prose became one deliverable** — his proposal, and he refuted the
  model's counter-proposal on the mechanics. See Key Decisions.

- **TODO closure review** (this boundary):

```
  ✓ done   — "docs/design/funnel-driver.md + its GO record"   (b3a6b6a · d4c4e62)
  ✓ done   — "Name the umbrella"                              (Drive; CONTEXT.md decision)
  · open   — "Slice 2: one gate's question set"               (unblocked, not started)
  · open   — "The gate-visuals goal gate"                     (untouched, second day)
  · open   — "Frame the measurement item"                     (untouched)
  · open   — "The four kept items from the standards spike"   (untouched)
  · open   — "The archaeology batches"                        (untouched)
  · open   — "Retire the old register"                        (untouched)
  · open   — "The suspect-link stamp"                         (untouched)
  ~ amended — "conductor marker cannot carry a sitting's open time"
             (THIRD instance; 66-minute understatement today, so the defect now
              has three measured sizes rather than two)
  + new    — "Diagram-and-prose-together: flip the default in the skills"
```

## Artifacts produced

- `docs/design/funnel-driver.md` — the Drive design package. Canonical language,
  the item/session split, what Drive is, how it calls conductor, the question
  set's home, the completeness check, named answers, open questions, the limit.
- `docs/design/funnel-driver/span-vs-slice.html` — a work item's span and a
  session's slice on one grid. Sealed `fp:5adeb340c7ee`.
- `docs/design/funnel-driver/span-vs-slice.png` — the render. Never approved.
- `docs/gates/2026-08-23-funnel-driver-design.md` — the design GO record,
  carrying a `**Clock:**` line.
- `docs/product/funnel-driver.md` — frame amended: mechanism shrunk to frame
  altitude, canonical-language section added, name recorded, two following
  slices named and measured, `stage:` flipped framed → designed.

## What's Next

- **Slice 2: one gate's question set**, hand-written and used once for real. It
  needed the design's vocabulary and now has it. No template system, no shape
  inference, no second gate.
- **The contract spec** — `docs/plans/*-funnel-driver-spec.md` is the only thing
  the contract rung is missing.
- **`gate-visuals` goal gate** — untouched for a second day.
- **Position:** `funnel-driver` sits at **contract**. `gate-visuals` sits at
  **goal**. Nothing else moved.

## Key Decisions

**The umbrella is DRIVE** (`/kerd:drive`, `skills/drive`) — and the architecture
statement that came with it is the sharpest form of the umbrella argument: Drive
owns the **work item** (idea → loop, spanning many sessions, state on disk);
conductor owns the **session** (orient → close, one sitting, state in
`.active-modes`). Two objects, two lifetimes, perpendicular — which is why the
driver could never have been a graduation into conductor. · **The canonical
language is work item / work record / `docs/work/`**, with `docs/product/` demoted
to physical storage. · **`project type` superseded and retired**, split into work
type, route and lifecycle position; the fifteen become migration evidence. ·
**Diagram and prose are ONE deliverable, produced together from one structure** —
refining the 2026-08-02 decision rather than superseding it. The model proposed
*draw first, then write*; he refuted it on mechanics — *"what would prompt you to
write the prose after the diagram?"* — and nothing reliably would, which is the
discipline failure this repo has measured at zero twice. Declining to draw costs
a `view: n/a — <reason>`, which `kit.py` already refuses without a reason, so the
change is a default flip and not new machinery.

## Commits

Five, all pushed to origin/main: `b3a6b6a` (design package + supersession) ·
`0d8b45e` (third view sealed) · `984a969` (render) · `d4c4e62` (design GO) ·
`1c1b8b4` (render), plus this boundary.

## Gotchas

- **CONTEXT.md rode along in a work commit (`b3a6b6a`), against conductor's own
  rule.** The trap is structural rather than careless: conductor tells you to
  record decisions in CONTEXT.md *during execute*, so the file is freshly edited
  and staging it alongside the work feels natural. The rule says session-state
  files never ride a work commit — switch owns them at the boundary. Caught while
  writing a later edit, not by any check; nothing machine-enforces the split.
- **The SVG text-overflow gotcha fired again, on the first render of
  `span-vs-slice`** — an orange annotation ran straight through a neighbouring
  bar, invisible in the markup and obvious in the PNG. Third consecutive session.
  Already mirrored in the playbook; this is a confirming instance, not a new one.

## Insights

- **The producer's naming refusals are running one level deeper each time, and
  each one is the same error class.** `funnel` was refused as a command name
  (sales-funnel association). Then `docs/product/` was refused as the category
  name — same mistake, one layer down, in a path that predates the conversation
  by weeks. Then `project type` fell for the same reason inside the field
  itself. **One refusal exposed two more that had been sitting on disk unnoticed**,
  which suggests naming defects cluster rather than occurring singly.
- **The model wrote a wrong insight into a message and he caught it in one line.**
  Collapsing `route` into work type was asserted confidently as a
  *"real simplification"*; it was a conflation of two axes, made in the same turn
  that diagnosed a conflation of three. **Diagnosing a defect class does not
  immunise you against committing it.**
- **A commitment specific enough to be wrong is what surfaced the hole.** The
  model proposed *draw first, then write* as a default rather than offering an
  either/or menu, and he immediately found the mechanics problem — nothing would
  trigger the prose. Had the question been *"draw-first or same-time?"*, he would
  have picked one and the trigger question would never have come up.
- **The GO record now carries WHY the approval waited**, which a bare "yes" would
  not have. That is dated, immutable evidence a later frame can cite when someone
  asks whether the diagram rule earns its cost — three measured instances in two
  days of a picture landing where accurate prose did not.
- **Checking the fifteen types before writing cost about two minutes and changed
  the session's largest decision.** The design doc would otherwise have named two
  axes and inherited the broken field silently. The check was prompted only by
  his invitation to push back — which means the finding depended on a social
  beat, not on any machinery.

---

# Session 2026-08-23 (late naming sitting, 21:04–22:11 EDT)

**Machine:** Anthonys-MacBook-Pro.local
**Branch:** main
**Tracking:** origin/main (up to date)

> **The heading's open time understates this sitting by about two hours and
> forty minutes — the FOURTH instance of the marker defect, and the largest yet.**
> Switch-in ran at ~18:01 and conductor's `orient` marker was written then; the
> item choice and the whole currency test ran inside `plan`, which restamped at
> 19:28; `execute` was not stamped until 21:04, when the frame was about to be
> written. The marker holds one line, so only the last phase survived to
> close-out. Three measured sizes existed before today (14 minutes, 66 minutes);
> this adds a fourth at ~157 minutes. **No substitution was made** — 21:04 is the
> honest machine-written value that reached the boundary. The Backlog row already
> carries both candidate fixes and is unchanged.

## What Was Done

**Per-task actuals, handed over by close-out:**

```
frame rung-vocabulary        — started 21:04 (marker) · landed 21:15 (2b4131a)
draw the ladder, restructure — started 21:15 · landed 21:38 (66afbe8)
seal the-ladder, fix matrix  — started 21:38 · landed 22:00 (c82ce33)
seal both, fix three rows    — started 22:00 · landed 22:04 (4c26254)
```

The first two-and-a-half hours were the plan phase: choosing the item, running
the currency test across all eight rungs, and the naming conversation. Only the
last hour was execution.

- **The session was redirected before it started, and the redirect was right.**
  It opened on `funnel-driver`'s contract spec — the item that "unlocks the
  most", chosen on evidence that it is the only board item with work stacked
  behind it. Before any spec was written the producer stopped it: *"i think we
  need to decide on naming first, slice in particular feels loose and
  propriotry."* That became a new work item which now blocks the spec on purpose.

- **The currency test ran across all eight rungs**, against two questions: does
  the field use this word for this thing, and does it read across all six work
  types Drive moves. Four keep (`frame`, `viability`, `design`, `build`), one
  kept and watched (`loop`), three fail.

- **The model's first framing of the defect was wrong, and he sharpened it.**
  The model reported `slice` as "not proprietary — the concept has real
  currency". True but beside the point. His correction: *"they are software-shaped
  and mean the deliverable increment, not the phase… A newcomer may know what
  'scope' means; they may not know why a business plan is being 'sliced.'"*
  Evidence fetched rather than recalled: Humanizing Work defines a vertical slice
  around **"system behavior"** and **"architectural layers"** — both software.

- **`goal` failed harder than the rung he flagged**, which neither of us
  expected. It names the target, not the phase where the target is proven hit,
  and it is the only renamed rung appearing in an immutable filename.

- **`handoff` was objected to on measured evidence and the objection was
  overruled with a better argument.** The model raised 15 incumbent uses in
  `skills/switch/SKILL.md` alone and argued an inside collision is worse than an
  outside one. His ruling inverted it into a benefit — same verb at two
  altitudes, qualified consistently, reinforces the item-vs-session split — plus
  the line the model had not weighed: *"that is cleaner than inventing a weaker
  word just to avoid reuse."*

- **Drawing the ladder found a structural defect underneath the naming one.**
  He read the first drawing and corrected it in one message: *"build and
  acceptance are not peer rungs."* `RUNGS` goes eight → seven, `loop` becomes a
  container holding build/verify/adjust, `acceptance` becomes the producer's last
  gate. Redrawing the correction then proved **the machinery already implements
  it** — old `goal` is a pure machine check, old `loop` is gated by the human key
  — so only the labels were inverted. That is what made the change cheap.

- **The matrix view was refused twice before sealing, and both refusals were
  right.** First for overclaiming a rename's cost; second for three wrong rows.
  Fixing the first of the three produced a better finding than the fix.

- **TODO closure review** (this boundary):

```
  · open   — "Slice 2: one gate's question set"          (deferred behind rung-vocabulary)
  · open   — "The gate-visuals goal gate"                (untouched, third day)
  · open   — "Frame the measurement item"                (untouched)
  · open   — "The four kept items from the spike"        (untouched)
  · open   — "The archaeology batches"                   (untouched)
  · open   — "Retire the old register"                   (untouched)
  · open   — "The suspect-link stamp"                    (untouched)
  ~ amended — "conductor marker cannot carry a sitting's open time"
             (FOURTH instance; ~157-minute understatement, the largest yet)
  + new    — "Write docs/design/rung-vocabulary.md and take the GO"  (now #1)
  + new    — "## Grounding cannot cite an external source"           (Backlog, High)
```

Nothing closed. This was a framing sitting, and the verdict list says so rather
than manufacturing a completion.

## Artifacts produced

- `docs/product/rung-vocabulary.md` — the frame. Value in his words, the
  eight-rung currency test, the settled seven-gate ladder, the qualification
  rule, a five-row risk ledger, and slice 1 as one atomic restructure.
- `docs/design/rung-vocabulary/the-ladder.html` — why loop is a container and
  acceptance is the producer's last gate. Sealed `fp:a6fe2ec1e1c3`.
- `docs/design/rung-vocabulary/the-ladder.png` — the render. Never approved.
- `docs/design/rung-vocabulary/rungs-and-artifacts.html` — what each gate checks
  and what each renamed word actually costs. Sealed `fp:33b9932399e7`.
- `docs/design/rung-vocabulary/rungs-and-artifacts.png` — the render.
- `docs/playbook.md` — one new gotcha (index-slicing duplication).
- `docs/plans/progress.*` — board refreshed; `rung-vocabulary` enters at design.

## What's Next

- **Write `docs/design/rung-vocabulary.md` and take the GO.** Three things the
  frame deliberately left for design: the `stage:` values for `handoff` and
  `acceptance` (`scoped` is settled), whether `loop` becomes `learn`, and **what
  marks an item done once `acceptance` is the last route position** — today a
  finished item reports `enters at: loop` forever, and the restructure moves that
  ambiguity rather than resolving it.
- **Then `funnel-driver`'s contract spec.** One thing was asked and answered but
  never written down: **does slice 2 build `skills/drive/SKILL.md`, or only the
  question set and its checker?** He answered yes — it builds the skill — but the
  frame still under-describes slice 2. Fix that before the spec is written.
- **Position:** `rung-vocabulary` sits at **design** (framed and walked there in
  one sitting, both views sealed). `funnel-driver` sits at **contract**, blocked
  behind it, fourth day. `gate-visuals` sits at **goal**, third day untouched.
  Nothing else moved.

## Key Decisions

Full rationale in CONTEXT.md. **The currency rule gains a second clause —
cross-work readability**, so a term with real currency in software can still
fail; `slice` is the proof · **three renames: `slice → scope`, `contract →
handoff`, `goal` folds away** · **the ladder is seven gates, not eight** — `loop`
is a container holding build/verify/adjust and `acceptance` is the producer's
last gate; **build, verify and adjust are loop internals, never route positions**,
because gates are producer-visible state transitions and execution mechanics are
not · **bare `handoff` is banned in living docs where ambiguity matters** —
session handoff is switch's, work handoff is the rung's · **a retired name is an
alias for READING, never for writing**, which is what makes a fold a retirement
rather than a synonym.

## Commits

Five, all pushed to origin/main: `2b4131a` (the frame) · `66afbe8` (ladder
restructure + both views) · `c82ce33` (seal the-ladder, correct the matrix) ·
`4c26254` (seal both, fix three rows) · `cb1285e` (board refresh), plus this
boundary.

## Gotchas

- **Slicing a file between two `.index()` anchors silently duplicates it when
  the second anchor matches earlier.** `s[:start] + new + s[end:]` is only safe
  if `end > start`, and `str.index` returns the *first* match. Editing a view,
  the anchor for "the divider below the table" found the column-header rule above
  it, so every original row was re-appended. The output was valid HTML, rendered
  without error, and showed every row printed twice on top of itself. Caught only
  by opening the PNG. Mirrored in `docs/playbook.md`.
- **`## Grounding` refuses every external URL.** AU5 resolves each reference
  against the filesystem, so a citation to a standard or an article is always a
  `problem`. Law 4 obliges learning from industry practice and the section that
  records what was read cannot hold it. Backlog row filed; workaround is to cite
  inline in the findings, where nothing checks it.

## Insights

- **The producer stopped the session's chosen work before a line was written,
  and that was worth more than the work would have been.** The item picked on
  "what unlocks the most" was correct on its own terms; starting it would have
  baked a vocabulary we were about to change into a contract spec, and into every
  consuming repo the moment Drive ships. **The cheapest moment to catch a naming
  defect is before the artifact that spreads it.**
- **The diagram rule paid off three times in one sitting, and once it beat its
  own author.** Drawing the rungs made accumulation visible and turned up that
  the three renames have wildly different costs. He then read the drawing and saw
  a structural defect prose had hidden for weeks. Redrawing his correction proved
  the machinery already implemented it. None of the three came from the list.
- **Two of the three most useful findings came from being told the drawing was
  wrong.** The overclaim ("nothing on disk · pure vocabulary" for a word that
  sits in `RUNGS`, `STAGES` and `GATE_RECORD_RE`) and the wrong section name both
  came back from review. **Fixing the second revealed something better than the
  fix**: the `scope` gate's own section is `## Risk ledger`, which contains no
  "slice" and is untouched by the rename — the heading that *is* bitten belongs to
  the design gate. The finding existed only because a wrong claim was checked.
- **"If the intent is to keep writing goal.md forever, then `goal` is not really
  folded away."** The sharpest line of the exchange, and it exposed an unstated
  assumption in the model's own countermeasure. "Retired names stay as read-only
  aliases" sounded complete and was ambiguous between reading and writing. An
  alias still being written is a synonym, and two live names for one thing is the
  exact defect the item exists to remove.
- **The model diagnosed a conflation and then committed one, again.** It reported
  `slice` as "not proprietary" — technically right, and it missed that currency
  in one field is not currency across six, which is the whole premise of the item
  it was testing. Same class as yesterday's route/work-type collapse: **diagnosing
  a defect class does not immunise you against committing it.** Second recorded
  instance in two days.
