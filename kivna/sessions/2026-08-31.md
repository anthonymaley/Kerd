# Session 2026-08-31 (requirements-success-measurement scope gate, 08:18–10:18 EDT)

**Machine:** AnthonyacStudio.attlocal.net (the Mac Studio; thin-client host, tmux over SSH from the MacBook)
**Branch:** main
**Tracking:** origin/main (up to date)

> **The heading's open side is the `execute` stamp, and it under-reports this
> sitting badly.** Switch-in ran at **16:50 EDT on 2026-08-30**; orient and plan
> ran from there, across an overnight gap, and `execute` only stamped 08:18 on
> 08-31 once the producer's rulings had landed. So a reader looking for the
> evening of 2026-08-30 finds it *here*, not in `2026-08-30.md` (which closed its
> own sitting at 15:18). **Tenth recorded instance of the conductor marker
> holding one line and therefore only ever reporting the LAST phase, while the
> open time is a property of the FIRST** — and the largest gap yet measured at
> ~15.5 hours wall clock. Written honestly rather than smoothed; the row is in
> TODO's Backlog with the running tally.

## What Was Done

**Per-task actuals:**

```
requirements-success-measurement viability -> scope
                                       — started 08:18 (marker) · landed 08:19 (332d96c)
```

**`requirements-success-measurement` passed the SCOPE gate and sits at `design`.**
`gate.py route` reports `enters at: design`; `stage:` flipped `framed → scoped`.
Rigor level **`mvp`**, declared by the producer. One file changed
(`docs/product/requirements-success-measurement.md`), `tools/` untouched.

- **The killer risk classed FATAL under the repo's own rule, and that is what
  did the work.** Row 1's impact is *the whole of* the declared value (0 of 52
  stays 0 of 52); *fatal = impact ≥ declared value at any likelihood*
  (2026-08-03) means it cannot be accepted by name and needs a real
  countermeasure. **The machine never said so** — `parse_ledger` only refuses a
  State cell that literally reads `fatal`. This was a reading of a standing rule
  applied to a row, and it is the reason the session produced work rather than a
  rubber stamp: `accepted` would have passed the gate silently.
- **The sketched countermeasure pointed at machinery that does not exist.**
  It named `rigor-level` slice 2's per-level floors — specced 2026-08-05, never
  built, `grep -i floor tools/gates/*.py` still returns nothing, cited in the
  row's own Evidence cell. The producer refused both easy exits (absorb the other
  item; or leave the claim standing) and supplied an executable manual control
  plus a return condition naming `rigor-level` slice 2, so the dependency stays
  visible as a ledger row instead of vanishing into a merged scope.
- **He then tightened the likelihood on review, and that is the sharpest edit of
  the session.** "Medium with the control" had to be written as an explicit
  producer judgment, *"because the control is agreement-based rather than
  machine-enforced. The record must not imply that Drive structurally guarantees
  compliance."* The cell now carries that sentence verbatim.
- **Row 2 → `countermeasure - permanent`**, its countermeasure depending on
  nothing unbuilt; impact sized explicitly *below* the declared value, which is
  what makes it correctly not-killer. Its review trigger read `scope gate` —
  spent on arrival — replaced with *the first design that proposes carrying a
  measurement step inside conductor's own protocol*.
- **The invented state was removed, not legalised.** `LEGAL_STATES` stays at
  five values.

**TODO closure review** (this boundary):

```
  ✓ done   — "requirements-success-measurement -> scope"   (332d96c; route reports enters at: design)
  · open   — "funnel-driver acceptance gate"               (untouched; the next large piece)
  · open   — "gate-visuals slices 2 and 3"                 (untouched)
  · open   — standards spike ×4 · archaeology · retire register · suspect-link stamp
  + new    — a product doc's stage: is never checked against the derived route (hooks-autoload overclaims by two rungs)
  + new    — Drive invented a risk-state value rather than refusing
```

## What's Next

- **Its design session, and it carries an entry condition from the producer:**
  *declare at least one `concerns:` entry BEFORE relying on the design gate.*
  Without one the gate passes **vacuously** — `parse_concerns` returns `None`
  when nothing is declared, so zero views are counted and `design pass` is
  evidence of nothing. Especially hollow on this item, which exists to make
  measurement visible and would otherwise ship a design nobody could show was
  designed.
- **The `funnel-driver` acceptance gate** remains the largest open piece —
  `94f4304..d318e9e`, 20 files / 5,349 insertions / 10 commits. Read
  `docs/gates/2026-08-30-gate-visuals-acceptance.md` first.
- **Position:** `requirements-success-measurement` at **design** ·
  `gate-visuals` at **ready-to-release** · `funnel-driver` at **acceptance** ·
  Kerd at **v0.104.0** (no bump — a work-record edit is not a skill change).

## Key Decisions

Full rationale in CONTEXT.md. **A killer risk whose countermeasure points at
unbuilt machinery gets an executable MANUAL control and `countermeasure -
temporary`** — never a claim on machinery that does not exist, and never an
absorbed dependency, because absorbing *"would merge two work items, enlarge the
smallest proof, and make this scope depend on machinery it was not framed to
build"* · **an agreement-based control must say so in the record** — *"the record
must not imply that Drive structurally guarantees compliance"* · **"unqualified"
is workflow incompleteness, not a durable risk disposition**, so `LEGAL_STATES`
stays at five · **`stage:` is validated for legality and never against the
derived route**, and it has already overclaimed by two rungs on `hooks-autoload`.

Nothing was pruned from CONTEXT.md: no acceptance record landed and no drop was
agreed, so this session was structurally not a licensed prune event.

## Commits

Three, all pushed to origin/main: `332d96c` (the scope-gate work — **CI red, see
Gotchas**) · `b3ecb68` (render refresh — CI green, `33401743815`) · this
boundary.

## Gotchas

- **A work commit that MOVES the board must be pushed together with its render
  refresh, or the tip is red.** The ship flow is *work commit → refresh → render
  commit → **one push***, and the emphasis is the last two words. `332d96c` went
  out alone and failed CI step 7 (`stale: docs/plans/progress.excalidraw`,
  `progress.svg`) for two hours until `b3ecb68` converged it. **Passing a gate
  changes a derived rung, and therefore changes the render** — that is the
  connection that was not made. Mirrored to `docs/playbook.md`; its sibling rule
  there guards the opposite direction (don't put board-moving edits *inside* the
  render commit).

## Insights

- **The verification gate passed every check it defined and still shipped a red
  tip.** `route`, `audit`, `git diff tools/` and the collateral diff read were
  all run and all green — and none of them looks at the derived render. The gate
  is only ever as wide as what you thought to check, and step 5's "did anything
  change that shouldn't have" is aimed at the *diff*, not at artifacts the change
  makes stale elsewhere. It surfaced only because close-out re-ran the renderer
  and the tree came back dirty. **The boundary caught what the step's own gate
  could not**, which is an argument for the boundary rather than a reassurance
  about the gate.
- **The fatal reading is what converted a form-filling exercise into real work.**
  Every machine check would have passed with `accepted` in that cell. What forced
  a genuine countermeasure was applying a *standing prose rule* to a row the
  machine was happy with — and that rule lives in CONTEXT.md, reachable only by
  having read it. This is the derived-from-disk principle's blind spot in
  miniature: the machine holds position perfectly and holds judgment not at all.
- **Drive's invented state never fooled the gate — it fooled a reader.**
  `State '...' not a legal value` is the identical refusal an empty cell yields,
  so the machine was never deceived. The damage was that a human opening the
  record saw a considered disposition where none existed. Worth generalising: a
  model filling a slot it cannot honestly fill costs nothing at the machine layer
  and everything at the trust layer, which is exactly why it is easy to do.

---

# Session 2026-08-31 (requirements-success-measurement design gate, 13:22–23:34 EDT)

**Machine:** AnthonyacStudio.attlocal.net (the Mac Studio; thin-client host, tmux over SSH from the MacBook)
**Branch:** main
**Tracking:** origin/main (up to date)

> Second sitting of the day; the first (08:18–10:19) is above. The producer
> chose this item over the `funnel-driver` acceptance gate on continuity —
> value, risks, MVP boundary, dependency and the acceptance-measurement lesson
> were all fresh from the morning.

## What Was Done

**Per-task actuals:**

```
declare concerns + draw the anatomy view   — started 13:22 (marker) · landed 16:35 (91b63d8)
draw lifecycle + assurance + design prose  — started 16:35 · landed 19:52 (5285666)
correct the false trace claim; redraw fork — started 19:52 · landed 20:00 (ea9cf18)
two decisions, three outcomes              — started 20:00 · landed 23:12 (4ce0753)
design GO: key, seal, record               — started 23:12 · landed 23:31 (c01e10d)
```

**`requirements-success-measurement` passed the DESIGN gate and sits at
`handoff`.** `gate.py check design` → **PASS, 11 inputs on disk**; `stage:`
`scoped → designed`. Three views keyed and sealed from final content on the
producer's key: `condition-anatomy` `fp:fefa90380fe3` · `condition-lifecycle`
`fp:0a91dbcac981` · `assurance-boundary` `fp:c9b8d06ebfb6`.

- **The entry condition is what made the PASS mean anything.** The producer
  required at least one `concerns:` entry declared *before* any view was drafted.
  Declaring three **demoted the item `design → scope`** with three named unmet
  needs. This is the first design GO in the repo issued against a gate that had
  something real to refuse — every prior one passed a gate that counts views
  against a *declared* concerns block, and could pass vacuously with none.
- **The design closes a question open since 2026-08-14.** *What carries the
  acceptance criterion?* The catalog held **two incompatible answers under one
  `slice 2` return condition**, four sections apart — deferred merged FIELDS on
  the requirement block versus a typed `verified-by` EDGE to a separate object.
  Neither had ever been built, which is why nobody noticed. The producer ruled
  for the edge.
- **Four objects, three typed edges, and the requirement block gains no field.**
  `MSC` is a new category rather than an overloaded `TST`. Evidence is a linked
  `Observed result`, never a mutating field.
- **Boundary held exactly as set:** nothing built, `tools/` untouched,
  `LEGAL_STATES` still five, `catalog.md` unedited, `rigor-level` slice 2 not
  absorbed. Kerd stays at v0.104.0.
- **The artifacts, named by path so a pickup can reach them.** The design
  package is `docs/design/requirements-success-measurement.md`. The three sealed
  views — the fingerprint covers the `.html` only — are
  `docs/design/requirements-success-measurement/condition-anatomy.html`,
  `docs/design/requirements-success-measurement/condition-lifecycle.html` and
  `docs/design/requirements-success-measurement/assurance-boundary.html`. Each
  was rendered for the producer's eye to
  `docs/design/requirements-success-measurement/condition-anatomy.png`,
  `docs/design/requirements-success-measurement/condition-lifecycle.png` and
  `docs/design/requirements-success-measurement/assurance-boundary.png`; **a
  render is never the view**. The GO record is
  `docs/gates/2026-08-31-requirements-success-measurement-design.md`.

**TODO closure review** (this boundary):

```
  ✓ done   — "requirements-success-measurement design session"  (c01e10d; route reports enters at: handoff)
  ✓ done   — "declare concerns before relying on the design gate"  (three declared, all three sealed)
  · open   — "funnel-driver acceptance gate"                    (untouched; wants its own cold-eyes session)
  · open   — "gate-visuals slices 2 and 3"                      (untouched)
  · open   — standards spike ×4 · archaeology · retire register · suspect-link stamp
  + new    — MSC owes a categories.md disposition + schema work
  + new    — reciprocal link stamping owed (kit.py:1445 verified one-way)
  + new    — where the Observed result lives is undecided
  + new    — the reading-satisfies-target comparison is unenforced
```

## What's Next

- **The work specification** — `docs/plans/<date>-requirements-success-measurement-spec.md`
  with `## Pieces` and a `**Verify:**` on every step. Composer territory, and it
  wants a fresh session.
- **The `funnel-driver` acceptance gate** remains the largest open piece.
- **Position:** `requirements-success-measurement` at **handoff** ·
  `gate-visuals` at **ready-to-release** · `funnel-driver` at **acceptance** ·
  Kerd at **v0.104.0**.

## Key Decisions

Full rationale in CONTEXT.md. **Acceptance is TWO decisions and THREE outcomes —
a linked reading proves *assessable*, never *met***, so `NOT MET` is a real
reportable outcome and a design whose only outcomes are *proven* and *couldn't
tell* cannot say no · **a requirement and its success condition are two artifacts
joined by typed edges, and the requirement block gains no field**, with `MSC` a
new category rather than an overloaded `TST` · **the suspect-link stamp protects
one direction only** and the unprotected one is the dangerous one, so reciprocal
stamping is owed · **if a checker mistakes struck text for a live claim, teach
the checker — do not rewrite history to satisfy a raw text scan**, and a design
decision favouring a replacement is not a licence to edit the thing it replaces.

Nothing was pruned from CONTEXT.md: no acceptance record landed and no drop was
agreed, so this sitting was structurally not a licensed prune event.

## Commits

Six, all pushed to origin/main, CI green at the tip (`33466512897`): `91b63d8`
(concerns + anatomy) · `5285666` (lifecycle, assurance, prose + render) ·
`ea9cf18` (false-claim correction, fork redraw) · `4ce0753` (two decisions,
three outcomes) · `c01e10d` (design GO, seals, stage flip) · `9ea01ca` (render)
· this boundary.

## Gotchas

- **A producer approval does not launder a claim.** The anatomy view was
  approved with *"edit either end and the other is flagged for re-look"* inside
  it — a false statement about `tools/gates/kit.py:1445`, where the stamp is the
  TARGET's hash stored on the SOURCE. Approval was given on a drawing whose
  sentence nobody had checked against the code, and only the producer asking
  *"verify the claim"* caught it. **Seal only after the claims in a view are
  verified, not merely after the layout is agreed** — a fingerprint certifies
  that content has not changed, never that it was ever true.
- **Alt-text is sealed content and is read by nobody.** The `<desc>` carried
  *"PROVEN holds the later reading"*, contradicting the four-object model, and
  survived every visual review because no render shows it. It would have been
  fingerprinted unread. Grep the `<desc>` against the drawing's own claims before
  sealing.

## Insights

- **Every blocking finding was a claim; not one was a visual.** Four producer
  blocks — a false trace claim, an incoherent fork order, a missing third
  outcome, a contradicting alt-text sentence — and the visual system was called
  *"clear and readable"* on the first combined review. This reproduces the
  `gate-visuals` pattern exactly: drawings land, and the sentences *about* the
  drawings are where the defects live.
- **The fork checkpoint after drawing 1 did what it was argued for.** Two of the
  three drawings inherit the structure model; agreeing it at one drawing instead
  of three is why this gate took four rounds where `gate-visuals` took six. The
  producer named it correctly as *"a dependency checkpoint, not an extra
  approval gate"*.
- **A correction that touches a model cascades into artifacts already approved,
  and asking is cheaper than assuming.** The two-decision fix created a new
  assurance question, so the *already-approved* assurance view had been silently
  omitting a row. The producer's instruction — approve #3 *"unless this new
  comparison reveals an assurance claim that it currently omits"* — is a
  re-review trigger attached to an approval, and it fired. Worth copying: an
  approval can carry its own invalidation condition.
- **The catalog contradiction is the repo's declared-contract-nobody-checks class
  at a new altitude.** Two incompatible designs for one deferred capability, in
  one file, under one return condition, undetected for weeks — because *neither
  was built*. Unbuilt deferrals are invisible to every check the repo has, and
  the one that finally read both was a design session asking what they said.
