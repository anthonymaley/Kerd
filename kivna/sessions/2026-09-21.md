# 2026-09-20 14:33 to 2026-09-21 15:1x EDT: the front page told the truth, the site went live, 0.143.0

One sitting across a night, with gaps (about 20:55–09:53 and 15:15–19:43 on the 20th). Switch
In recommended checking the rest of the front page the way the install had been checked;
Anthony answered "y". The session ran Kerd 0.142.1's text from the plugin cache.

## What shipped

**Four front-page claims corrected** (`1f8e8f5`, no version bump, on the ruling made that
morning). The rollback section pinned to `716a099`, whose manifest carries the SSH address
0.142.1 removed. Run in a throwaway profile: the install fails with `Permission denied
(publickey)` for a keyless reader, and `CLAUDE_CODE_PLUGIN_PREFER_HTTPS=1` does not rescue it,
because the source is an explicit `url`. The trial route's "nothing is installed or disabled
globally" was true as written and misleading as read: a session through `--plugin-dir`
records a usage row even when it does nothing. "All four are ordinary Markdown" was false of
the repo saying it. And the page's own guarantee, "nothing below claims more than the repo
can show", was falsified by the three above it. The trial-route sentence lived in three
places; all three were corrected.

**The site is live** at https://kerd-six.vercel.app/ (`77d084e` `vercel.json`, `8ee8464`
the README link). It had never been published anywhere. Checked in Chromium once live:
every entry point redirects home, all ten pictures load, every nav link answers.

**0.143.0: your partner can see what you are looking at** (`1c383b9`). The first issue
reported by a Kerd user other than Anthony: asked to show mockups to their Codex partner,
Claude had no route and sent them to a fresh Codex with no project context. Tested on a
throwaway Codex terminal: `codex queue` refuses image attachments outright, but a partner
given the file's absolute path opens it with its own viewer, inside or outside its folder.
Agent now says so. Guidance, not code; a wording test guards it. Live on the marketplace.

Not Kerd work, recorded because it governs how views here look: **the diagram theme is now
the Kerd look** (`site/DESIGN.md`), made the tool's default on this machine on Anthony's word.
Case in `docs/decisions.md`.

## How the sitting turned

Shape sent out two read-only jobs, a cold claim inventory (Opus, high) and the site's first
browser rendering (Sonnet, high). Both were observed on the model and effort requested, and
both had their central claim re-checked, not accepted. The inventory's headline, the
rollback pin, was confirmed by reading the old manifest; its second-ranked finding was
wrong and rejected, since it had read a mid-work risk list and stopped before the close
that settled it. The browser job's 6.96px figure was re-derived from the SVG's own viewBox.

Anthony then corrected the premise under the picture finding: the audience is desktop and
laptop, not phone. At 1440 every label clears 12px. Asked "next?", Claude checked before
recommending the six site/README contradictions and found the site had never been published
at all; publishing came first.

## Review

Codex (`codex-tui`), before-push, seven reads. It returned "not clear to push" twice, both
times correctly, both on the rollback paragraph. First: "every commit before 0.142.1" was
false, because the manifest was born HTTPS at `34d212d` and the SSH address entered at
`bc0d78e`; traced across the url's whole history before changing anything. Second: the
narrowed sentence introduced a new overstatement, "commits before `bc0d78e` are
unaffected", false for the one commit with no manifest at all; deleted rather than
qualified. It cleared the corrections, `vercel.json`, the README link (after a wording note
that was taken) and 0.143.0 on first read. It ran no installs and said so each time.

## Things I got wrong

- **Three overstatements in one paragraph**, the same failure it caught the day before. The
  mechanism was right each time; the sentence about it claimed more than the run showed.
- **The rollback fix I would have written before running it was wrong.** It would have told
  readers to set the HTTPS variable, which does nothing for an explicit `url`.
- **My first footprint check tested the wrong thing.** `plugin list` through `--plugin-dir`
  wrote nothing; it is a subcommand, not a session. Only a session writes the row.
- **I briefed the site job with "seven README pictures".** The README embeds four.
- **I recommended fixing phone-width pictures first** on a premise Anthony corrected.
- **I told Anthony a diagram had the right theme "by the session's choice, not by setup"**,
  then corrected it: every diagram on the machine defaulted to Krutho.
- **I set the Kerd look up in diagram-design 2.6.6 believing it active.** A fresh session
  read 2.6.7. The cold test caught it; a second run caught the tool's hardcoded white fill.
- **My own views broke the Kerd look**: a clay-red status colour, rounded panels,
  uppercase eyebrows, 12px labels. Left as records; new views come from the tool.
- **One question offered two options** ("keep waiting on Codex, or read the diff"), against
  the one-proposal rule.

## Seen in real use

The report shape held across many reports but strained again whenever a result and a
correction travelled together. The speech-bubble question with its capsule was answered with
a bare "y", "yes" or "sure" about a dozen times. Fan-out ran twice more (two players, then
one test agent run three times). Switch In's arrival and its "Yes" route were seen again.
Not seen: the composer, managed Conductor, a roll.

## Collaboration

Peer session `weefish-c8` sent research on showing which agent is stuck (herdr.dev) and
then verified the Claude Code hook claim; Claude re-read the doc and found one caveat the
peer missed. Parked in `TODO.md` `## Backlog` by Anthony's agreement. `work-anthony` was
told its diagram needs redrawing in the Kerd look; no reply requested.

## What this sitting touched

Four commits on `main` (`1f8e8f5`, `77d084e`, `8ee8464`, `1c383b9`) and this close;
`git show --name-only` on each gives the inventory. Sketchbooks at
`docs/work/front-page-claims/` and `docs/work/partner-images/`. A third party's screenshot
was deliberately not committed. Kept out of Git by Anthony's standing decision, on this
machine only: `docs/work/product-package/`, `docs/guide/reference-from-readme.md` and
`kerd-laptop-result.patch`.

## Open at close

**Recommended next, proposed not agreed:** fix the six places where the live site and the
README contradict each other. **His:** the launch plan, the output style, GitHub's rendering
of the README pictures, a repo homepage, the "prove Kerd first" hold, and two harmless
leftovers from the image test. Everything else is in `TODO.md` `## Now`.
