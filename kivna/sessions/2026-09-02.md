# Session 2026-09-02 (the design-gate sitting — three record types born, closed 15:21 EDT)

**Machine:** AnthonyacStudio (the Mac Studio; thin-client host, tmux over SSH from the MacBook)
**Branch:** main
**Tracking:** origin/main (up to date)

> **No conductor ran this sitting, so there is no honest open time and the
> heading carries a close time only**, per switch's rule. The work was
> producer-directed turn by turn. For orientation only, not as a derived range:
> the session's own `UserPromptSubmit` hook stamps put the first exchange at
> **07:55 EDT**, making this roughly a seven-and-a-half hour sitting.

## What Was Done

**The ladder gained three record types, and three of five scaffolding items were
closed honestly rather than papered over.** Four pushes, all CI green.

**`inline-composer` — design KEYED** (`83b1299`, board `19bc0f4`). The keyed
design in the producer's words: one work-specification artifact sized by content ·
that file may supply distinct evidence at design and handoff, the producer keying
design judgment while the machine checks build readiness · a waiver is an
exceptional deliberate skip with visible debt, not the inline default · a legacy
closure advances historical work past a routing gap while preserving the
missing-evidence gap · a forgotten gate remains blocked.

**Revision 1 was REFUSED, and the refutation is the day's best finding.** The
draft argued a score cannot satisfy design *because the composer is not the
producer*. The producer killed it: every design document here was typed by a
model, so **authorship was never the test — the key is**. He also caught that the
rejected shape made a ten-field waiver the inline default, **recreating the
item's own killer risk verbatim**. Two further blockers: the `view: n/a` refusal
was rejected (a three-way decision with different downstream consequences is
exactly what a drawing carries), and the measurements were not targets.

**The first design gate in this repo that checked something real.** Declaring a
`concerns:` block correctly **demoted the item to `scope`** until the view was
approved and sealed — the gate refusing honestly — then PASS with 7 inputs. View
sealed on the key from final content, `fp:17d01f87d0e8`. The board was
deliberately not refreshed during the dip.

**`switch-fidelity` — design rung WAIVED** (`55b753d`). Verbatim decision,
2026-08-07: *"as many as we can do, lets fill the gaps before we lose the
window."* Evidence of deliberateness is threefold and all same-day: the session
log's *"flagged before proceeding. First work item to do so. The goal gate is
kept"*, CONTEXT.md's standing decision, and the conscious retention of the goal
gate — a forgotten rung does not arrive with a decision about which other gate
survives. **The assurance lost is the item's own broken constraint:** its ledger
closed with *"the three accepted unknowns all carry the design rung as their
review trigger. None may reach the build rung unanswered"*, and slice 1 shipped
with all three unanswered. Expires when slice 2 is framed.

**`model-effort-advisory` — LEGACY CLOSURE** (`65a2265`). Two measured facts
settled that it was neither GO nor waiver: the frame and the build landed in the
**same commit** (`f4c51c0` created the product doc), so no design rung ever stood
in front of it to skip; and the exempting rule was live in the same file at the
same commit — conductor's *"No composer call, no spec file"*. Nothing was owed,
so nothing was skipped.

**Two items were NOT keyed, and reporting that was the deliverable.**
`requirements-traceability`'s design doc blocks itself — `## The design package
— BLOCKED, not yet reworked`. `shared-memory`'s 164 lines are one annotation
round plus three open questions. Neither is keyable; no package was manufactured
for either.

**The morning's scoping pass** established the shape the sitting then followed:
of five scaffolding items, three had already shipped their first slice and were
never closed on the ladder, and the genuinely unbuilt remainder across all five
is the `--root` invoke side, a status word, and two edits to conductor.

**Two artifacts published** as orientation surfaces, both dated snapshots with
nothing checking them for staleness — named as such on the pages themselves.

**The artifacts, named by path so a pickup can reach them.**

This sitting's own: the design package `docs/design/inline-composer.md`; the
work record carrying the concerns block and the seal
`docs/product/inline-composer.md`; the sealed view — the fingerprint covers the
`.html` only — `docs/design/inline-composer/inline-routes.html`, re-rendered for
the producer's eye to `docs/design/inline-composer/inline-routes.png`, and **a
render is never the view**. The three keyed records:
`docs/gates/2026-09-02-inline-composer-design.md`,
`docs/gates/waivers/2026-09-02-switch-fidelity-design.md`,
`docs/gates/closures/2026-09-02-model-effort-advisory-design.md`.

Also in this boundary's range, and read during the morning's scoping pass,
though they landed on 2026-09-01 — the range reaches back because of the
`fidelity.py` anchor defect already filed in Backlog: the work record
`docs/product/requirements-success-measurement.md`, the design package
`docs/design/requirements-success-measurement.md`, its three sealed views
`docs/design/requirements-success-measurement/condition-anatomy.html`,
`condition-lifecycle.html` and `assurance-boundary.html`, with their renders
`docs/design/requirements-success-measurement/condition-anatomy.png`,
`docs/design/requirements-success-measurement/condition-lifecycle.png` and
`docs/design/requirements-success-measurement/assurance-boundary.png`, and the
revised work specification
`docs/plans/2026-09-01-requirements-success-measurement-spec.md`.

## What's Next

- **The viability question set, as a SPIKE.** One rung, one question: can a set
  be derived from what is already decided, or does each need fresh Law 4
  research? Shape settled — per rung, work-type-neutral, six small files.
- **`inline-composer` is now at `handoff`** with no spec. Writing it is the
  natural first use of the mechanism it just defined.
- **`hooks-autoload` is the sibling legacy closure**, named in the same frame
  clause, closing the same way on its own key.
- **Two items need a record and a key, not work** — `funnel-driver` and
  `progress-html`, zero Pieces each.
- **Position:** `inline-composer` at **handoff** · `requirements-success-measurement`
  at **loop** (16 of 20 Pieces) · `gate-visuals` at **ready-to-release** ·
  `funnel-driver` and `progress-html` at **acceptance**, 0 Pieces ·
  `switch-fidelity` and `model-effort-advisory` still reporting `design` by
  design, until waiver-aware routing is built · Kerd at **v0.104.0**.

## Key Decisions

Full rationale in CONTEXT.md. **A rung leaves one of four things behind** — GO,
waiver, legacy closure, or nothing and stays blocked — and conflating them is the
failure the records exist to prevent · **the two exception types keep separate
homes**, because *"a waiver proves an obligation existed and was consciously
skipped; a legacy closure proves the obligation did not yet exist and must not be
reconstructed"* · **authorship is never the test, the producer's key is** ·
**a legacy closure closes the routing gap and never the evidence gap**, and must
never say nobody could have produced a design — they could have; the process did
not require or retain it · **do not distort a conceptual route around today's
file-glob behaviour**.

Nothing was pruned from CONTEXT.md: no acceptance record landed and no drop was
agreed, so this sitting was structurally not a licensed prune event.

## Commits

Four work commits plus this boundary, all pushed to origin/main, CI green on
each: `83b1299` (inline-composer design keyed) · `19bc0f4` (render refresh) ·
`55b753d` (switch-fidelity waiver) · `65a2265` (model-effort-advisory legacy
closure).

## Gotchas

- **A waiver or legacy-closure record has no legal filename under
  `docs/gates/`.** `AU3` requires every filename directly there to match
  `^\d{4}-\d{2}-\d{2}-<slug>-<rung>\.md$`, and the only legal suffixes are rung
  names — so landing either type there turns CI red. Both went into
  subdirectories (`waivers/`, `closures/`), which AU3's non-recursive glob
  correctly does not count as gate records. **The sibling of CONTEXT.md's
  existing gap:** gate records can only say GO; a refused gate has no dated home,
  and now neither does a waived one. Mirrored to `docs/playbook.md`.
- **Declaring a `concerns:` block demotes the item until the view is sealed.**
  Adding a concern with no approval line fails the design rung immediately, so
  the item drops a rung mid-sitting. That is the gate working — but the board
  must not be refreshed during the dip. Second trigger for the same rule the seal
  downgrade already carries. Mirrored.
- **A scripted edit that appends a `Clock:` line can leave the drafting stamp
  beside the key's.** Two `Clock:` lines landed in the waiver — 13:04 from the
  draft and 15:01 from the key — and only a read of the file's tail caught it.
  **When a record is drafted then keyed, the draft's timestamps are content to
  remove, not scaffolding that disappears.** Mirrored.

## Insights

- **The producer's one-line refusal beat the model's derived evidence twice in
  two days.** *"drive has not shipped - its not possible"* overturned a claim
  built from a board reading and a Pieces count; *"the composer is not the
  producer"* was refuted by pointing out that a model typed every design document
  in the repo. Both times the contradicting evidence was already on screen and
  the model had reasoned past it.
- **The honest record types were discovered by attempting the dishonest one.**
  The sitting began with an instruction to prepare five design packages; two of
  the five could only have been closed by backfilling GO records — which is the
  exact move that motivated `inline-composer`'s frame in August. Surfacing that
  conflict rather than executing it produced the waiver and the legacy closure.
  **The instruction was right and its sequence was wrong, and saying so early
  cost one exchange.**
- **"Blocked, and here is precisely why" is a deliverable, not a failure.** Two
  of the five could not be keyed. Manufacturing packages for them would have
  produced five approvals and two lies. The producer's own success condition —
  *"five independently reviewable design packages, not five automatic
  approvals"* — is what made reporting the blockage the correct output.
- **A gate that can refuse is worth more than a gate that passes.** Declaring the
  concerns block made `inline-composer`'s design gate demote its own item. Before
  that, the gate verified a file existed. Four of 22 items declared concerns at
  the start of the day; five do now, and the other seventeen still have a design
  gate that can be satisfied by touching a file.

---

# Session 2026-09-02 (the derivation spike, 16:44–17:00 EDT)

**Machine:** AnthonyacStudio (the Mac Studio; thin-client host, tmux over SSH from the MacBook)
**Branch:** main
**Tracking:** origin/main (up to date)

## What Was Done

**The next move on the board became visible to the machine, and then answered
its own question.** Conductor ran the full protocol; the sitting produced one
task, three commits, CI green on each.

question-set-derivation spike — started 16:44 (marker) · landed 16:57 (e556e42)

**`question-set-derivation` framed as a spike** (`5fafc19`, board `1451adf`).
The item TODO had named as the next sitting had no slug, no frame and no board
row — the exact invisibility `funnel-driver` paid three days for. `route: spike`,
so the ladder is bypassed and `## Kill-or-keep` is the only section the gate
checks. It was written with three kill criteria and **PARTIAL declared a legal
outcome before the derivation ran**, so the verdict could be read off rather
than argued afterwards.

**The derivation ran and the verdict is PARTIAL** (`e556e42`, findings at
`docs/design/question-set-derivation-findings.md`).

**Five of six questions in a candidate viability set trace to a machine-enforced
refusal in `kit.py`, cited by line.** The nine exit demands were read from the
code, not from memory: ledger header and rows (`:457`, `:469`), non-empty
Evidence (`:482`), legal State (`:486`), FATAL as a named refusal (`:488`),
conditional Countermeasure and Review trigger (`:492`, `:496`), the `## Scope`
section (`:804`) and a legal Rigor level (`:809`).

**The one that does not derive is a measured gate gap.** `Impact` and
`Likelihood` are declared in `LEDGER_COLUMNS` (`kit.py:61`) and `parse_ledger`
**never refuses an empty one** — so risk sizing is structurally optional today,
while the 2026-08-03 decision requires it. That question rests on the standing
decision alone. Found by trying to derive against the code rather than by
reading it.

**Tested against three items that genuinely passed viability** — `rigor-level`
(hollow waiving), `requirements-success-measurement` (friction/skip),
`gate-visuals` (approved-unread). The derived set surfaced the material issue in
**three of three**, with the disposition question carrying the most weight in
all three cases.

**The structural rule the whole derivation rests on was verified, not assumed.**
`tools/gates/README.md:42`: *"a rung's exit is the next rung's entry; it is the
frame gate's input, not viability's."* So viability's exit demands are what
`check_rung(slug, "scope")` adds. Confirmed live — `hooks-autoload` sits at
viability and its single blocker for scope is an illegal `State` in its ledger.

**Nothing was built.** No `kit.py` edit, no check, no AU rule, and deliberately
**no `docs/work/question-sets/viability.md`** — the candidate set lives inside
the finding as evidence, because writing it to the directory Drive reads at
intake would ship it. `git diff --stat -- tools/` returned zero at every step.

**The release close-out pass did NOT fire, and that was checked rather than
assumed** — no version field moved (`.claude-plugin/` shows 0 lines of diff
across the session) and no acceptance record landed.

**The artifacts, named by path so a pickup can reach them.**
`docs/product/question-set-derivation.md` (the frame and its declared
kill-or-keep) and `docs/design/question-set-derivation-findings.md` (the verdict,
the candidate set with citations, and the recommended mechanism).

## What's Next

- **The staleness mechanism is the real work, not the six sets.** Source-bound
  invalidation plus a scheduled discovery review, ruled and recorded, **not
  built**. It has no slug and is not framed — the same invisibility this spike
  just fixed for itself, one level up.
- **The other five rungs' sets** are now a bounded job rather than five research
  items. Each rung's exit demands enumerate from `check_rung` the same way.
- **`inline-composer` at `handoff`** with no spec — the natural first use of the
  mechanism it defined.
- **`hooks-autoload`** — the sibling legacy closure, its own key.
- **`funnel-driver` and `progress-html`** — a record and a key, 0 Pieces each.
- **Position:** `question-set-derivation` is a **SPIKE — ladder bypassed** ·
  `inline-composer` at **handoff** · `requirements-success-measurement` at
  **loop** (**4 of 20 Pieces landed, 16 remaining**) · `gate-visuals` at
  **ready-to-release** · `funnel-driver` and `progress-html` at **acceptance**,
  0 Pieces · `requirements-traceability`, `shared-memory`, `switch-fidelity`,
  `model-effort-advisory` at **design** · `hooks-autoload` at **viability** ·
  Kerd at **v0.104.0**.

## Key Decisions

Full rationale in CONTEXT.md. **A derived set is invalidated by SOURCE CHANGE,
never re-derived per use** — named sources · a derivation fingerprint over them ·
refusal when any named source moves · plus a periodic Law 4 discovery review.
The producer ruled out both alternatives by name: re-deriving per invocation
*"wastes work without solving the harder problem: an unlisted new decision would
remain invisible"*, and a date-only trigger *"is also too weak."* **The two
halves answer different failures** — invalidation catches the sources you named,
discovery catches the ones you did not — which is why it is two mechanisms and
not a choice. · **A spike's honest output may be "most of it derives, and here
is precisely what does not"**, with PARTIAL declared legal before the work ran.

Nothing was pruned from CONTEXT.md: no acceptance record landed and no drop was
agreed, so this sitting was structurally not a licensed prune event.

## Commits

Three, all pushed to origin/main, CI green on each: `5fafc19` (frame) ·
`1451adf` (render refresh) · `e556e42` (findings, verdict PARTIAL).

## Gotchas

- **`route: spike` bypasses the entire ladder, and `gate.py route` on a spike
  prints two lines that look like an error.** `kit.py:698` short-circuits every
  rung check: *"the only thing ever checked is the `Kill-or-keep` section,
  regardless of which rung was asked for."* Value, Risk ledger and killer risks
  are **not** demanded of a spike. The output is `frame pass` / `enters at:
  frame` and nothing else — no seven-rung table — which reads as truncation and
  is the bypass working. Confirm with `--json`: `"bypass": true`. Mirrored to
  `docs/playbook.md`.
- **CONTEXT.md's lines are long enough that a 50-line slice exceeds the tool
  output cap.** 237 lines carrying 185KB — a mean of ~800 bytes per line, with
  the longest at 5,128. Reading it in full at switch-in needs slices of roughly
  12–25 lines; a `sed -n '1,60p'` gets persisted to a file instead of returned.
  **The failure this causes is the silent one switch already warns about:**
  reading one chunk and reporting the file as read looks identical from the
  outside. Mirrored.

## Insights

- **The spike answered its own framing question by being framed.** TODO named
  "the viability question set" as the next sitting; the frame forced the slug to
  name the *question* instead — `question-set-derivation` — and that reframing is
  what made the answer generalise past viability. The deliverable's name and the
  work item's name were not the same thing, and only writing the frame surfaced
  it.
- **The interesting finding came from the question that failed to derive.**
  Five questions traced cleanly and taught nothing. The sixth would not trace,
  and chasing why produced a measured gate gap — `Impact` and `Likelihood`
  unrefused — that nobody had noticed in a ledger format used by 22 items. **A
  derivation is a search for gaps wearing the costume of a construction.**
- **Declaring PARTIAL legal before starting removed the pressure to round.**
  With only pass/fail available, five-of-six invites rounding up to "derivable"
  and the unrefused-columns gap disappears into a footnote. The third outcome
  was declared in the frame at 16:47, before any evidence existed to be
  flattered by it.
- **An untestable criterion is a result, not an omission.** Kill criterion 3
  asked whether a derived question would wave through an item that should have
  been stopped. No item on the board records such a case, so it is reported as
  *searched and not found* — never as satisfied. The temptation to quietly count
  three-of-three on the two testable criteria is exactly how a spike's reach gets
  over-read six weeks later.
