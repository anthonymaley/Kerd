# Session 2026-08-04

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

*Continuation of the 2026-08-03 evening session across midnight — the same
conductor run walked two more rungs. The vertical spine is complete.*

## What Was Done

**Walked and reviewed the CONTRACT rung** (`Write the contract · Size and
assign`). The design package arrives INTACT — the contract is written from
upstream truth, never a digest. **Two-tier access by role:** the overseer holds
all upstream truth; a builder gets the exact spec for their piece plus access
to related materials, and no more. The turn of the interview: **no human gate
at contract time** — Tony: *"i dont need to approve contract if we can measure
it meets output of other stages"* — provided every piece is measurable against
an upstream declaration; an unmeasurable piece cannot pass by assertion and is
a push-back to design (DONE-assembled applied to the contract itself). And the
**escalation contract**: he hears only of a gap no agent role can answer that
is a blocker; otherwise the next report is goal achieved — stricter than
today's surface-before-correcting behaviour. The board's CONTRACT row was also
de-tooled: it said "orchestrator" and "spec file", the exact
solution-vocabulary trap flagged earlier in the day.

**Walked and reviewed the BUILD rung — it consolidated 4→2 plus a rung-wide
property**, the same shape DESIGN's collapse took. *Build a piece · Prove it*:
done = measured against ALL RELEVANT specs and measurements — the piece's own
criteria plus everything its change touches, relevance scoped by the overseer;
"relevant" covers collateral by construction (the swallowed-helpers class shows
up because neighbouring terrain is relevant). *Prove the whole · Goal gate*
absorbs *Review unanchored* and *Verify against what we said*: cold eyes on
the whole change — seeing only the work order and the change, verdict can
BLOCK — once per goal plus on-demand for risky pieces; per-layer conformance
(code, logic, architecture, pixel vs design, product measurements — never one
verdict); then the human key, **the expert-user pass**: Tony uses the output
itself — *"im checking it as the expert user."* *Refuse bad work* stopped
being a function and became the property: **every gate on the rung must block
from outside the model; advisory output is not a check.** CI stays in the MVP
sequence as the property's first concrete instance.

**The cold-review scoping was my recommendation, adopted.** Tony asked
"thoughts?" instead of answering; the recommendation — once per goal plus
on-demand, never routinely per piece — carried because it rested on a flaw-class
distinction: measurement catches violations of what was declared; cold eyes
catch gaps in the declared truth itself, and those live at assembly.

**Walk state: 9 of 25, four rungs complete, everything reviewed both-keys.**

## What's Next

- **SESSION rung, 4 functions:** open/close · route to the altitude · drive to
  done · keep context optimal. Then CROSS-CUTTING (5 open). Expect
  consolidation — every drafted rung has shrunk under interview.
- Debt unchanged: `Frame the intent` acceptance checklists; the `reachable`
  clause; accepted risks age with nothing to bring them back.
- Backlog note: CI on `~/3of3` is now load-bearing twice over — it gates
  `/loop` AND it is the BUILD property's first instance.

## Key Decisions

- **No human gate at contract time** — provided every piece is measurable
  against an upstream declaration. Unmeasurable = push-back to design.
- **Two-tier access by role:** overseer holds everything; builder gets exact
  spec + related materials, no more.
- **Escalation contract:** the human hears only role-unanswerable blockers;
  otherwise goal achieved.
- **BUILD is two functions + a property.** Cold eyes once per goal (+
  on-demand); per-layer conformance; the expert-user pass is the human key.
- **A check that cannot block from outside the model is not a check.**

## Commits

`840a555` CONTRACT capture · `c0c0b55` CONTRACT reviewed · `c109008` BUILD
capture · `c81bccd` BUILD reviewed. Plus the switch-out commit.

## Insights

- **Every rung so far shrank under interview** — DESIGN 4→1, BUILD 4→2+property,
  and the survivors are consistently *his* outcome shapes, not the drafted
  decompositions. The draft over-splits because it mirrors tools and
  mechanisms; the interview merges because Tony thinks in outcomes. The
  remaining rungs (SESSION, CROSS-CUTTING) are served by Kerd's own skills —
  the over-split pull will be strongest there.
- **Tony's keys concentrate where his judgment is unique and vanish where
  measurement suffices.** Drawings (design GO), the output as expert user
  (goal gate) — but no gate on the contract, no key per piece. The pattern is
  experiential keys, documentary machine checks. Any future gate proposal that
  asks him to read a document should be suspect.
- **The escalation contract makes the refusal property load-bearing:** the
  human can only afford to hear nothing if nothing bad can pass silently.
  Written as a dependency on the diagram — the two requirements justify each
  other, which is exactly the relationship-between-functions class of finding
  that the full shallow pass was chosen to surface.

---

# Session 2026-08-04 (evening) — THE WALK IS COMPLETE

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

**Walked the SESSION rung — it consolidated 4→1 + a rung-wide property**,
the same collapse DESIGN and BUILD took. *Drive to done* is the sole
survivor. *Open/close · Keep tempo · Hold state* became the property: state
lives in the DECLARED ARTIFACTS, never in the session; anything worth
keeping is written the moment it exists; a session may die at any instant
and the loss is bounded to the in-flight piece, redone from its spec. The
turn of that interview was Tony's counter-question "why would sessions end
mid flight" — the boundary machinery exists for a conversation-shaped model
of work that the target system doesn't have. *Route to the altitude*
dissolved into the ENTRY GATES in series (work enters at the lowest rung
whose declared inputs all exist; the SPIKE is the one licensed bypass —
"mvp needs measurable results and a spec that can be reliably built
against"). *Keep context optimal* dissolved by construction — exact slice
per piece + free restarts made the missing degradation signal unnecessary.

**Walked ALL of CROSS-CUTTING in one pass** — the entry gate (thin gate
riding the ladder), Show where we are (push at every stage close + pull
anytime; LIVENESS at piece granularity — "confirmation of activity not
hang", not a static 'working…' line), Size work to a model (the declaration
that makes the ladder's "lowest role" computable; wrong sizes invisible to
the human), external tools (a tool is staffed like a PLAYER — bounded
contract, returns to the caller, killable), and How we talk (four rules:
decision questions carry what/why/gap/win/lose in the simplest terms; GROUND
before asking; never assume Tony's position; straw-man yourself first).

**Two properties promoted system-wide**: the ROLE LADDER (three independent
callers — contract escalation, loop questions, gate refusals; every
blocker/question/refusal rides one ladder, human last) and
STATE-IN-DECLARED-ARTIFACTS. **The reachable clause CLOSED**: an artifact is
reachable when at least one function's declared grounding names it,
gate-enforced — lost is now a checkable state.

Both new flows (`2026-08-04-session-flow`, `2026-08-04-crosscutting-flow`)
were generated, driven onto the live canvas, and two-key reviewed with
nothing to annotate. MVP keystone reworded on the board: entry gates, not a
dispatcher. One `(?)` remains on the entire board.

**TODO closure this session:** "Settle the reachable clause" closed
(commit 0046fb2); the walk item in ## Now completed and replaced with the
post-walk phase.

## What's Next

- **The post-walk phase: define how the skills must change to support the
  agreed requirements** (Tony's standing instruction: challenge everything
  current tooling does; findings logged during the walk, fixes designed
  after it). First logged finding: close-out-deferred capture — switch's own
  reflection step — violates the state-in-artifacts property.
- MVP sequence stands: entry gates (keystone) · CI/refusal (first property
  instance) · the SPIKE.
- Debt: `Frame the intent` route-specific acceptance checklists; accepted
  risks age; one `(?)` (Design: machine-checkable interface values).

## Key Decisions

- SESSION 4→1 + property; sessions end BETWEEN pieces, the cut chosen.
- Routing dissolved into the gates; SPIKE = the one declared bypass;
  spike output re-enters via the gates.
- The role ladder is system-wide; the human is its last rung; while an
  answer waits on the human nothing is built that the answer could
  invalidate (park vs stop = driving role's call).
- Show where we are: push at stage close/task end + pull anytime; the push
  is a report, never an ask; liveness at piece granularity derived from disk.
- A tool is staffed like a player; kill authority over rogue tasks.
- Four talk rules; facts never asked, positions never guessed.
- Reachable = named in ≥1 function's grounding, gate-enforced.

## Commits

`1079a68` SESSION walked · `64b63a8` SESSION reviewed · `0046fb2`
CROSS-CUTTING walked, walk complete · `a7ba20a` CROSS-CUTTING reviewed.
Plus the switch-out commit.

## Gotchas

- `navigator.clipboard.writeText` fails with NotAllowedError whenever
  Chrome isn't the OS-focused app — from a terminal-driven session that is
  always. `pbcopy` + trusted `cmd+v` through the extension is the path.
  (Mirrored to playbook.)
- A status marker detected by substring is asserted by any prose quoting
  it: "was (?), resolved" re-flagged three settled rows. (Mirrored to
  playbook.)

## Insights

- **The consolidation pattern held to the last rung**: 25 drafted functions
  became 15 walked/agreed, and the two biggest outputs of the final day were
  PROPERTIES, not functions. The draft over-split every rung it drafted;
  Tony's shapes are outcomes plus properties. Any future decomposition
  Claude drafts should be held loosely — the interview is where the real
  shape lives.
- **The ladder found itself**: contract escalation, loop questions, and gate
  refusals were interviewed on different days with different vocabulary and
  produced the same structure three times. That convergence — not any single
  answer — is the strongest evidence the requirement is real.
- **The walk indicted its own tooling**: switch defers gotcha/reflection
  capture to close-out, which is exactly the loss window the
  state-in-artifacts property forbids. Logged, not fixed — the post-walk
  phase owns it. (This very session log is written at the boundary it
  criticises.)

---

# Session 2026-08-04 (late evening) — the worked example and the six decisions

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

**Drew the Celtic Ticket Exchange worked example** — the whole agreed flow,
spark to shipped, ten steps on the canvas with skills/tools called out per
stage and four red deeper-consideration boxes (the value in units · UK
resale law as a survival-condition risk · the loop may not run until
something refuses · the expert-user pass scenario). Tony annotated in two
rounds — nine questions total — each read back, answered, logged in
`annotations/log.md`, and deleted from the canvas (the queue held).

**The nine questions became the post-walk phase's agenda, and all six
resulting decisions were taken in one sitting**
(`docs/plans/2026-08-04-post-walk-tooling.md`):

1. Sensei: the story grammar extracted as the system talk-format library
   (nine layouts, each already carrying its used-when trigger), three tiers
   by weight — layouts for everyday visuals · /sensei:story for documents an
   audience reads cold · /sensei:work for problems. Reference exemplar: the
   2013 API Gateway proposal (one page, current drawn, ideal drawn, scored
   evaluation, costs, roadmap).
2. Superpowers: BUILD our own approaches capability; the Technical
   Evaluation Matrix (FSS eval xls, de-spreadsheeted) is THE comparison
   standard — its ○/△/× legend IS the walk's risk vocabulary (circle = meets ·
   triangle = meets only with a countermeasure · cross = cannot meet, none).
3. Interrogate: modified into the tiered risk ledger.
4. Sherpa: CUT — the lifecycle IS the ladder; return condition named. The
   SPIKE dissolved with it (routing-to-skills died with function 16).
5. Ratified: top tier default at FRAME · progress renderer grows from the
   diagram toolkit · docs derived · domain tools per the tool rule.
6. Conductor: becomes the DRIVING ROLE (rung two of the ladder); its
   protocol is the seedbed the functions graduate from. No-rip transition.

**TODO closure this session:** the nine-questions agenda item closed
(7c90743); the SPIKE backlog item removed (dissolved by decision 4).

## What's Next

- **Turn the six decisions into specs** — the design work for the new skill
  layer: the format library into How-we-talk · approaches capability +
  evaluation matrix · risk ledger · conductor's split · the sherpa cut
  (executed only when rips are licensed).
- MVP sequence: entry gates (keystone) · CI/refusal · progress renderer.
- `Sensei Input/` triage decision at this boundary (see switch-out).

## Key Decisions

All six above, recorded in full in the phase doc and CONTEXT.md.

## Commits

`b76dad2` Celtic worked example · `1e77759` three more dispositions +
viewport gotcha · `7c90743` the six post-walk decisions. Plus this
switch-out commit. (`0046fb2`/`a7ba20a` closed the walk earlier today.)

## Gotchas

- Clipboard writeText needs OS focus; substring-detected markers are
  asserted by quoting them; canvas coordinates go stale when the human
  moves the camera — all three mirrored to the playbook earlier today.

## Insights

- **The grounding corpus did the deciding.** Every decision sharpened the
  moment a real artifact was read — the layouts carry their own route
  declarations, the eval matrix carries the risk vocabulary, the API
  Gateway paper carries the one-page bar. Tony's 2013 practice and the
  walk's 2026 requirements agree to a degree that suggests the walk
  recovered his working method rather than inventing one.
- **The worked example earned its cost immediately**: nine questions in two
  annotation rounds, six decisions from nine questions, one sitting. A
  concrete cargo (Celtic) surfaced questions the abstract board never did.

---

# Session 2026-08-04 (night) — the six specs

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

**Wrote and reviewed the entire spec package — all six, though the plan
promised two.** Each drafted, passed Tony's key, committed individually:

1. `docs/design/talk-formats.md` — eight formats with native used-when
   triggers, the three-tier weight rule, system-moment mappings,
   machine-checkable conformance. First citizen of docs/design/.
2. `docs/design/design-instrument.md` — independent approaches (shared
   riskiest assumption = one approach) + the evaluation matrix with the
   Toyota marks and the scoring discipline. Tony's two live corrections:
   scoring is KEY, and weighting is an OPT-IN mechanism — marks always ·
   scores when close · weights when criteria differ.
3. `docs/design/risk-ledger.md` — five states, fatal-by-impact-alone,
   killer-first, spike as instrument; NEW: review triggers on accepted
   states, closing the accepted-risks-age debt carried since the walk began.
4. `docs/design/progress-view.md` — two clocks (liveness per piece
   boundary, position per stage close); derived from disk, never
   self-reported — a hung model produces no commits and the strip shows it.
5. `docs/design/entry-gates.md` — the gate table (eight gates, exact
   on-disk requirements, the loop's gate demands a live refusal instance);
   grounding-was-read as the gate's second check; the keystone doubles as
   the FIRST REFUSER (files + front matter + sections = trivially CI-able).
6. `docs/design/conductor-role.md` — the seat at rung two; the graduation
   map (nine protocol pieces, destination functions, concrete release
   triggers); interactive tempo permanent.

**TODO closure:** "turn the six decisions into specs" — done (commits
4577208 · 2ef635b/a3b8f64 · 929a784 · 6aadf82 · f008861 · 9ea19fe).

## What's Next

- **Build the entry gates** — router + first refuser in one piece.
- Then: CI/refusal wider · the progress renderer.
- Rips (sherpa cut, superpowers un-routing) wait for the design-approval
  license.

## Key Decisions

Recorded in CONTEXT.md ("Rules born in the specs"): evaluation mechanisms
layer in; pre-declared weights; scores never overrule marks; review
triggers on accepted risks; progress derived from disk; gates check
grounding-was-read; interactive tempo permanent.

## Commits

Six spec commits (above) plus this switch-out commit.

## Insights

- **The specs wrote themselves from the corpus + the walk** — every section
  traces to an agreed requirement or a real artifact of Tony's. Where a
  spec added something new (review triggers, pre-declared weights), it was
  the walk's own machinery applied one step further, and Tony passed each
  without rework. Requirements agreed at the right altitude make design
  fast.
- **Two live corrections mid-review both went the same direction**:
  mechanisms should layer in as needed, never be mandatory ceremony. That
  instinct — rigor rises, ceremony stays low — is now visible in four
  places (sensei tiers, ledger tiers, matrix mechanisms, sherpa's old
  principle) and should be treated as a design invariant.

---

# Session 2026-08-04 (late night) — the entry gates ship; the four roles run live

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

**Built and shipped the entry gates — the router and the system's first
refuser** (commit `884d9c4`, v0.69.0). `tools/gates/kit.py` (front-matter
parser, the eight-gate table, cumulative check_rung, routing, four audit
rules, a 12-case fixture selftest), `gate.py` (route/check/audit/selftest
CLI, `--json` seam for the progress view), the canonical schema README,
and `.github/workflows/gate.yml` — **Kerd's first CI**. The first
entry-gate Actions run completed green in 7s on the pushed SHA. The
refusal was demonstrated on the real tree before push: a planted
`docs/design/2099-01-01-canary.md` made audit exit 1 naming it; removal
returned clean.

**This was the first live end-to-end run of the four-role delegation.**
Orchestrator (Fable subagent) called in two passes — scoping returned a
bounded 9-file list; pass 2 wrote the ~290-line spec straight to disk
(`docs/plans/2026-08-04-entry-gates-spec.md`) and returned only a step
list + gate risks. Six delegated steps played by sonnet/haiku players,
two keeps (diff review, ship). One player deviation: kit's viability
block short-circuited on a missing file (1 need line where the score
demanded 3 enumerated). The keep review classified it player-lane,
one re-dispatch fixed it — the score was never touched.

**TODO closure:** the entry-gates ## Now item — done (evidence above);
## Now rewritten to the next MVP items.

## What's Next

- **CI/refusal wider** (the `~/3of3` backlog item is now unblocked in
  kind — the pattern exists) · **progress renderer** (grows from
  `tools/diagram/`, consumes `gate.py --json`).
- Deferred from this piece, explicitly: grounding-was-read (spec A8
  sketches the landing site — `grounding` slot per rung + read-receipts
  on the mark_reviewed precedent).
- Rips (sherpa cut etc.) still wait for the design-approval license.

## Key Decisions

- **The ladder vocabulary locked in, Tony-approved:** `route: new |
  problem | spike` · `stage: framed…done` (rung past-tensed) · rung slugs
  `frame viability slice design contract build goal loop`. He questioned
  `slice`; kept for one-vocabulary consistency with the board's "Slice a
  release" (renaming in the tool would fork the walk's language).
- **Declared simplifications ride with it:** goal gate counts checked
  boxes (until the progress view cross-checks git); grounding-was-read
  deferred rather than invented ahead of its design.
- Front matter required on `docs/product/*` + `docs/gates/*`, validated
  wherever present; the six `docs/design/` specs get NO retrofit.

## Commits

`884d9c4` Entry gates: the router and the first refuser (tools/gates +
CI) — 9 files, work commit pushed mid-session at the verification gate.
Plus this switch-out commit.

## Insights

- **The cross-step verify line was the only independent check.** The
  step-1 player wrote both the code and its selftest assertions, so the
  selftest passed with the deviation in place; what caught it was step
  2's verify text in the score ("3 missing") — written by the
  orchestrator, executed by a different player, judged by the conductor.
  Spec-level verify lines that cross step boundaries are load-bearing;
  same-author tests are corroboration, not verification.
- **Re-dispatch-never-re-specify held under real pressure and was
  cheap:** classify (player vs score), cite the spec line, one targeted
  re-dispatch. The score survived the whole build untouched.
- **The two-pass orchestrator call did what it promised:** the scoping
  answer stayed bounded (9 named files, two declined for size), and
  writing the spec to disk kept it out of session context until the
  conductor chose to read it for review.

---

# Session 2026-08-04 (second evening/night) — five releases: the tooling layer executes

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

**Five releases, 0.70.0 → 0.74.0, each a four-role build with its own spec,
each CI-verified on the pushed SHA, each landed in the progress view.**

1. **0.70.0 — the release-rules refuser.** `gate.py release` (R1 version
   sync · R2 capability-list identity · R3 `kerd:` namespace on a
   living-files allowlist) as a third CI step; refusal demonstrated both
   ways before push (planted version drift and a planted bare reference
   each exited 1). Five live namespace violations found and fixed; two of
   the five were MENTIONS (docs quoting the bad form as illustration) —
   reworded to the `/<skill>` placeholder rather than adding a waiver
   mechanism. Session clarification that reshaped the task: **3of3 is not
   part of Kerd** — "CI/refusal wider" meant wider IN KERD.

2. **0.71.0/.1 — the progress renderer.** `tools/diagram/progress.py` +
   `progress_kit.py`: one derived model, three surfaces (table · `--json` ·
   Excalidraw/SVG pair), 10 fixtures in real temp git trees, fourth CI
   step. The `Piece: <slug>/<n>` commit-trailer convention went live in its
   own shipping commit. **CI refused the first ship** — the score had
   pinned REPO to an absolute Mac path; all five local verifies passed
   because they ran from that path; GitHub's runner didn't. Corrected via
   orchestrator callback (never by the conductor editing the score), fixed
   as 0.71.1, root-independence proven from a copied checkout.

3. **0.72.0 — interrogate becomes the tiered risk ledger.** Licensed only
   after an evidence check Tony demanded ("lets not just trust its the
   right thing"): engine verbatim-fit, output replaced (rows/eight
   columns/five states, killer-first, FATAL by impact alone in
   declared-value units), tiering answers the overkill worry structurally.
   Tony's composer amendment at the gate — **no line survives if it is
   wrong for this or not needed** — produced four reported cuts of
   readiness-era text and one closed spec gap.

4. **0.73.0 — capturerequirements cut (the first true rip).** Evidence: a
   five-row coverage table (every captured field has a gate-checked home);
   its dated-snapshot output violated the date-split rule; zero usage
   ever. jit's Reqs step re-points at the frame flow — routes over tools.
   What's New names the cut as a loss.

5. **0.74.0 — sherpa cut (the second rip).** The lifecycle IS the ladder,
   executed: stages→rungs, exit tests→gate checks, jump-back→gate
   re-entry + FATAL rows, expedition log→derived position (kivna/sherpa.md
   never existed on disk). Return condition recorded in What's New: a need
   the gates + progress view cannot answer.

**The graveyard queue** (Tony: "be careful of the graveyard of skills we
built and never used") is down to one verdict: mode + the modes/ files.

**Method alignment settled mid-session:** design-first, then fit tools —
four outcomes (adopt whole / transform / cut / build own), the requirement
fixed, the tool variable. And: **dead solutions stay dead** unless a named
return condition fires.

## What's Next

- **The last graveyard verdict: mode + the eleven modes/ files** —
  per-piece evidence check, then execute.
- Remaining design-layer builds: talk-formats wiring · approaches
  capability + evaluation matrix · conductor graduation map ·
  grounding-was-read · push wiring for the progress view.
- Repin the plugin cache (still kerd/0.68.0, several versions behind).

## Key Decisions

- Per-piece evidence check before each transformation replaces any
  whole-package design license (Tony's working pattern, adopted as
  default).
- The composer amendment: keep-verbatim is conditional — a kept line must
  earn its place or die, every cut reported. Now precedent for all
  rewrites.
- R3 quoting convention: living docs illustrate the bare form with the
  `/<skill>` placeholder — no waiver mechanism.
- The `Piece:` trailer convention; a ship step's trailer lands in the
  follow-up commit (it cannot trailer its own commit).
- Counts stayed at Eleven through the sherpa cut — the orchestrator
  refused the conductor's "Ten" directive with four confirming
  observations (sherpa was never counted).

## Commits

`1693fee` release rules (0.70.0) · `5e8fb68` progress renderer (0.71.0) ·
`01398ef` REPO fix after CI refusal (0.71.1) · `2268936` ci-refusal boxes
· `d4e403d` risk ledger (0.72.0) · `b46fc4e` capturerequirements cut
(0.73.0) · `6743874` sherpa cut (0.74.0) · four render-refresh commits
(`54384cf` `b1f46f1` `0539022` `fdcb44d`, the last two carrying the cuts'
ship-piece trailers). Plus this switch-out commit.

## Gotchas

- `gh run list --limit 1` immediately after a push can return the
  PREVIOUS commit's run — it completed suspiciously fast and its step
  list was one short. Always verify the run's headSha against the pushed
  SHA before watching. (Mirrored to playbook.)
- A file "already in context" can be a STALE SNAPSHOT: the cut-spec
  orchestrator quoted CLAUDE.md's line 3 as it stood at session start,
  not after this session's own 0.72.0 edit to it. Spec old-strings must
  be validated against disk at execution time. (Mirrored to playbook.)
- `git rm -r` STAGES the deletion — the ship step's `git add <deleted
  path>` then fails ("pathspec did not match"). Stage everything else by
  name; the deletion is already in the index. (Mirrored to playbook.)
- A player died mid-step on an API error with plant/revert edits pending
  — the tree held no residue (state-in-artifacts bounded the loss), but
  CHECK THE TREE before re-dispatching a step that mutates uncommitted
  files. (Mirrored to playbook.)

## Insights

- **The refusers bit their own builder, correctly, twice.** The release
  sweep flagged five violations in the very session that built it, and CI
  refused the renderer's first ship over a bug five local verifies
  couldn't see (they all ran from the path the bug depended on). The
  BUILD property ("a check that cannot block from outside the model is
  not a check") earned its keep on day one.
- **The roles now guard each other in both directions.** Down-chain:
  keep-reviews caught player deviations (nonsense meta-doc edits, a
  README "bare /kerd:" inversion). Up-chain: the orchestrator refused the
  conductor's wrong count directive with cited evidence, and corrected
  the conductor's misattribution of its own reading list. The contract
  (re-dispatch never re-specify; callback for score defects) held under
  five builds including two failures.
- **Evidence checks before transformation are cheap and decisive** — the
  ledger check (~10 min) surfaced the mention-vs-use class, the
  capturerequirements table made the cut unarguable, the sherpa check
  found the skill was mostly skeletons. None of the three verdicts
  required trust.
- **The mention-vs-use distinction recurs**: substring markers asserted
  by quoting prose (yesterday), R3 hits on illustrative examples (today).
  Any checker that detects by pattern will be asserted by documentation
  OF the pattern; the placeholder convention is the general fix.

---

# Session 2026-08-04 (third evening) — three releases and the ladder's first passenger

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

**Three releases (0.75.0 → 0.77.0) plus the ladder's first real work item,
every push CI-green and headSha-verified.**

1. **0.75.0 — the mode cut, the third rip; the graveyard queue is CLOSED.**
   Evidence check first (usage never; routing died with the gates; phase
   menus violate the talk rules; the step tracker was self-reported
   position; all eleven modes were Tony's — zero community modes in 4.5
   months). Four-role build, 24 files, 13 deletions. Three score defects,
   all caught by machinery: a verify grep found a third "community mode"
   occurrence; the step-9 sweep found scripts/demo-mode.sh (deleted — its
   subject died) and a stale hook comment (amended); the diff read found
   AGENTS.md is GITIGNORED — its edits are machine-local and unshippable.
   All fixed by orchestrator callback; the score never edited mid-flight.

2. **0.76.0 — talk-formats wiring, lean/inline.** Conductor's four live
   talk moments now name their format: decision gates speak Proposal (loss
   named as a required section), user's-terms changes are Compare &
   Contrast, 3-fix reports follow Correcting Discrepancy from Standard,
   three survived fixes trigger the problem tier. Found and backlogged,
   not fixed: README conductor section's stale "doesn't touch git" line.

3. **0.77.0 — the design instrument's machine half, four-role build.**
   tools/design/ (format standard + kit + CLI + 14 fixtures); a living
   design doc opts in with `## Evaluation matrix`; CI grows to SIX steps
   (matrix selftest + matrix audit). Named refusals: undeclared criterion,
   score without basis, △ without countermeasure+confidence, arithmetic
   drift (OVERALL/RANK recomputed), dead option (× on M) preferred. Render
   draws the movement-9 table; glyph survival fixture-checked. The
   brainstorming prompt set salvaged into the instrument doc; the
   superpowers tie CUT — sweep-verified zero routing references in living
   docs. 13/13 then 14/14 fixtures passed on players' first runs. One
   score defect (step-1 verify expectation contradicted its own content
   contract) fixed by callback; the player self-judged PASS instead of
   stopping — noted as the session's one player deviation.

4. **The graduation map resolved to zero graduations — and the ladder
   fired instead.** Per-trigger evidence check: none ripe under the no-rip
   rule; shared blocker was zero docs/product/ docs ever. So push-wiring
   became the first real gate-routed work item: killer risk probed live
   before framing (render self-reference is depth-1, not infinite —
   consecutive renders byte-identical by md5; the trailer commit is what
   moves the model), docs/product/push-wiring.md framed with Value in
   units, a three-row qualified ledger, and the staleness-refuser slice.
   `gate.py route push-wiring` → **enters at design**; frame, viability,
   slice pass on the doc's own artifacts. Composer key on the frame;
   stage: sliced; pushed as 1dfb268.

## What's Next

- **The push-wiring design conversation** — drawn package, two-key GO
  (`docs/design/push-wiring.md` + a `docs/gates/*-push-wiring-design.md`
  record are the contract gate's named needs), then contract → build of
  the staleness-refuser slice (progress.py `stale` + seventh CI step).
- After it: grounding-was-read · gate.py rendering through the progress
  view (both still design-layer builds).
- The AGENTS.md verdict and the README conductor git-line fix sit in
  Backlog Medium/Low.

## Key Decisions

- mode CUT (v0.75.0); losses named; spike.md content to git history with
  a return condition; skill count Eleven → Ten.
- Conductor's talk moments follow the format library (v0.76.0).
- The evaluation matrix is machine-checked (v0.77.0); form A — a tool,
  not a skill; validation scoped to living design docs only.
- Zero graduations executed — no trigger ripe under no-rip; the ladder's
  first real use is what makes them ripe.
- push-wiring framed: staleness at a push tip target 0, remember-steps
  target 0, enforced not remembered; auto-push excluded as accepted
  unknown with a review trigger on CI write-back.

## Commits

`874c93e` mode cut (0.75.0) · `8318029` render + mode-cut/10 ·
`129f438` talk-formats wiring (0.76.0) · `8b22e87` design instrument
(0.77.0) · `805f8bb` render + design-instrument/11 · `1dfb268`
push-wiring frame. Plus this switch-out commit.

## Gotchas

- **A file can be edited, verified, and still unshippable: AGENTS.md is
  gitignored.** The orchestrator's scoping called it "a real file, not a
  symlink" — true, and irrelevant: `git check-ignore` was the missing
  check. Any spec that stages a file by name must confirm the file is
  tracked/trackable first. (Mirrored to playbook.)
- The step-1 player self-judged PASS on a verify whose expected value
  contradicted its output, instead of stopping — expected values in
  scores are load-bearing, and players treat them as advisory under
  pressure to succeed. The spec's own STOP rule is the countermeasure;
  it held everywhere else. (Pattern note, not mirrored — one instance.)

## Insights

- **The refusal surface caught every defect this session** — five score
  defects across two four-role builds (missed occurrence, unclassified
  sweep hits, gitignored file, wrong verify expectation), each found by
  a verify grep, a sweep, a diff read, or a gate, never by luck. The
  BUILD property is earning compound interest.
- **Evidence checks can license NOT building** — the graduation map's
  correct outcome was zero executions plus a redirect. "The item was
  dodged" would have been the wrong reading; the map's own no-rip rule
  made zero the only honest answer.
- **Probe-first framing is cheap and decisive**: two shell probes (~1
  minute) turned the push-wiring killer risk from an argument into a
  measured depth-1 property, and the countermeasure fell out of the
  probe result. Same lesson as the transformation evidence checks.
- **The ladder's first routing behaved exactly as designed** — work
  entered at the deepest rung whose inputs existed, and the gate named
  the missing contract inputs in artifact vocabulary. One session after
  the gates shipped, they routed real work.

---

# Session 2026-08-04 (late night) — the ladder's first passenger delivered

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

**push-wiring walked design → GO → contract → build → ship in one
sitting; v0.78.0; the refusal surface is now seven CI steps.**

1. **The design conversation.** Package drafted from grounding reads
   (renderer determinism verified in code: counter ids, arithmetic
   seeds, sorted globs, no random/time), drawn via
   `tools/diagram/gen_push_wiring.py`, driven onto the live canvas.
   One round: Tony's font call (packages read in Nunito, not the
   hand-drawn kit default — applied in the gen script so the committed
   artifact IS the reviewed artifact), then GO. The gate record
   (`docs/gates/2026-08-04-push-wiring-design.md`) is docs/gates/'
   first citizen; stage → designed; `gate.py route` → enters at
   contract (2b2cbe8, CI green).

2. **A defect caught at design time:** `actions/checkout@v4` defaults
   to a shallow clone; the renderer derives landed pieces from git log
   trailers, so a fresh CI render would see one commit and refuse
   every push. `fetch-depth: 0` shipped with the slice. Same class as
   the 0.71.1 REPO bug — caught by grounding reads this time, not by a
   refused push.

3. **The contract.** Orchestrator two-pass (bounded 9-file scoping;
   ~400-line spec written straight to disk). Six steps, four pieces.
   Three risks surfaced at the gate and approved: the first CI run IS
   the Mac↔Linux experiment; the ship is structurally two push rounds;
   the single-serializer refactor (write_pair) amends the old
   progress.py-owns-writes split.

4. **The build: four players, zero deviations.** Steps 1/2/4/5
   (sonnet ×2, haiku ×2) — every verify matched expected on first run;
   the step-3 diff review found nothing to re-dispatch. First
   four-role build with no player failures and no orchestrator
   callbacks.

5. **The ship.** Both-ways refusal on the real tree (the way-one
   exit 1 was the tree's GENUINE staleness — the spec file itself);
   work commit with three Piece trailers → refresh → render commit →
   one push (1f7f570) → all seven steps green on the pushed SHA,
   cross-platform byte-identity proven by the birth run. Record round
   (bc0c07e + 8c0a838) same shape, green. Strip: `#### 4 landed ·
   0 remaining`.

**TODO closure:** the design-conversation Now item and the
contract→build follow-on — both done (evidence above).

## What's Next

- **push-wiring's goal record** — the route's one remaining need:
  `docs/gates/*-push-wiring-goal.md` with a "Done condition" section;
  cold eyes on the whole change + the expert-user pass (Tony ships
  something real under the new flow).
- Then: grounding-was-read · gate.py rendering have/need through the
  progress view.
- Standing flow change: every push now carries a current render or CI
  refuses it, message naming the fix.

## Key Decisions

- **Single-serializer rule:** `progress_kit.write_pair` is the only
  writer of the render pair; render and stale check both write
  through it — two serializations would drift and defeat the compare.
- **Ship flow is now commit → refresh → render commit (no trailer) →
  one push**; render-only commits carry no trailer (bounds divergence
  at depth 1).
- **`fetch-depth: 0` on the CI checkout** — not polish; shallow
  clones derive an emptier model.
- **Design packages read in Nunito** (Tony's call, in the gen script).
- Two-push ship shape approved at the gate rather than discovered
  mid-flight.

## Commits

`2b2cbe8` design GO + gate record · `4a0f45f` the staleness refuser
(pieces 1–3) · `1f7f570` render refresh · `bc0c07e` ship record
(piece 4) · `8c0a838` render refresh. Plus this switch-out commit.

## Gotchas

- **Multiple open Excalidraw tabs silently clobber a paste — last
  writer wins on the shared localStorage scene.** The driven paste
  vanished from every tab with no error when another of Tony's open
  Excalidraw tabs saved over it. One canvas tab while driving; eyes
  on the scene after every paste. (Mirrored to playbook.)

## Insights

- **Zero deviations traces to the spec carrying the implementation.**
  The orchestrator wrote Part B as verbatim code blocks — players
  copied instead of interpreting. Where judgment was already spent in
  the contract, cheap models executed flawlessly; the two earlier
  builds' deviations all happened where players had room to interpret.
- **The refuser's first genuine refusal was its own birth tree** —
  way-one of the demonstration wasn't planted; the uncommitted spec
  file made the committed render truly stale. The cleanest possible
  evidence that the check measures reality.
- **Grounding reads at design time caught what CI caught last time.**
  The shallow-checkout defect is the same environment-mismatch class
  as 0.71.1's absolute REPO path — that one cost a refused ship;
  this one cost a paragraph in the design doc.

---

# Session 2026-08-04 (goal gate, after the boundary) — push-wiring closes

## What Was Done

**The goal gate ran and closed — push-wiring walked every rung in one
day.** Cold-eyes reviewer (opus, work order + change only): layers 1–4
PASS, layer 5 BLOCK — CI on a direct push detects after landing, it
cannot refuse before; the declared "impossible to push" was false as
built. Tony chose amendment (b) over protecting main: declarations
restated as detection-at-tip (821f475), true prevention now a ledger
accepted-unknown with a named return condition; at-HEAD/disk
contradiction and spec stage lag fixed in the same commit; PR-event
edge backlogged. Goal record stamped
(`docs/gates/2026-08-04-push-wiring-goal.md`, 0cc6b34) — and pushing it
re-staled the render, the refuser's third genuine catch; its own fix
line was followed and the pair pushed as 4ac852a, CI green ×3 tonight.

**The expert-user pass produced the next piece.** Tony's verdict on
the slice: value stands. His finding: the human pull surface (terminal
+ static SVG) is hard to consume quickly — he wants an interactive
HTML view. That finding is the Value line of the ladder's next
passenger (merging the "gate.py through the progress view" slice),
now in TODO ## Now.

## What's Next

- **Frame the interactive HTML progress view** — through the gates,
  Value in units from Tony's own words.
- Then: grounding-was-read.

## Key Decisions

- Detection-at-tip declared honestly over prevention-by-protection
  (Tony's call: protecting main kills the direct-push style; the cost
  is priced in the ledger's return condition).
- The goal record's Done condition assembled entirely from upstream
  declarations — the walk's DONE-assembled rule, first real use.

## Commits

`821f475` amendment (b) · `0cc6b34` goal record · `4ac852a` render
refresh. Plus this boundary commit.

## Insights

- **The cold-eyes review earned its cost on its first run** — four
  clean layers and one real block that neither the builder, the
  conductor, nor five green CI runs had seen: the gap was in the
  DECLARED TRUTH, exactly where the walk said this flaw class lives.
- **The expert-user pass is a requirements instrument, not a rubber
  stamp** — its first run rejected nothing and still produced the
  next piece's Value line verbatim.
