# Session 2026-08-04

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

*Continuation of the 2026-08-03 evening session across midnight — the same
conductor run walked two more rungs. The vertical spine is complete.*

## What Was Done

**Walked and reviewed the CONTRACT rung** (`Write the contract · Size and
assign`). The design package arrives INTACT — the contract is written from
upstream truth, never a digest. **Two-tier access by role:** the overseer holds
all upstream truth; a builder gets the exact spec for their piece plus access
to related materials, and no more. The turn of the interview: **no human gate
at contract time** — Tony: *"i dont need to approve contract if we can measure
it meets output of other stages"* — provided every piece is measurable against
an upstream declaration; an unmeasurable piece cannot pass by assertion and is
a push-back to design (DONE-assembled applied to the contract itself). And the
**escalation contract**: he hears only of a gap no agent role can answer that
is a blocker; otherwise the next report is goal achieved — stricter than
today's surface-before-correcting behaviour. The board's CONTRACT row was also
de-tooled: it said "orchestrator" and "spec file", the exact
solution-vocabulary trap flagged earlier in the day.

**Walked and reviewed the BUILD rung — it consolidated 4→2 plus a rung-wide
property**, the same shape DESIGN's collapse took. *Build a piece · Prove it*:
done = measured against ALL RELEVANT specs and measurements — the piece's own
criteria plus everything its change touches, relevance scoped by the overseer;
"relevant" covers collateral by construction (the swallowed-helpers class shows
up because neighbouring terrain is relevant). *Prove the whole · Goal gate*
absorbs *Review unanchored* and *Verify against what we said*: cold eyes on
the whole change — seeing only the work order and the change, verdict can
BLOCK — once per goal plus on-demand for risky pieces; per-layer conformance
(code, logic, architecture, pixel vs design, product measurements — never one
verdict); then the human key, **the expert-user pass**: Tony uses the output
itself — *"im checking it as the expert user."* *Refuse bad work* stopped
being a function and became the property: **every gate on the rung must block
from outside the model; advisory output is not a check.** CI stays in the MVP
sequence as the property's first concrete instance.

**The cold-review scoping was my recommendation, adopted.** Tony asked
"thoughts?" instead of answering; the recommendation — once per goal plus
on-demand, never routinely per piece — carried because it rested on a flaw-class
distinction: measurement catches violations of what was declared; cold eyes
catch gaps in the declared truth itself, and those live at assembly.

**Walk state: 9 of 25, four rungs complete, everything reviewed both-keys.**

## What's Next

- **SESSION rung, 4 functions:** open/close · route to the altitude · drive to
  done · keep context optimal. Then CROSS-CUTTING (5 open). Expect
  consolidation — every drafted rung has shrunk under interview.
- Debt unchanged: `Frame the intent` acceptance checklists; the `reachable`
  clause; accepted risks age with nothing to bring them back.
- Backlog note: CI on `~/3of3` is now load-bearing twice over — it gates
  `/loop` AND it is the BUILD property's first instance.

## Key Decisions

- **No human gate at contract time** — provided every piece is measurable
  against an upstream declaration. Unmeasurable = push-back to design.
- **Two-tier access by role:** overseer holds everything; builder gets exact
  spec + related materials, no more.
- **Escalation contract:** the human hears only role-unanswerable blockers;
  otherwise goal achieved.
- **BUILD is two functions + a property.** Cold eyes once per goal (+
  on-demand); per-layer conformance; the expert-user pass is the human key.
- **A check that cannot block from outside the model is not a check.**

## Commits

`840a555` CONTRACT capture · `c0c0b55` CONTRACT reviewed · `c109008` BUILD
capture · `c81bccd` BUILD reviewed. Plus the switch-out commit.

## Insights

- **Every rung so far shrank under interview** — DESIGN 4→1, BUILD 4→2+property,
  and the survivors are consistently *his* outcome shapes, not the drafted
  decompositions. The draft over-splits because it mirrors tools and
  mechanisms; the interview merges because Tony thinks in outcomes. The
  remaining rungs (SESSION, CROSS-CUTTING) are served by Kerd's own skills —
  the over-split pull will be strongest there.
- **Tony's keys concentrate where his judgment is unique and vanish where
  measurement suffices.** Drawings (design GO), the output as expert user
  (goal gate) — but no gate on the contract, no key per piece. The pattern is
  experiential keys, documentary machine checks. Any future gate proposal that
  asks him to read a document should be suspect.
- **The escalation contract makes the refusal property load-bearing:** the
  human can only afford to hear nothing if nothing bad can pass silently.
  Written as a dependency on the diagram — the two requirements justify each
  other, which is exactly the relationship-between-functions class of finding
  that the full shallow pass was chosen to surface.

---

# Session 2026-08-04 (evening) — THE WALK IS COMPLETE

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

**Walked the SESSION rung — it consolidated 4→1 + a rung-wide property**,
the same collapse DESIGN and BUILD took. *Drive to done* is the sole
survivor. *Open/close · Keep tempo · Hold state* became the property: state
lives in the DECLARED ARTIFACTS, never in the session; anything worth
keeping is written the moment it exists; a session may die at any instant
and the loss is bounded to the in-flight piece, redone from its spec. The
turn of that interview was Tony's counter-question "why would sessions end
mid flight" — the boundary machinery exists for a conversation-shaped model
of work that the target system doesn't have. *Route to the altitude*
dissolved into the ENTRY GATES in series (work enters at the lowest rung
whose declared inputs all exist; the SPIKE is the one licensed bypass —
"mvp needs measurable results and a spec that can be reliably built
against"). *Keep context optimal* dissolved by construction — exact slice
per piece + free restarts made the missing degradation signal unnecessary.

**Walked ALL of CROSS-CUTTING in one pass** — the entry gate (thin gate
riding the ladder), Show where we are (push at every stage close + pull
anytime; LIVENESS at piece granularity — "confirmation of activity not
hang", not a static 'working…' line), Size work to a model (the declaration
that makes the ladder's "lowest role" computable; wrong sizes invisible to
the human), external tools (a tool is staffed like a PLAYER — bounded
contract, returns to the caller, killable), and How we talk (four rules:
decision questions carry what/why/gap/win/lose in the simplest terms; GROUND
before asking; never assume Tony's position; straw-man yourself first).

**Two properties promoted system-wide**: the ROLE LADDER (three independent
callers — contract escalation, loop questions, gate refusals; every
blocker/question/refusal rides one ladder, human last) and
STATE-IN-DECLARED-ARTIFACTS. **The reachable clause CLOSED**: an artifact is
reachable when at least one function's declared grounding names it,
gate-enforced — lost is now a checkable state.

Both new flows (`2026-08-04-session-flow`, `2026-08-04-crosscutting-flow`)
were generated, driven onto the live canvas, and two-key reviewed with
nothing to annotate. MVP keystone reworded on the board: entry gates, not a
dispatcher. One `(?)` remains on the entire board.

**TODO closure this session:** "Settle the reachable clause" closed
(commit 0046fb2); the walk item in ## Now completed and replaced with the
post-walk phase.

## What's Next

- **The post-walk phase: define how the skills must change to support the
  agreed requirements** (Tony's standing instruction: challenge everything
  current tooling does; findings logged during the walk, fixes designed
  after it). First logged finding: close-out-deferred capture — switch's own
  reflection step — violates the state-in-artifacts property.
- MVP sequence stands: entry gates (keystone) · CI/refusal (first property
  instance) · the SPIKE.
- Debt: `Frame the intent` route-specific acceptance checklists; accepted
  risks age; one `(?)` (Design: machine-checkable interface values).

## Key Decisions

- SESSION 4→1 + property; sessions end BETWEEN pieces, the cut chosen.
- Routing dissolved into the gates; SPIKE = the one declared bypass;
  spike output re-enters via the gates.
- The role ladder is system-wide; the human is its last rung; while an
  answer waits on the human nothing is built that the answer could
  invalidate (park vs stop = driving role's call).
- Show where we are: push at stage close/task end + pull anytime; the push
  is a report, never an ask; liveness at piece granularity derived from disk.
- A tool is staffed like a player; kill authority over rogue tasks.
- Four talk rules; facts never asked, positions never guessed.
- Reachable = named in ≥1 function's grounding, gate-enforced.

## Commits

`1079a68` SESSION walked · `64b63a8` SESSION reviewed · `0046fb2`
CROSS-CUTTING walked, walk complete · `a7ba20a` CROSS-CUTTING reviewed.
Plus the switch-out commit.

## Gotchas

- `navigator.clipboard.writeText` fails with NotAllowedError whenever
  Chrome isn't the OS-focused app — from a terminal-driven session that is
  always. `pbcopy` + trusted `cmd+v` through the extension is the path.
  (Mirrored to playbook.)
- A status marker detected by substring is asserted by any prose quoting
  it: "was (?), resolved" re-flagged three settled rows. (Mirrored to
  playbook.)

## Insights

- **The consolidation pattern held to the last rung**: 25 drafted functions
  became 15 walked/agreed, and the two biggest outputs of the final day were
  PROPERTIES, not functions. The draft over-split every rung it drafted;
  Tony's shapes are outcomes plus properties. Any future decomposition
  Claude drafts should be held loosely — the interview is where the real
  shape lives.
- **The ladder found itself**: contract escalation, loop questions, and gate
  refusals were interviewed on different days with different vocabulary and
  produced the same structure three times. That convergence — not any single
  answer — is the strongest evidence the requirement is real.
- **The walk indicted its own tooling**: switch defers gotcha/reflection
  capture to close-out, which is exactly the loss window the
  state-in-artifacts property forbids. Logged, not fixed — the post-walk
  phase owns it. (This very session log is written at the boundary it
  criticises.)

---

# Session 2026-08-04 (late evening) — the worked example and the six decisions

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

**Drew the Celtic Ticket Exchange worked example** — the whole agreed flow,
spark to shipped, ten steps on the canvas with skills/tools called out per
stage and four red deeper-consideration boxes (the value in units · UK
resale law as a survival-condition risk · the loop may not run until
something refuses · the expert-user pass scenario). Tony annotated in two
rounds — nine questions total — each read back, answered, logged in
`annotations/log.md`, and deleted from the canvas (the queue held).

**The nine questions became the post-walk phase's agenda, and all six
resulting decisions were taken in one sitting**
(`docs/plans/2026-08-04-post-walk-tooling.md`):

1. Sensei: the story grammar extracted as the system talk-format library
   (nine layouts, each already carrying its used-when trigger), three tiers
   by weight — layouts for everyday visuals · /sensei:story for documents an
   audience reads cold · /sensei:work for problems. Reference exemplar: the
   2013 API Gateway proposal (one page, current drawn, ideal drawn, scored
   evaluation, costs, roadmap).
2. Superpowers: BUILD our own approaches capability; the Technical
   Evaluation Matrix (FSS eval xls, de-spreadsheeted) is THE comparison
   standard — its ○/△/× legend IS the walk's risk vocabulary (circle = meets ·
   triangle = meets only with a countermeasure · cross = cannot meet, none).
3. Interrogate: modified into the tiered risk ledger.
4. Sherpa: CUT — the lifecycle IS the ladder; return condition named. The
   SPIKE dissolved with it (routing-to-skills died with function 16).
5. Ratified: top tier default at FRAME · progress renderer grows from the
   diagram toolkit · docs derived · domain tools per the tool rule.
6. Conductor: becomes the DRIVING ROLE (rung two of the ladder); its
   protocol is the seedbed the functions graduate from. No-rip transition.

**TODO closure this session:** the nine-questions agenda item closed
(7c90743); the SPIKE backlog item removed (dissolved by decision 4).

## What's Next

- **Turn the six decisions into specs** — the design work for the new skill
  layer: the format library into How-we-talk · approaches capability +
  evaluation matrix · risk ledger · conductor's split · the sherpa cut
  (executed only when rips are licensed).
- MVP sequence: entry gates (keystone) · CI/refusal · progress renderer.
- `Sensei Input/` triage decision at this boundary (see switch-out).

## Key Decisions

All six above, recorded in full in the phase doc and CONTEXT.md.

## Commits

`b76dad2` Celtic worked example · `1e77759` three more dispositions +
viewport gotcha · `7c90743` the six post-walk decisions. Plus this
switch-out commit. (`0046fb2`/`a7ba20a` closed the walk earlier today.)

## Gotchas

- Clipboard writeText needs OS focus; substring-detected markers are
  asserted by quoting them; canvas coordinates go stale when the human
  moves the camera — all three mirrored to the playbook earlier today.

## Insights

- **The grounding corpus did the deciding.** Every decision sharpened the
  moment a real artifact was read — the layouts carry their own route
  declarations, the eval matrix carries the risk vocabulary, the API
  Gateway paper carries the one-page bar. Tony's 2013 practice and the
  walk's 2026 requirements agree to a degree that suggests the walk
  recovered his working method rather than inventing one.
- **The worked example earned its cost immediately**: nine questions in two
  annotation rounds, six decisions from nine questions, one sitting. A
  concrete cargo (Celtic) surfaced questions the abstract board never did.

---

# Session 2026-08-04 (night) — the six specs

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

**Wrote and reviewed the entire spec package — all six, though the plan
promised two.** Each drafted, passed Tony's key, committed individually:

1. `docs/design/talk-formats.md` — eight formats with native used-when
   triggers, the three-tier weight rule, system-moment mappings,
   machine-checkable conformance. First citizen of docs/design/.
2. `docs/design/design-instrument.md` — independent approaches (shared
   riskiest assumption = one approach) + the evaluation matrix with the
   Toyota marks and the scoring discipline. Tony's two live corrections:
   scoring is KEY, and weighting is an OPT-IN mechanism — marks always ·
   scores when close · weights when criteria differ.
3. `docs/design/risk-ledger.md` — five states, fatal-by-impact-alone,
   killer-first, spike as instrument; NEW: review triggers on accepted
   states, closing the accepted-risks-age debt carried since the walk began.
4. `docs/design/progress-view.md` — two clocks (liveness per piece
   boundary, position per stage close); derived from disk, never
   self-reported — a hung model produces no commits and the strip shows it.
5. `docs/design/entry-gates.md` — the gate table (eight gates, exact
   on-disk requirements, the loop's gate demands a live refusal instance);
   grounding-was-read as the gate's second check; the keystone doubles as
   the FIRST REFUSER (files + front matter + sections = trivially CI-able).
6. `docs/design/conductor-role.md` — the seat at rung two; the graduation
   map (nine protocol pieces, destination functions, concrete release
   triggers); interactive tempo permanent.

**TODO closure:** "turn the six decisions into specs" — done (commits
4577208 · 2ef635b/a3b8f64 · 929a784 · 6aadf82 · f008861 · 9ea19fe).

## What's Next

- **Build the entry gates** — router + first refuser in one piece.
- Then: CI/refusal wider · the progress renderer.
- Rips (sherpa cut, superpowers un-routing) wait for the design-approval
  license.

## Key Decisions

Recorded in CONTEXT.md ("Rules born in the specs"): evaluation mechanisms
layer in; pre-declared weights; scores never overrule marks; review
triggers on accepted risks; progress derived from disk; gates check
grounding-was-read; interactive tempo permanent.

## Commits

Six spec commits (above) plus this switch-out commit.

## Insights

- **The specs wrote themselves from the corpus + the walk** — every section
  traces to an agreed requirement or a real artifact of Tony's. Where a
  spec added something new (review triggers, pre-declared weights), it was
  the walk's own machinery applied one step further, and Tony passed each
  without rework. Requirements agreed at the right altitude make design
  fast.
- **Two live corrections mid-review both went the same direction**:
  mechanisms should layer in as needed, never be mandatory ceremony. That
  instinct — rigor rises, ceremony stays low — is now visible in four
  places (sensei tiers, ledger tiers, matrix mechanisms, sherpa's old
  principle) and should be treated as a design invariant.
