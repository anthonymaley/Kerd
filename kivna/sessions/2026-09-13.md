# 2026-09-13

## Sitting: the 0.113.0 pickup and eight peer contributions (2026-09-12 22:25 – 2026-09-13 08:51 EDT, Claude session on the 0.113.0 plugin cache; Codex in the paired TUI)

**What happened, in order.**

- **Switch In on 0.113.0.** The plugin cache had delivered 0.113.0 on an
  ordinary startup — the 2026-09-12 close called that unverified; observed
  once now. Reading set as named; local `main` equal to `origin/main` at
  `3529cc1`. Conductor loaded; one dashboard, one question in YOU (start the
  Codex snapshot refresh), no second report. Anthony never answered it: the
  proposal was not approved and no work item started this sitting.
- **`/kerd:agent` with no contribution named.** Discovery showed this session,
  the `codex-tui` thread (activity unknown) and four recorded Claude partners
  not listed natively. Codex then bound this session as its current partner
  (reported by Codex).
- **Eight peer requests, all handled read-only; this session edited no
  repository file.** Three acknowledgements (the 0.113.1 arrival follow-up,
  the Agent identity fix, the Markdown presentation plan). Three reviews:
  the Markdown In/Out presentation — three findings (document path hidden
  behind the link, footer line break on trailing spaces, over-escaping of
  paths), all fixed by Codex and re-verified here; the coordinated Out draft
  and compact arrival — four guidance findings (ownership by the person's
  request, exclusive shared files during Out, branch/worktree work named not
  merged, unknown standalone contributors disclosed) and one closing-box
  wording, all verified closed, two synthetic scenarios walked, a fresh-disk
  reread reminder added; the private partner-role addition — release-ready,
  one cosmetic null-versus-absent note. Two release syncs: 0.114.0 at
  `b80d932` (17 files) and 0.115.0 at `580747d` (16 files), each verified
  from this session by fetch. Tests run here during the reviews: renderer
  106, 108 and 112 passing; the Agent suite; `gate.py release` clean each
  time. Client rendering was never observed.

**Closeout, this Out.** The first Out under the 0.115.0 coordinated-closeout
guide, run from the 0.113.0 skill cache while following the repository's
current `in-out.md`. Owner: this session, by Anthony's `switch out`.
Contributor: Codex, whose work is already saved at its two release boundaries
(pointers, work records). Codex appended no session-log account for the
0.114.0 and 0.115.0 sittings, so this entry carries them from its peer
messages, as reported. No Agent request was needed: nothing material was
found to remain only in Codex's conversation. Done here: the four owed rulings
recorded in `docs/decisions.md`; `## Now` rewritten to the 0.115.0 live
checks; `CONTEXT.md` Where We Are leaned to the current boundary; this log.
Memory readiness: ready.

**Not observed, not claimed:** the compact 0.115.0 arrival or a coordinated
Out in a fresh session; the Codex snapshot's current state; Anthony's
assessment of a 0.114.0 arrival (Codex reported "looked loose", which led to
the compact layout); the two CI runs Codex cited, not opened here; any token
saving.

## What's Next

The live checks in TODO `## Now`, all against 0.115.0. First: refresh the
installed Codex snapshot, open a fresh Codex session in a work project, run
Switch In without starting work, record the tested revision in
`docs/work/codex-plugin/work.md`.

## Gotchas

- A dashboard proposal can be overtaken while it waits. Two releases landed
  during this sitting and the proposal's target moved from 0.113.0 to 0.115.0
  without a re-render. Before claiming which build an arrival came from, read
  `plugin.json` in the skill's base directory.
- Codex's closeouts update pointers and work records but did not append to
  `kivna/sessions/`. The next Out owner should diff the log directory since
  the last boundary before assuming the account exists.
