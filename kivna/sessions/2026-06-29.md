# Session 2026-06-29

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync before this switch-out commit)

## What Was Done

- **Locked all four Phase 0 decisions** for the sherpa/lifecycle redesign (ran as a `dian` session: orient → plan → execute → close-out). This gates the whole build.
  1. **Renames:** `dian` → **`conductor`** (session conductor — keeps one session in tempo, present/in-the-moment); `dial` → **`sherpa`** (the lifecycle PM — guides the long staged ascent across many sessions). Worked the names live: "coach" rejected as too sporty; "sensei" rejected (collides with the existing sensei plugin + oversells — dian enforces discipline, it doesn't teach).
  2. **sherpa = one skill** holding all 5 stages + graduation logic, not five mode files.
  3. **Kerd stays one plugin** (skills + sherpa + conductor), no split.
  4. **Mode-set prune:** spike/jit fold into sherpa (Explore/Build stages); the rest (legal/sales/writing/research/strategy/maintain + greenfield/quickfix/deepwork) retire as modes — they're skill-use, not right-sizing contracts.
- **Recorded the decisions** in the design doc Phase 0 block (boxes checked `[x]`, Phase 3 rename line corrected to target `conductor`/`sherpa`) and in `TODO.md` (Context entry + forward state).
- **Surfaced the first Phase 1 blocker** (didn't resolve it — Tony wrapped the session): sherpa's cross-session state can't live in `.active-modes` (it's session-ephemeral). See What's Next.

## What's Next

- **Resolve the open Phase 1 decision FIRST: where sherpa's cross-session state lives.** Design doc says `.active-modes (terse)`, but that's cleared on close-out and sherpa spans many sessions — it must persist which idea, current stage, and each stage's produced handoff. Lean: durable per-idea `kivna/sherpa/<idea-slug>.md` (committed) + a lightweight current-session pointer in `.active-modes`. Decide, then write the sherpa SKILL.md skeleton (Phase 1).
- Then Phase 1 proper: sherpa conductor skeleton (stage tracking, start-at-any-stage, advise-next, jump-back-on-failure, rising rigor, JIT decision-style), plus the stage-handoff mechanic.
- Phase 2 trains each stage as an eyeball-gated slice (Validate still needs its own deeper design session first).

## Key Decisions

- **Names: conductor (session) + sherpa (lifecycle).** Two concrete guide-roles split by timespan: sherpa guides the long multi-session climb (knows the route, paces stage to stage, calls the turn-back); conductor keeps one session in tempo. "coach" out (sporty), "sensei" out (collides + dian enforces rather than teaches).
- **sherpa is one skill, Kerd stays one plugin, modes prune to skill-use.** All three confirmed off their design-doc leans, no change.
- **`.active-modes` is the wrong home for sherpa state** (it's session-ephemeral). Sherpa needs durable, committed, per-idea state — open exactly where, but `.active-modes` is ruled out.

## Insights

- **The name fell out of the role's timespan, not a thesaurus.** "sherpa vs coach vs sensei" resolved once the question became *how long does this thing's job last?* — a single session (conductor) vs a months-long expedition (sherpa). Picking the metaphor by duration, not vibe, made the choice obvious and surfaced the sensei-plugin collision as a real constraint rather than a stylistic preference.
- **Locking decisions exposed the next design hole.** Recording "stage tracking in `.active-modes`" forced the question "but that file gets cleared" — the act of writing the decision down found the Phase 1 blocker. Cheap to surface now, expensive to discover mid-build.
