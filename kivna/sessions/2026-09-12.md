# 2026-09-12

## Sitting: from the morning pickup to the 0.113.0 closeout (09:39–22:14 EDT, Claude release session; Codex in the paired TUI)

**What happened, in order.**

- **First real Conductor session on 0.111.0.** Opened on a typed "approve"
  that turned out to be meant for a held Codex peer request; the job was owed
  and read-only, so it ran. A Claude player (Opus 5, requested; identity not
  observed) ran as a native background subagent through the host, not through
  `ask.py`: 73 Backlog rows verdicted — 3 done, 8 dead, 60 open, 2 unsure.
  Record and dispatch proof:
  `docs/work/model-ready-work/trials/2026-09-12-conductor-session-closure-review.md`.
- **Codex's release-111 follow-up.** Codex reviewed 0.109.0–0.111.0, wrote
  `docs/work/release-111-followup/spec.md`, and asked this session for a design
  review, then an implementation review; both delivered by reply marker and
  retrieved from this transcript. Anthony switched peer messages to accept
  (user-wide `crossSessionInbound`; it accepts every local sender). The Agent
  TUI route proved itself in both directions today: two Codex reviews of the
  Out box came back by `codex queue` and rollout markers (requests `84616584`,
  `a3a489f6`), so the owed re-run of the 0.109.0 proof is met on 0.112.0.
- **0.112.0 released** (branch `main`, subject "One arrival, one decision; the
  saved-place box returns"; remote-verified by the helper; CI green). One
  arrival with the decision in YOU; the Out saved-place box restored
  (`render_closing`, `--closing`, `ClosingBoxTests`); Codex's five fixes; my
  three wording fixes. Codex reviewed the box and the final diff: four
  findings, fixed, verdict ready — `docs/work/release-111-followup/out-box-review.md`.
- **0.113.0 reviewed.** Codex built the Codex core package (four skills, no
  hooks, version derived from the release), the measured reading selections
  (`read_args`, `reaches_eof`), and the pickup clarifications (factual answer
  is not approval; ask once; `--question-below`; logs preserve claims). This
  session's independent review: all suites and gates green on the pending
  tree, package builds cleanly away from the source, four findings (a drift
  from the players-are-native-subagents ruling, fixed; Kerd's own start point
  silent on the work, this closeout; two clarity items, one fixed). Evidence
  and the live checks: `docs/work/codex-plugin/work.md`. Anthony then said
  "okay lets release"; Codex owns the publication.

**Closeout, this Out.** Eleven closed Backlog rows moved to
`docs/backlog-archive.md` with verdict, evidence and date (the hooks
sub-finding kept open as its own row); `## Now` rewritten to the live checks;
`CONTEXT.md` Where We Are rewritten with the reading set and its measured size;
the release-111 spec's obsolete "not yet committed" pointer repaired. Records
prepared only: no helper save by this session; Codex publishes the combined
boundary.

**Produced since the last boundary (b139519), for the fidelity gate:**
`docs/work/release-111-followup/spec.md`,
`docs/work/release-111-followup/out-box-review.md`,
`docs/work/model-ready-work/trials/2026-09-12-conductor-session-closure-review.md`,
`docs/work/codex-plugin/work.md`, this log. The Codex catalog under `output/`
is generated and ignored.

Implementation references for the same boundary (detail stays at the source):
`docs/work/codex-plugin/review-prompt.md`; packaging `START.md`, `build.py`,
`claude-plugin.json`, `codex-plugin.json`, `test_build.py`; Agent `agent.py`,
`test_agent.py`; Conductor `execution.md`, `journey.md`, `model-jobs.md`;
Switch `in-out.md`, `handoff.py`, `test_handoff.py`, `where_we_are.py`,
`test_where_we_are.py`. The prior 0.112.0 grounding repairs touched
`docs/product/conductor-boundary.md`, `docs/product/funnel-driver.md`,
`docs/product/grounding-was-read.md`, `docs/product/question-set-derivation.md`,
`docs/product/requirements-success-measurement.md`,
`docs/product/requirements-traceability.md`, `docs/product/rigor-level.md`,
`docs/product/shared-memory.md`, `docs/product/switch-fidelity.md`,
`docs/product/vault-unhook.md`. These are references, not new work in 0.113.0.

The final release validation and its limits are recorded in the Codex work
record: notably one Conductor process-check PermissionError followed by a full
37/37 isolated rerun, with no test weakened and no claimed diagnosis.

**Not observed, not claimed:** a fresh Codex session discovering the four
skills (the installed 0.113.0 snapshot predates the pickup corrections and
needs an explicit refresh first); a fresh Claude pickup on a released build
showing the composed arrival; the behavioural Yes/No/Not-sure scenario run by
a model; token savings. The Claude installation was not changed. The plugin
cache on this machine held 0.111.0 at 12:00; later delivery on an ordinary
startup remains unverified.

## What's Next

Resolve the authorized release boundary by `main` and subject
`Release Kerd 0.113.0: Codex core and reliable pickup`; verify Git and CI before
retrying publication. Then perform the live checks in TODO Now against the
actual installed build. The existing Codex snapshot needs an explicit refresh.

## Gotchas

- A typed "approve" in this terminal is not delivery of a held peer message.
  Codex asked Anthony to approve a held request; the word landed here as a
  chat message and started an owed job instead. With peer messages accepted
  the hold no longer occurs; the misreading is recorded, not hidden.
- The measured reading must be taken to a fixed point: pasting the number
  changes the file's size.
