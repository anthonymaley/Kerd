# Session 2026-08-13 (afternoon, 12:55–13:16 EDT) — hooks ship the standard way, stop.sh cut

**Machine:** Anthonys-MacBook-Pro.local
**Branch:** main
**Tracking:** origin/main (up to date)

> A side-quest from a support question: Tony reported `/switch` giving "Unknown
> command" in other repos. Diagnosis went past the red herring to a real
> distribution bug, and the fix shipped as v0.96.0 under a proportional-middle
> conductor slice. Requirements (the standing focus) was not touched.

## What Was Done

Per-task actuals from the conductor marker and git commit times:

- **hooks-autoload — started 12:55 (execute marker) · landed 13:06 (`2146925`, main change) · 13:09 (`a0b5fde`, session-start follow-up)**

The reported symptom (`Unknown command: /switch` in `~/work-anthony`) was a red
herring: skills are namespaced, so the invocation is `/kerd:switch`. The *real*
bug the investigation found is that **Kerd's hooks were dead across eleven
repos**. The old mechanism (tend pastes a version-pinned plugin-cache path into
each repo's `settings.local.json`) is the anti-pattern the plugin docs name:
Claude Code garbage-collects old cache versions, so every pinned path dies the
moment its version is pruned — `docs/playbook.md` already documented this exact
GC-pin-rot gotcha (2026-07-11).

Fixed by moving to the standard mechanism, shipped v0.96.0:

- **`hooks/hooks.json`** created (pair, session-start, skill-complete) — the
  harness auto-registers it on enable, and `${CLAUDE_PLUGIN_ROOT}` resolves at
  runtime, so no repo ever pins a cache path again. Verified from live docs via a
  claude-code-guide agent (code.claude.com): the var expands in a plugin's own
  hooks.json but NOT in user settings.local.json.
- **`hooks/stop.sh` cut**; `hooks/hooks.template.json` retired (superseded).
- **`skills/tend/SKILL.md`** Category 9 rewritten — migrate/remove stale manual
  entries instead of wiring them; dev-repo exception dropped (would double-fire).
- **`skills/slainte/SKILL.md`** check 5 inverted — `hooks.json` is now the
  intended file, not an error.
- **De-referenced stop.sh** in `README.md` (+ v0.96.0 What's New entry, "three
  hooks", hook section rewritten for auto-load), `docs/state-contract.md`,
  `docs/design/time-awareness.md`, `docs/playbook.md` (GC-pin-rot gotcha marked
  resolved), `docs/requirements/categories.md` (SEC row — the outbound git-fetch
  is now auto-loaded for all users, sharpening the finding).
- **`tests/hooks_test.sh`** — stop tests removed, the template test re-aimed at
  `hooks.json`, session-start assertions updated for `/kerd:switch in`.
- **`hooks/session-start.sh`** — fixed its own bare `/switch in` → `/kerd:switch in`
  (same dead-command defect as stop.sh).
- **Version 0.95.0 → 0.96.0** in all three locations; frame at
  `docs/product/hooks-autoload.md` (route: problem, rigor: mvp).
- **Eleven consumer repos migrated** — dead hook entries stripped from each
  `.claude/settings.local.json` (all gitignored/machine-local, all Kerd-only
  hooks, non-hook settings preserved): 3of3, apple-music, Bree, dinner-tonight,
  eolas, krutho-founders, krutho-strategy, leru, toyota-sensei, weefish,
  work-anthony.

Verified: hook harness 21/22 (1 pre-existing env failure), release gate + full
audit clean, selftest 26/26, both CI runs green, progress board re-rendered with
the slug on it. Release close-out pass (tend + slainte) ran clean — no fixes
needed, the cross-cutting sweep during the build kept everything consistent.

## What's Next

- **THE open item — verify auto-load empirically.** It is proven from docs and
  the config is valid, but never fired on this machine: the cache is still
  0.95.0 (no hooks.json). **Repin to 0.96.0** (`/plugin` update or reinstall
  kerd), then a fresh session in the Kerd repo must show `📋 Last session: …`
  from `session-start.sh`, sourced only from auto-load (manual wiring is
  stripped). If it does not appear, auto-load failed — fallback is resolved
  absolute paths (worse, but working). Logged High in TODO; the risk ledger in
  `docs/product/hooks-autoload.md` carries it.
- **Position:** `hooks-autoload` sits at **slice** on the board (framed +
  sliced, built proportionally with no separate design/contract rung — Tony's
  proportional-middle call). Requirements-traceability, funnel-driver,
  switch-fidelity and shared-memory all remain at **design**, untouched.
- Requirements (the standing focus) resumes where 2026-08-08 left it: the
  blocking AU7/AU8 validator for `catalog.md`'s declared refusals.

## Key Decisions

- **Hooks ship via plugin auto-load; stop.sh cut** (full rationale in CONTEXT.md
  Key Decisions). Standard mechanism = never breaks on update, zero per-repo
  wiring, what users expect. stop.sh named as a loss.
- **Proportional-middle altitude** — framed for board-trackability, built inline
  as one task, no composer/spec-file machinery, because the design was settled in
  conversation and the edits were mostly mechanical.
- **Dev-repo exception dropped** — no repo wires manually under auto-load
  (double-fire), including Kerd itself; hook dev is validated by the test harness.

## Commits

- `2146925` — Ship hooks via plugin auto-load; cut stop.sh (v0.96.0)
- `a0b5fde` — Fix bare /switch in reference in session-start hook (v0.96.0)

Both pushed to origin/main, CI green.

## Gotchas

- **`${CLAUDE_PLUGIN_ROOT}` expands in a plugin's own `hooks/hooks.json` but NOT
  in user `settings.local.json`** — the whole reason auto-load works where manual
  wiring needed absolute paths. Mirrored to the playbook (folded into the
  resolved GC-pin-rot gotcha).
- **AU5 grounding refs must resolve on disk** — a URL in a `## Grounding` line
  fails the audit ("does not resolve"). Put external-doc citations in prose/risk
  evidence, not as a grounding bullet.
- **A subagent told "STRICTLY READ-ONLY" still wrote `output/`** into the repo
  (StrictDoc's `_cache`, 2026-08-08) — it is still sitting untracked at root,
  left for boundary triage.

## Insights

- **The reported symptom was not the bug.** "Unknown command: /switch" pointed at
  the command namespace; the actual failure was dead hooks from cache-version
  pinning. Chasing the red herring would have "fixed" nothing. The dead-command
  string even turned out to be *emitted by the broken hook itself* (stop.sh's
  `/switch out`), closing the loop.
- **The fix supersedes a backlogged countermeasure.** The playbook's proposed
  fix for GC-pin-rot was an automated staleness check in tend. Auto-load removes
  the pinned path entirely — killing the bug class beats detecting-and-repairing
  it. The backlog row is now moot.

---

# Session 2026-08-13 (afternoon second sitting, 13:37–16:29 EDT) — the register gets its refuser, and conductor learns to advise down

**Machine:** Anthonys-MacBook-Pro.local
**Branch:** main
**Tracking:** origin/main (up to date)

> Two releases in one conductor sitting, both on Fable as the session model —
> which itself became the session's second work item: the producer watched the
> top tier conduct mechanical work and turned the observation into FUN-010
> mid-build. The register's capture beat ran end-to-end live for the first
> time: said → `proposed` → keyed `final` → built, in one session.

## What Was Done

Per-task actuals from the conductor marker and git commit times:

- **AU7/AU8 register validator — started 13:37 (execute marker) · landed 13:52 (`8b7f52d`, v0.97.0)**
- **FUN-010 model+effort advisory — started 16:19 (execute marker) · landed 16:23 (`f4c51c0`, v0.98.0)**

**Task 1 — the register's declared refusals got their refuser (v0.97.0).**
`catalog.md` had declared "an unknown field is a hard error" and "the audit
REFUSES" since 2026-08-08 with nothing enforcing either. Now AU7 (blocks and
states) and AU8 (links) ride `gate.py audit` — CI gained no step. Hard
refusals: illegal ID/state/category, unknown field, missing Source/statement,
`Approved` hash divergence (state never rewritten), `superseded` without its
`superseded-by`, unregistered link roles, dangling targets, duplicate IDs.
Non-blocking findings, per the catalog's own flag-vs-refuse words: stale link
stamps and the missing-parent trace gap (aggregated to one line). Six fixtures
T27–T32 (selftest 26 → 32), all first-run green. The hash recipe was verified
empirically against shipped `Approved` values before the checker was written.
Live tamper demo: editing PRD-001's statement in a copy produced the exact
declared refusal. The real register passes clean. Nothing hardcoded — the
legal category set is read from the project's own `categories.md`, which
closed the "move the twenty codes out of kit.py" TODO row as premise-dead.

Closure inference correction recorded: TODO claimed the 2026-08-08 prototype
was "in this sitting's log" — false. The log described it; no code was ever
committed. Written fresh from the catalog.

The stale-count sweep the blocked design predicted: "AU1–AU6"/"six rules"
counts updated in both READMEs and `skills/slainte/SKILL.md`, and in two
diagram generators whose strings are baked into committed renders —
`tools/diagram/gen_kerd_map.py` → `docs/design/kerd-map.excalidraw` +
`docs/design/kerd-map.svg`, and `tools/diagram/gen_release_closeout.py` →
`docs/design/release-closeout.excalidraw` +
`docs/design/release-closeout.svg`, both regenerated.

First live catch: `TST-003`'s heading had an empty title (`### TST-003 — `).
Parser now accepts the empty-title shape (Title is optional) and the block
got a real title.

**Task 2 — conductor advises the pair, both directions (v0.98.0), building
FUN-010.** The requirement arrived verbatim mid-task-1 and landed as
`proposed` the moment it was said; the producer keyed it `final` at the
framing gate. The Model advisory section became Model + effort advisory:
state the believed current pair and its staleness (the system prompt names
the model at session start and goes stale on every mid-session `/model`
switch — proven live this session; effort is exposed nowhere), confirm inside
the existing gate, advise the named downgrade when overpowered, dispatch the
composer at its own sized effort. Framed at
`docs/product/model-effort-advisory.md` (route: new, mvp, proportional).

**The trace-gap sequencing was agreed** (46 of 51 requirements unparented):
fills top-down — producer files the missing BUS/STA/USR origin rows, then
model drafts `refines` parents in category batches for his key as sets;
folded into the design-package rework, the AU8 finding is the live tracker.

Release close-out pass ran: tend 9/9 categories passing (nothing to fix),
slainte 0 findings (left untouched: playbook Current Status duplication —
tracked Medium row; vault staleness — informational by rule).

## What's Next

- **Position:** `requirements-traceability` stays at **design**; its package
  rework is the next real work, now absorbing the trace gap. `hooks-autoload`
  at slice (its auto-load verification still needs a cache repin + fresh
  session — unchanged, High in Backlog). `model-effort-advisory` on the board
  at slice (built proportionally, same shape as hooks-autoload).
- **In order:** `gate.py --root` (script-location half undesigned) → design
  package rework → release grouping artifact.
- **Owed by the producer:** the BUS/STA/USR origin rows (~5-10 blocks);
  his key on `categories.md`; the board mechanism question.
- **Model note for the next session:** `/model fable` was saved as the
  default for new sessions on 2026-08-13 — the next session opens on Fable
  unless `/model opus` is set there. Under the new advisory, conducting
  routine work wants Opus at a middling effort.

## Key Decisions

- **AU7/AU8 ship as CI-riding audit rules with a findings channel** — full
  rationale in CONTEXT.md Key Decisions (refusals vs findings split on the
  catalog's own vocabulary; `--json` stays a bare problems list).
- **The trace gap fills top-down, folded into the design rework** — origin
  rows first, else the trace is manufactured (CONTEXT.md Key Decisions).
- **FUN-010 keyed final and built same-session** — the capture beat's first
  end-to-end run (CONTEXT.md Key Decisions).
- **Catalog wording corrected**: "inbound `refines`" → the outbound parent
  edge, with a dated note at the correction site.

## Commits

- `8b7f52d` — Ship AU7/AU8 — the requirements register validator (v0.97.0)
- `f4c51c0` — Conductor advises the pair — model AND effort, both directions (v0.98.0)
- `98def1c` — Refresh progress render for model-effort-advisory

All pushed to origin/main, CI green ×2 (verified per push).

## Gotchas

- **`gh run list --commit <short-sha>` silently returns nothing** — it needs
  the full 40-char SHA. A poll loop that treats empty output as "not yet"
  spins forever on this; the first CI monitor this session did exactly that
  (silent-on-failure, the Monitor guidance's named failure mode). Match
  terminal states or use the full SHA.
- **A risk-ledger State cell is an exact enum token** — `accepted (with a
  parenthetical)` was refused by the slice gate. Qualifiers go in Evidence;
  the State cell carries only the legal token. The refuser catching its own
  author on the day it shipped.
- **A register statement whose FIRST line matches the meta shape
  (`**Word**: ...`) is refused as an unknown field** — ambiguity is refused,
  never guessed (documented in `parse_register`'s docstring). Reword the
  statement's opening or the block misparses by design.

## Insights

- **The capture beat paid for itself on its first real run.** FUN-010 went
  said → `proposed` (free, mid-build, no interruption) → keyed `final` at the
  next gate → built and shipped, in under three hours. The 2026-08-08 measure
  was "50 produced, zero promoted"; the promotion flow now has one complete
  live traversal.
- **The refuser refused its builder twice on day one** — the empty-title
  heading (a data defect surfaced) and the illegal ledger state (this
  session's own frame). Both caught by machinery this repo built, neither by
  discipline. That is the argument for gates over vigilance, demonstrated on
  the gates' own author.
- **The advisory's central mechanism was proven by the session that built
  it**: the system prompt still said Opus 4.8 while the session had run on
  Fable since 13:26 — the staleness the belief-plus-confirmation design
  exists to survive was live in the room.

---

# Session 2026-08-13 (evening, 16:40–23:08 EDT) — the reset

**Machine:** Anthonys-MacBook-Pro.local
**Branch:** main
**Tracking:** origin/main (up to date)

> A session that set out to close two shipped features on the ladder and was
> stopped twice for jumping to solutions, then stopped a third time for the
> real reason: neither party could say whether what exists serves the goal.
> Tony called a full reset. Nothing was built. The most valuable output of the
> day is a document containing almost nothing but his own words.

**Time note — the conductor marker failed.** It was written at `orient` 16:40,
never advanced through five hours of work, and was restamped to `execute` at
21:46 when the slip was noticed. So 21:46 is a restamp, not an execution start,
and the heading's open side uses the orient stamp instead. Per-task times below
come from commit timestamps, which are machine-written and trustworthy. This is
the exact drift the same-turn rule exists to prevent, and it happened anyway.

## What Was Done

Per-task actuals from git commit times (the marker being unusable, see above):

- **The reset interview — first capture 17:42 (`cddfcce`) · last ruling 23:05 (`e8e5aa4`)**
- **Goals — drafted ~18:05, APPROVED by Tony 18:30 (`ad03ad7`)**
- **Requirements draft — first draft 21:51 (`3c1b4bb`) · after adversarial review 21:56 (`fdc2a7a`) · after his rulings 22:46 (`d06bf72`)**

**Switch-in closed the top High backlog row on evidence.** Hooks auto-load was
verified empirically for the first time: the cache carries 0.96.0 with
`hooks/hooks.json`, neither the repo-local nor the global settings hold any Kerd
hook wiring, and `📋 Last session: 2026-08-13` appeared at session start — a
string built only by `hooks/session-start.sh`. Auto-load works;
`${CLAUDE_PLUGIN_ROOT}` resolves at runtime with zero per-repo wiring.

**Then the session went wrong twice, and both are worth recording because the
reset is a consequence of them.** It first proposed hand-writing four
retroactive gate records for `model-effort-advisory` and `hooks-autoload` — both
shipped, both stuck below `goal` because a proportional build skips rungs the
gates demand. Tony stopped that: the tax is the common case, not an edge case.
It then framed `docs/product/inline-composer.md` as the fix. He stopped that
too — *"perhaps we are jumping to solutions too quick on this though"* — and
named the real issue: requirements are not captured and managed from design
through launch, that work was started on 08-08 and abandoned the next session.

**The evidence supported him, harder than expected.** `ls docs/gates/ | grep
requirements` returns **nothing** — zero gate records for any requirements slug
in five days, while three releases shipped. The three most recently written
product docs were all displacements of the standing focus, the newest written by
this session four minutes earlier. The register holds 51 requirements with 46
unparented, because it was filled from the middle of the chain outward with no
goals above it: BUS zero rows, STA zero, USR one. **Capture was built;
management never existed.**

**The full stop.** *"honestly i feel we are lost here… the fact that we are both
confused tells me we need a reset."* His instruction was operational: go back to
interview level and **do not ground anything in existing knowledge** until it is
captured as the source of truth.

**What the reset produced:**

- **`docs/kerd-interview.md`** — six questions answered verbatim (what are we
  building · what does it have to do · what does good look like · why not use
  another skill · why would you use it · why would someone else want it), plus
  the walkthrough, the definition of launch, and every subsequent ruling. Read
  back and **confirmed by him as the source of truth** at 17:41.
- **`docs/kerd-goals.md`** — **APPROVED 18:30.** Three laws, eight goals, each
  carrying a design input in his own words. He added a design input to G1
  directly into the file.
- **`docs/kerd-requirements.md`** — 59 entries, drafted by a composer that was
  blocked from reading the old register so the later comparison is honest.
  Survived its own straw-man, an independent adversarial review, and two rounds
  of his corrections. **Still a draft.**

## What's Next

- **What the requirements system actually IS.** His ruling: *"we cannot consider
  this markdown file as how we capture and version and work on requirements. we
  need a robust and easy to engage with solution."* The pre-reset tooling
  evaluation (`docs/design/requirements-traceability.md`) is **now legitimate
  input** — the bracketing rule expired when the interview was captured.
  Re-examine it against the approved goals rather than as precedent.
- **Work the requirements draft through to final.** Goals are inputs to
  requirements; finalisation is a worked process, not one dispatch and a yes.
- **The build-vs-adopt decision (Q4) is still open** and now has criteria: the
  approved goals. The superpowers material is experience, not requirement —
  input to that evaluation only.
- **Position:** unchanged and now questionable. `requirements-traceability`,
  `funnel-driver`, `switch-fidelity`, `shared-memory` at **design**;
  `hooks-autoload` and `model-effort-advisory` at **slice** despite having
  shipped. `inline-composer` framed at **frame**, deliberately unbuilt. The
  board misreports two live features, and the reset may change what the ladder
  is, so do not walk them by hand.
- **`hooks-autoload` fails the slice gate** on an illegal risk-ledger State
  token — `accepted (named loss)` is not a legal value. One-line fix, untouched
  because of the reset.

## Key Decisions

Full rationale in CONTEXT.md Key Decisions. In brief:

- **The reset itself** — the interview is the source of truth, the goals are
  approved, and everything predating them is evidence rather than precedent.
  Nothing was pruned from CONTEXT.md; the licensed-prune rule did not fire and a
  reset is not a licence to delete the record of how we got here.
- **Three laws born:** every project has its own repo (non-negotiable); every
  change lands in the spec, design or requirements (**absolute law,
  proportionate ceremony**); check your own work before it counts, inward *and*
  outward.
- **Goals carry design inputs, never metrics** — his correction, and it killed a
  first draft in which four of eight measures would have been passed perfectly
  by a system that produced nothing at all.
- **The superpowers commentary is experience, not requirement.**
- **Wrong agreement at the approval gate is an accepted residual risk**,
  mitigated not cured, and what the backbone buys is that **changing your mind
  becomes controlled** rather than indistinguishable from disruption.

## Commits

- `cddfcce` — Capture the reset interview as the source of truth
- `8d06a3f` — Interview: define launch, and the UI/UX-measured-during-build requirement
- `7ff6bf3` — Goals carry design inputs, not measures — Tony's correction
- `d6380de` — G3 gets its design input — fine tuning is a recognised class with a guard
- `476fc5f` — G3's guard corrected — a fine tune still lands in the spec
- `ee1a56f` — Law 2 — every change lands in the spec, design, or requirements
- `fb5d3b2` — Approval is a blocking gate, not a status; self-check triggers answered
- `ba7bc3c` — The fine-tune classifier — five questions, and who approves
- `ad03ad7` — Goals APPROVED by Tony, 2026-08-13 18:30
- `3c1b4bb` — Draft requirements from the approved goals — awaiting review
- `fdc2a7a` — Requirements revised after adversarial review — 53 to 57
- `9bc1729` — Six rulings — C-T1 dissolves, self-checking becomes Law 3
- `d06bf72` — Requirements: C-T1 dissolved, superpowers material reclassified, outward pass added
- `2892983` — Three rulings — the markdown file is not the requirements system
- `289a174` — Wrong-agreement risk accepted as mitigated, with the residue named
- `e8e5aa4` — The backbone enables controlled change of mind, not just recovery

All pushed to origin/main.

## Gotchas

- **A fact stated in a subagent's brief is indistinguishable, from the outside,
  from the subagent having read the source.** The adversarial reviewer reported
  a contradiction — the requirements file claimed clean-room isolation while
  citing the old register's count of 51 — because it could not see that the
  number came from the brief, not from the register. The finding was wrong and
  the reasoning was sound. Any document produced under an isolation constraint
  must state the provenance of every external fact it cites, or its own
  isolation claim is unverifiable.
- **The `📋 Last session:` acceptance test in `docs/product/hooks-autoload.md`
  quotes the rendered string, but the source literal is lowercase `last
  session:` (capitalised at `hooks/session-start.sh:63` during assembly).** A
  grep for the documented string returns nothing and reads as "the hook never
  fired". An acceptance test must quote the source literal, not the output.
- **`tac` does not exist on macOS.** Use `git log --reverse` rather than piping
  through it; the failure is a bare `command not found` mid-pipeline.
- **The conductor marker went stale for five hours and nothing noticed.**
  `kivna/.active-modes` is gitignored, so no CI step and no hook can refuse a
  wrong phase — the file is exactly as true as its last write, and this session
  proved the discipline fails silently under a long conversational stretch.

## Insights

- **The self-check ladder is real, and its tiers have measurably different
  reach.** First live run: the composer's own straw-man found five things wrong
  with what it had written and missed three things Tony said that it had never
  written. An independent adversarial reviewer found all three. **Self-criticism
  structurally audits only what made it in**, so absence is invisible to it —
  which is the argument for the third tier existing at all, measured rather than
  asserted. A standing omission pass was added to the composer's straw-man as a
  result.
- **The outward pass found something neither model could reach from inside.**
  Required by Law 3's second dimension, comparing against outside practice
  rather than our own intent, it found that our entire answer to false approval
  is *presentational* — borders, brevity, visuals — while current
  human-in-the-loop practice treats rubber-stamping as an **attention** failure
  answered by **active confirmation**. Measuring work only against stated intent
  cannot detect that the intent is behind the field.
- **Two models built an elaborate false tension out of over-reading one word.**
  Both read "the composer or conductor roles should have the approval" as
  authority over the agreements. Tony meant operational unblocking. Neither the
  composer nor the adversarial reviewer caught it; he did, by reading what they
  wrote. The adversarial tier is not a substitute for the producer reading the
  artifact.
- **The mechanism designed to prevent displacement was overridden on its first
  run.** The register's `proposed` state exists so a mid-session idea can be
  captured without becoming the work. It ran for the first time on `FUN-010`
  yesterday — and `FUN-010` was then built the same session. The capture beat
  worked; the discipline it was protecting did not.
- **Every correction this session came from Tony reading, not from either model
  checking.** The measures-not-inputs error, the superpowers misclassification,
  the C4 misreading, the markdown-file-as-system substitution. The checks caught
  real things, and none of them caught these.
