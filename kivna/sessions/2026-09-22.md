# 2026-09-21 18:23 to 2026-09-22 00:1x EDT: the site and README agree, and Jev was tried

One sitting. Switch In recommended fixing the six places where the live site and the README
contradicted each other; Anthony answered "y". The session ran Kerd 0.143.0's text from the
plugin cache, which is the tip.

## What shipped

**The six site/README contradictions** (`8ff2196`). Re-checked first: the live pages were
byte-identical to the repo, so all six still stood. Each settled against what Kerd does: the
site's "a second AI reads the work independently" became "can read the work too"; the third
problem's diagnosis follows the README; the docs page's "Claude and Codex together" became
"Claude or Codex"; Agent's and Visuals' capability cards got their own jobs instead of
Conductor's text; the README's Install gained the Codex pointer. Before-and-after picture:
`docs/work/front-page-claims/six-contradictions.png`.

**The Codex skill count** (`2816c9c`): `docs/guide/getting-started.md` said the Codex core
leaves out "the other eight skills"; it is four (Tend, Slainte, Kivna, Skriv), matching
`build.py`.

**The host line** (`927d46d`): "Kerd runs inside Claude Code or Codex" became "Kerd runs inside
Claude Code, and a four-skill core can be built for Codex" in the README and on every site
page. The approved draft said the core "runs in Codex"; withdrawn before review, because the
Codex record shows the core built and installed at 0.113.0 but no Codex conversation through it.

**`.env` ignored** (`9b763c2`). Anthony put a TypeSafe API key in `.env` at the repo root; it
was not ignored, so a blanket add would have published it.

All prose, no version bump (the 2026-09-21 ruling). Each push on Anthony's "yes"/"y"; CI
green on each; the live site checked byte-identical to the repo after each site change.

## The Jev trial

Anthony asked what Jev (TypeSafe AI's System One model, early access since 2026-09-15) could
do for Kerd. Record: `docs/work/jev-trial/work.md`. Three graded runs, 276 calls, no errors,
about 0.3 s each, well under a cent in all:
- Front-page claims: asked cold whether a claim is checkable, it failed; given the evidence
  note, 40 of 54 verdicts agreed, and confidence tracked correctness; on nine overstatement
  pairs every overclaim scored above every accurate version, on a thin margin.
- Backlog triage against the 2026-09-12 closure review: failed its pass line (88% at
  confidence ≥ 0.7 against 90%, and four live rows called dead).
- Screening review requests: a one-line wording rule did as well.
Conclusion: Jev grades written evidence cheaply; it does not find evidence, which is where
Kerd's cost is. Not worth a place in Kerd now. Other projects surveyed: 3of3's Archie hard
tail is the best fit on paper; Anthony declined testing it ("no").

## Review

Codex (`codex-tui`), before-push, four reads, each clear on the first: the six fixes, the
skill count, the host line (including the two lines deliberately left alone). No blocks.

## Things I got wrong

- **I told Anthony the 55 claims had recorded verdicts in the work record.** They were in the
  claim reader's report in the previous session's subagent transcript; the work record holds
  only totals. Corrected before any run.
- **The host-line wording I asked him to approve overclaimed** ("runs in Codex"). Caught on my
  own re-read before review, and the change was disclosed rather than slipped in.
- **My first keyword marks for the review replies were unusable** ("Ready. No consequential
  findings" marked blocked). Replaced by marking each reply by hand to one rule.

## Seen in real use

The arrival's "Yes" route and Conductor's Shape entry were seen again. The report shape held;
the speech-bubble question was answered with bare "y", "yes", "sure" or "no" about a dozen
times. Not seen: the composer, managed Conductor, a roll, fan-out (all work was inline).

## Collaboration

Codex: four before-push reviews through Agent, all returned. No other sessions contributed.

## What this sitting touched

Four commits on `main` (`8ff2196`, `2816c9c`, `927d46d`, `9b763c2`) and this close;
`git show --name-only` on each gives the inventory. `docs/work/jev-trial/` is committed except
`review_results.json`, which holds role text from the local-only review archive and stays on
this machine.

## Open at close

**Recommended next, proposed not agreed:** draft a launch plan for the Kerd that exists now,
for Anthony to rule on. **His:** the launch-plan ruling itself, the output style, GitHub's
rendering of the README pictures, a repo homepage, the "prove Kerd first" hold, and two
harmless leftovers from the 2026-09-21 image test. Everything else is in `TODO.md` `## Now`.

---

# 2026-09-22 08:13 to 18:2x EDT: a launch plan, six releases, and Kerd stops going quiet

One sitting, on the 0.143.0 plugin cache throughout, while the tip moved to 0.146.0. Switch
In recommended drafting a launch plan; Anthony answered "Yes — draft the launch plan".

## What shipped

**The launch plan** (`8cb9dac`, `b92b911`). `docs/design/launch-plan.md` was written around
the retired ladder — five outcomes, a ladder pilot, a critical path through work that no
longer exists. Redrafted for the Kerd that exists: launch means someone other than Anthony
carries real work in their own repository to its agreed result, unaided (his 2026-09-02
definition, reworded). Route: prove it at home, invite three to five people, they install
and work a first sitting and a second, then the open launch. Five checks per person: own
repository, unaided, finished to their goals, came back unasked, what broke. Accepted
09:38. Step 1 is 3of3's iCloud sync, kept light, and Kerd's own sittings count too.

**0.144.0 — a concert performs on its own branch.** A roll mid-concert is a Switch Out,
which commits and pushes, so half a build could land on `main`. A concert now starts
`concert/<work>` at its go; every save and roll lands there; a branch mismatch stops the
save; the merge back is the person's go. Switch's save helper already worked on any
branch, so no code changed.

**0.144.1 — the pictures stay readable on a laptop.** Measured in Chromium at 1440, 1280
and 1152: beside its text a picture is half the page, so at 1152 the smallest labels fell
to 12.6px, under Kerd's own 13px floor, and one label in the concert picture was under it
even at 1280. The site now stacks below 1200px (labels 26px) and that label went 28px to
32px. Also corrected the records that still called the retired modes file live state.

**0.145.0 — corrections lead, and a finish names the next item.** Two report-shape defects
from repeated real instances: a message that had to deliver a result *and* retract a claim
was over its five-item budget before it started; and a finish could end on "no action
needed" while open work waited.

**0.145.1 — the picture-by-path route has Claude evidence.** A fresh read-only Claude
worker on Sonnet 5 was given one absolute path and answered three picture-only questions
correctly. Still untested there: an established partner, and a file outside the project.

**0.146.0 — Kerd keeps going, or says what it is waiting for.** See the ruling in
`docs/decisions.md`; it covers rehearsal's own tasks, not only the concert.

**CI refuses a release note that reads two ways** (`f652a52`). The README and CHANGELOG
both hold the history with nothing checking they agreed, and today's notes were hand-copied
four times. Compared first: 71 README entries, 89 changelog, no README-only versions, every
shared note identical.

**The site tells the musical story** (`ec5df1a`, `4e5d4bc`). Rehearsal and the concert
appeared 17 times on the front page but only inside two body paragraphs and captions, never
defined. The hero now names the shape; a new band defines both before the pieces use them;
the capabilities intro defines them for a reader arriving from the nav; the README matches.

Also: the GitHub repo description now matches the site's line, and the Codex core guide says
eight skills with four excluded by name instead of twelve and eight.

## Checks

GitHub renders all four README pictures, smallest label 16.8px (checked 2026-09-22). The
repo homepage already linked the site, contrary to the record. Every release: 760 skill
tests, 16 hook tests, release check clean, and the live site verified byte-identical after
each site push.

## Review

Codex (`codex-tui`), checkpoints and before-push: eleven reads, four blocking, each
correct. It caught a report that read "go" as authorizing a push Anthony had not approved;
a claim that nothing writes `.active-modes` when Skriv still does; a history check that
would have accepted a truncated note, then the same check fooled by a `#` comment inside a
fenced block; and site copy claiming a concert never comes back to ask.

## Things I got wrong

- **I read a one-word "go" as approving both the plan and its push.** My own question had
  reserved the push for a separate go. Codex caught it; the record was corrected.
- **I trimmed test output to two lines, missed a failure, and pushed.** CI went red on
  0.145.1 and was fixed in the next commit (`4ba3278`).
- **I truncated two JSON files** by opening them for writing before reading; restored from
  Git.
- **I cut a true line from Tend** while removing dead references — the hooks do still read
  that file. Reverted.
- **`TODO.md` rode along in several release commits**, against the state contract, which
  gives session-state files to Switch Out alone.

## Seen in real use

The arrival and its "Yes" route; Conductor at Shape four times; Codex review at checkpoint
and before-push eleven times; a Claude worker dispatched through Agent; native subagents at
Opus/high with `job_evidence.py` confirming the observed pair. Not seen: the composer,
managed Conductor, a roll, the arrival's "Something else" route, or any of today's new
rules in force — this session ran the 0.143.0 cache.

## Open at close

**Next:** Anthony sends the drafted note to the SAM and Aubel.app teams
(`docs/work/launch-plan/team-note.md`) and takes 3of3's iCloud sync through Kerd as step 1.
**His alone:** whether the 2026-09-13 hold is lifted, who the invited few are, where and
when to announce, and whether the explanatory output style stays on. Everything else is in
`TODO.md` `## Now`.
