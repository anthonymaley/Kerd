# Session 2026-09-03 (the ruling-and-template sitting, 08:38–09:15 EDT)

**Machine:** AnthonyacStudio (the Mac Studio; thin-client host, tmux over SSH from the MacBook)
**Branch:** main
**Tracking:** origin/main (up to date)

## What Was Done

**The decision that blocked the critical path was made, its fix entered the
board as its own work item, and the session's other product was a communication
template born from a live correction.** Conductor ran the full protocol; two
tasks, two clean commit chains, CI green on each push.

risk-state-split framed — started 08:38 (marker) · landed 08:41 (36bd76a)
status-report template v0.105.0 — started 08:41 (previous work commit) · landed 09:03 (54c28cd)

**Task 1 — the State-column ruling and its frame** (`36bd76a`, board `971e66e`).
The producer ruled option 2: a risk's **severity** (how damaging) and its
**treatment** (what we are doing, and whether it is proven) become two separate
fields — his full reasoning verbatim in CONTEXT.md `## Key Decisions`. The
redefinition option died because it would make severity depend on treatment
quality, the exact dependence his 2026-09-02 refusal forbade. Framed as
`docs/product/risk-state-split.md` (route: new, rigor: mvp): the machine
confirms frame and viability pass, enters at viability, and the next rung
correctly refuses until its two rows — the half-migrated killer and hollow
treatment, both his boundaries — are qualified and `## Scope` is written.
`gate-reachability` verified still blocked, which is his sequencing: it stays
blocked until risk-state-split is designed and implemented. CI green
(run 33756709234).

**Task 2 — the Status Report template, released as v0.105.0** (`54c28cd`).
Born in this session: the producer refused a status spoken in repo shorthand
(*"these are not terms anyone can actually understand in context"*), the same
facts restated as Work item · Stage · Issue · Resolution path worked first
time (*"wow what a difference. make this a template please"*), and he added the
closing rule — exactly one question, never a compound "X, or Y?". Shipped as
format 9 in `docs/design/talk-formats.md`; `skills/conductor/SKILL.md` (orient
summary + talk-moments principle) and `skills/switch/SKILL.md` (step 10
Summarize) point at it; README What's New updated; version bumped in all three
locations. Deliberately unchanged, as restraint: the capability lists (no
high-level capability moved) and both skills' trigger descriptions (invocation
conditions unchanged). CI green (run 33758873760). The running session's cache
is 0.104.0, so the new skill text binds from the next repin; the rule itself
already binds via CONTEXT.md.

**The release close-out pass ran** (version-diff trigger): tend — all
structural categories green, one fix, three local `.DS_Store` files deleted;
slainte release pass — 0 high, 0 medium, 0 low, with restraint named
(playbook:74's "v0.104.0" is Drive's ship-version tag, historical, left;
kerd-map.svg's known stale claims stay filed Backlog rows; the What's New
header double-home was fixed forward and its structural fix stays a filed row).

**TODO closure review** (this boundary):

```
  ✓ done   — "rule the ledger-axes conflict"      (option 2 ruled; CONTEXT.md + 36bd76a)
  ✓ done   — "frame the fix on the board"        (risk-state-split, 36bd76a + 971e66e)
  ✓ done   — "status template" (Task 2)          (v0.105.0, 54c28cd, CI green)
  · open   — everything else — gate-reachability's blocker changed shape: it now
             waits on risk-state-split shipping, not on a decision. No unsure
             rows, no premise-dead rows found this session.
```

**The artifacts, named by path so a pickup can reach them.**
`docs/product/risk-state-split.md` — the frame, the ruling applied, its named
ledger. `docs/design/talk-formats.md` — format 9, the Status Report.
`skills/conductor/SKILL.md` and `skills/switch/SKILL.md` — the two pointer
edits. `README.md` — the v0.105.0 What's New entry and the switch-section line.
`.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` — the three
version fields. `CONTEXT.md` — the two new decisions and the rewritten open
question. `docs/plans/progress.*` — the board with risk-state-split on it.

Also in this boundary's range, though they landed on 2026-09-02 — the range
reaches back because of the `fidelity.py` anchor defect already filed in
Backlog: `docs/product/gate-reachability.md` (the item this session's ruling
unblocks-by-sequence) and `docs/product/question-set-derivation.md` (the
spike's frame), both recorded in full in `kivna/sessions/2026-09-02.md`.

## What's Next

- **`risk-state-split` at viability — the critical path.** Qualify its two rows
  (the half-migrated killer; hollow treatment) and write `## Scope`. Design and
  implementation follow as its later rungs: the two-field shape in
  `LEGAL_STATES`/`parse_ledger`, the 22-record migration, fixtures both ways.
- **`gate-reachability` waits on it** — then re-qualifies its killer row
  (Severity: fatal · Treatment: permanent countermeasure) and goes to scope,
  whose contents the producer has already stated.
- **Position:** `risk-state-split` at **viability** (framed this sitting) ·
  `gate-reachability` at **viability, blocked pending risk-state-split** ·
  `question-set-staleness` at **viability** · `inline-composer` at **handoff** ·
  `requirements-success-measurement` at **loop** (4 of 20 Pieces) ·
  `gate-visuals` at **ready-to-release** · `funnel-driver` and `progress-html`
  at **acceptance**, 0 Pieces · `hooks-autoload` at **viability** · Kerd at
  **v0.105.0**. **Launch: 0 of 5 outcomes.**

## Key Decisions

Full rationale in CONTEXT.md. **A risk's severity and its treatment are two
facts, not one field** — the State column splits rather than `fatal` being
redefined into something conditional; the migration cost is accepted and
sequenced as its own item. · **Status is spoken as Work item · Stage · Issue ·
Resolution path, plain words without the repository open, ending on exactly one
question** — never a compound "X, or Y?".

Nothing was pruned from CONTEXT.md: no acceptance record landed and no drop was
agreed, so this sitting was structurally not a licensed prune event. (A version
bump is a release, not a prune licence.)

## Commits

Three work commits plus this boundary, all pushed to origin/main, CI green on
each push: `36bd76a` (risk-state-split framed) · `971e66e` (render refresh) ·
`54c28cd` (v0.105.0, the status template).

## Insights

- **The template's proof was its own birth.** The producer could not act on a
  status message full of technically exact shorthand; the identical facts in
  four plain sections drew *"wow what a difference"* and an immediate ruling on
  the substance. The failure was never the content — it was the vocabulary
  asking the reader to have the repository open.
- **A correction made mid-session shipped as machinery the same session.** The
  plain-language rule went from refusal (08:24) to recorded decision to
  released format (09:03) in under forty minutes, because it needed no new
  mechanism — only a format row, two pointers, and the release checklist. The
  repo's standing lesson that a finding fixed only in the artifact returns was
  answered here by fixing the generator — the skills that speak — on the day
  the failure was seen.
