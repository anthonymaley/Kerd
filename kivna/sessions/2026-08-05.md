# Session 2026-08-05

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

**The journey-view frame conversation ran — and converged through the
artifact, not the interview.** Opened as a conductor session (lean/inline,
Fable at FRAME per the ratified default) with a straw-man Value in units;
Tony redirected immediately: *"lets either mock up the html and we can
iterate together."* Four live mock iterations followed:

1. **v1** — trail-map telling, 11 parallel endeavour journeys with lamp
   stations. Verdict: decent start, six corrections — the biggest: the
   page is ONE project's story (the idea/problem as the title), not
   parallel journeys; task cards need % / time-left / status words.
2. **v2** — problem as title, what's-cooking task cards, phase cards
   with per-phase delivered lists. Verdict: "hmmm no not right" → Tony
   switched to rapid-fire Q&A: how many stages idea→release, how many
   steps within each. The answers (8 rungs; fixed small counts except
   build, whose steps are the contract's Pieces) became the real
   information architecture.
3. **v3** — the page IS the ladder: a section per rung, steps with
   individual statuses, "what we created" lists with open-links; mock
   content = the freshness guard's genuine history.
4. **v4** — Tony: *"be inspired by the story templates from TPS —
   factual info, visually explained when needed."* Grounded on
   `docs/design/talk-formats.md`; the head became an A3 Proposal held to
   the API Gateway exemplar bar: current situation drawn (SVG) ·
   numbered pains · proposal drawn · targets in units · the cost named ·
   a measured-in-use strip; every ladder step carrying a fact instead of
   an adjective. Verdict: **"oooh now thats looking good."**

**Parked at v4 by Tony's call** — "leave it there for now, add a task to
revisit later, when we have more data." The mock left the ephemeral
scratchpad and became a dated record: `docs/plans/2026-08-05-journey-view-mock.html`,
pushed dcd970f, CI green headSha-verified. Revisit task in Backlog
Medium carrying the agreed shape; the full decision in CONTEXT.md.

**TODO closure:** the journey-view ## Now item closed (evidence above);
## Now moved to grounding-was-read. Stale-claim fix at the boundary: the
plugin cache is at kerd/**0.77.0** (both skills loaded from it this
session), not the 0.74.0 that memory and the debt line claimed.

## What's Next

- **grounding-was-read** — the remaining design-layer build (spec A8:
  a `grounding` slot per rung + read-receipts on the `mark_reviewed`
  precedent).
- The journey view sleeps in Backlog with a named revisit condition
  (more journeys walked through the gates) and a surfaced prerequisite:
  **progress % and time-left estimates need declared on-disk homes**
  before the real page can show them.

## Key Decisions

- **Journey view parked at mock v4; the shape is settled**: one page per
  journey, the idea/problem as the title; A3 Proposal head (current
  drawn · numbered pains · proposal drawn · targets in units · cost
  named) + measured-in-use strip; the ladder as page structure (section
  per rung: steps with status + facts, what-we-created with links); a
  front page above it with what's-cooking task cards (name · sentence ·
  progress % · time left · status word: on plan / taking longer /
  encountered issues) and a next-up queue.
- **Estimates are derived-from-disk too** — % and time-left must become
  declared artifact lines before any page may show them.
- progress-html stays held at proving; its trio plumbing stays.
- Loop-input stayed dropped; nothing in the mock reopened it.

## Commits

`dcd970f` journey-view mock v4 (parked, work commit mid-session). Plus
this switch-out commit.

## Insights

- **The frame converged through the artifact, not the interview** — the
  same lesson as the canvas walk, now proven in a second medium: every
  structural correction came from Tony reacting to a rendered page. The
  straw-man Value questions produced one redirect; four mocks produced
  the whole requirement.
- **The A3 grammar was the turn.** Three iterations circled structure;
  approval came immediately after the Proposal-shaped head landed
  (current drawn → pains numbered → proposal drawn → targets → cost).
  The talk-formats library works on web pages, not just whiteboards.
- **The rapid-fire Q&A was Tony building the information architecture
  himself** — the mock only clicked once its structure matched the
  ladder 1:1 (section per rung, steps within, artifacts per stage). The
  page's shape was never a design invention; it was the ladder's shape,
  waiting to be asked for.

---

# Session 2026-08-05 (afternoon/evening) — grounding-was-read walks the whole ladder

**Machine:** Mac
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

**Post-boundary framing: the rigor-level forcing function** (Tony's
opener: spikes, MVPs and production releases need different measurement
levels, and an unasked level means an insecure app passes DONE by
silence). Framed into Backlog High across two exchanges: the level is
declared IN the Release slice definition, gate-refused when absent;
declaring a level expands from a **rigor catalog** (living doc per
level) into a pre-filled per-class disposition table — measured /
waived-by-name / n-a-with-reason; the composer key touches only
deviations; measured classes become CI checks. Committed 86a2cb5 +
f4c2f0c.

**grounding-was-read: frame → viability → slice → design → contract →
build → goal → loop, in one day. v0.80.0.**

- **Frame** (23f9af6/4de610b): slice fork settled — slice 1 =
  declarations + reachability audit (hollow-proof); receipts deferred
  with the retrieval-not-comprehension claim. Tony's "what do you
  think?" surfaced a self-caught defect: the Value section had borrowed
  slice 2's evidence (the 6-Jul unread doc) to sell slice 1 — amended
  before his key.
- **Design** (0c2e9a5/f843120): grounded read found **A8's sketched
  landing site doesn't exist** (check_rung is inline code; no kit.GATES
  table) — the granularity decision became per-item `## Grounding`
  section by marks, static table dead on both M-criteria. Package
  drawn (gen_grounding_was_read.py, two layout faults caught by the
  checkers pre-paste), driven onto the live canvas, **zero-correction
  GO**. Gate record is docs/gates/' second design citizen.
- **Contract**: orchestrator two-pass (fable) — six files scoped with
  reasoned exclusions, 417-line spec written straight to disk, three
  gate risks raised (two dissolved by conductor evidence, the third —
  retiring the README's false Deferred section — kept).
- **Build**: five players (2 sonnet, 3 haiku), every verify matched on
  first run except step 5, where the player STOPPED on an output-order
  mismatch that was its own command retyping (glob expansion reorders
  grep args) — the score was right; conductor re-ran the spec's exact
  command, byte-match, verified. Cross-player check held (orchestrator
  fixtures exercising a different player's code). Both-ways refusal
  demonstrated; selftest 14 → 18. Ship d4f5d27/bad2003, CI green.
- **Goal** (005bed9/bb64bfe amendments · ac7c038/c08be31 record):
  cold eyes (opus) passed layers 1–3 and **BLOCKED twice** — README
  overclaimed AU5's reach ("every top-level list line" vs `- `-only),
  and the frame still promised the dead kit.GATES landing site plus an
  unenforceable "0 at any tip" unit. Amended per the push-wiring
  precedent; looser-resolution (absolute/directory refs resolve) became
  an accepted ledger row with a review trigger. **The staleness refuser
  fired on the amendment commit itself** (new ledger row moved the
  derived page) — its fourth genuine catch.
- **The expert-user pass produced a finding, not a rejection**: "I
  don't know that I will ever manually add a grounding line." The
  author of grounding is the machine (the dogfood section was
  model-written during framing); the human meets AU5 as the red
  check's beneficiary. Recorded in the goal record; consequence: slice
  2's receipts are model-facing, hollow-stamping central.

**The shame point fired twice**: an either-or at the slice gate
(answered with an unparseable "y") and again at the roll-into-contract
gate one hour later ("i avoided the x or y question"). Point
`state-default-dont-enumerate-options` now at seen 5, incident
captured, mirrored into this repo's memory
(`feedback_state_default_dont_enumerate.md`), both walks played.

## What's Next

- **No work in flight.** Next focus picks from Backlog — freshest:
  the rigor-level forcing function (High) · the journey view (parked,
  wakes when more journeys have walked).
- Slice 2 of grounding (receipts) stays deliberately unqueued behind
  its ledger rows.

## Key Decisions

- Rigor level: declared in the Release slice definition; catalog-driven
  pre-filled expansion; human key on deviations only (Backlog High).
- Grounding granularity: per-item `## Grounding` section; static
  per-rung table rejected on marks; hybrid deferred to slice 2.
- Goal-gate amendments over rebuild (push-wiring precedent), including
  the detection-at-tip unit re-wording the frame had regressed off.
- The expert-user finding reframes slice 2 as a model-facing
  instrument.

## Commits

`86a2cb5`/`f4c2f0c` rigor-level capture · `23f9af6`/`4de610b` frame ·
`0c2e9a5`/`f843120` design GO · `d4f5d27`/`bad2003` build (v0.80.0) ·
`005bed9`/`bb64bfe` goal amendments · `ac7c038`/`c08be31` goal record.
Plus this switch-out commit.

## Gotchas

- **A player's verify mismatch can be the player's command retyping,
  not the work**: step 5's STOP was grep-arg reordering from a glob
  the player substituted; the edits were perfect. Before classifying a
  failure player-vs-score, the conductor re-runs the spec's verify
  command EXACTLY as written — the classification is only as good as
  the reproduction. (Mirrored to playbook.)

## Insights

- **The cold-eyes layer has blocked on declared-truth gaps in both of
  its runs** (push-wiring: detection-vs-prevention; here: the dead
  landing site + the unenforceable unit). The flaw class the walk
  predicted for assembly time is showing up at assembly time, twice
  for two — the layer is load-bearing, not ceremony.
- **The frame is the artifact most likely to rot during its own
  build**: both blocks traced to the product doc keeping pre-design
  wording after the design settled differently. A design decision that
  contradicts the frame should trigger a frame amendment in the same
  sitting — worth considering as a gate check someday.
- **The expert-user pass keeps producing requirements**: progress-html's
  refusal produced the journey view; this pass produced "the machine is
  the author," which re-scopes slice 2 before it's ever framed. Two
  runs, two findings, zero rubber stamps.

---

# Session 2026-08-05 (third sitting) — rigor-level framed and sliced in one pass

**Machine:** Anthonys-MacBook-Pro.local
**Branch:** main
**Tracking:** origin/main (in sync at switch-out)

## What Was Done

**Switch-in (full):** all seven CI gate steps green locally; no
`(done? — confirm)` tags; board current, drift none. One consistency
catch at orient: both CONTEXT.md and TODO.md claimed the plugin cache
sat at kerd/0.77.0 — both skills this session verifiably loaded from
**kerd/0.79.0** (live load paths). Corrected in both files.

**Conductor session (lean/inline, Fable at FRAME): rigor-level walked
frame → slice in one sitting.** The straw-man product doc
(`docs/product/rigor-level.md`) passed viability and slice gates on
first machine check; Tony keyed all three rungs on first read
("good for me") — zero amendments between draft and key. Stage
`sliced`; route shows `enters at: design`. Shipped per the standing
flow: work commit 6e5c37e (one new file, by name, trailer) → render
refresh → render commit 54fd552 (no trailer) → one push → CI green
headSha-verified on the tip.

**TODO closure:** Backlog High's "Rigor level as a forcing function"
closed — its content now lives in the product doc (evidence: 6e5c37e).
`## Now` set forward-only: rigor-level at design as the natural next
pick.

## What's Next

- **rigor-level's DESIGN rung** — one conversation, one package
  (`docs/design/rigor-level.md` + canvas, per the DESIGN-rung
  decision); or pick elsewhere from Backlog.
- Slices 2 (catalog + disposition table) and 3 (measured classes as CI
  checks) stay deliberately unqueued behind slice 1.

## Key Decisions

- **rigor-level framed and sliced** — full record in CONTEXT.md Key
  Decisions and the product doc itself. The short of it: slice 1 thin
  (declared legal level + slice-gate refusal for new work, no
  retrofits); killer = hollow waiving, countermeasure permanent
  (accepted-state discipline on waivers + per-level catalog floors);
  level choice stays a human key.

## Commits

`6e5c37e` rigor-level product doc (framed + sliced) · `54fd552`
progress render refresh. Plus this switch-out commit.

## Gotchas

- **zsh does not word-split unquoted variables**: the switch-in smoke
  loop ran `python3 $cmd` with `cmd="tools/gates/gate.py selftest"` —
  zsh passed it as ONE argument, python tried to open a file literally
  named `gate.py selftest`, and all seven steps spuriously FAILed. The
  tell is the error text quoting the whole string as a filename. Fix:
  `eval` (or zsh's `${=cmd}`). Bash habits lie here — this shell is
  zsh. (Mirrored to playbook.)

## Insights

- **The frame was nearly free because yesterday's capture WAS the
  conversation.** grounding-was-read needed live iterations at frame;
  rigor-level converged straw-man → key in one pass with zero
  amendments, because the post-boundary framing exchange on 2026-08-04
  had already settled level-in-slice, catalog, pre-fill, and help
  seating — the Backlog entry held the whole shape. Capturing an idea
  AT the moment it's raised, in near-frame vocabulary, moves the frame
  cost to where the thinking already is.
