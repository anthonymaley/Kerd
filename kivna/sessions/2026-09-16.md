# 2026-09-16 — four releases, and a defect class produced while removing it

One continuous sitting, 2026-09-15 19:57 → 2026-09-16 14:20 EDT, continued by its
designated successor 14:33 → 18:13. Owner: the Claude session holding
`kerd-b5-review` (adopted at Switch In from the handoff designated against
`CONTEXT.md`). Boundary: branch `main`, the 0.133.0 release commit `7cf5782`;
`git log`/`git show` supply its detail.

**Contributions captured:** Claude (pickup, 0.131.0 build and release, 0.132.0 build,
behavioural testing, sole release ownership, records) + Codex `codex-tui` (nine review
rounds across two releases, twenty-one findings, all resolved) + a Codex-side Claude
session (the decision-capsule slice folded into 0.132.0, then frozen) + roughly forty
native subagents (three build players, ~thirty behavioural runs, several diagram
players) + a peer Claude session (the model-dispatch guard design). Missing: none.

## What shipped

- **0.131.0** — the question form gains pickers; Agent gains a role selector. This
  withdrew 0.129.0's "no boxed cards or native pickers", which was Claude's own
  extrapolation rather than Anthony's instruction — and which a *passing test* had been
  defending for two releases. Codex's own before-push review had hardened the ban rather
  than catching it, because review tests consistency with the recorded ruling, not
  whether the ruling matches what was asked.
- **The playbook fix** — `docs/playbook.md` had claimed ten skills since v0.108.0 while
  the repo carried twelve, and listed neither `agent` nor `visuals`. Found by the
  release close-out pass, which is the only check that looks at it.
- **0.132.0** — visuals by default, plus the decision capsule, combined on Anthony's
  ruling. Both behaviourally evidenced. Details in
  `docs/work/visual-communication/work.md`.

## The finding worth carrying

**The defect was never one sentence. It was a class of sentence.** Seven were found
across three files, each reading like discipline while handing the model an
unfalsifiable judgment about its own work:

> "only if it clarifies a real relationship" · "whenever they help understanding" ·
> "when it helps" · "when spatial detail warrants it" · "a substantial proposal" ·
> "Optional job/diagram tools" · "a small inline relationship can be enough"

That last one survived in `skills/visuals/SKILL.md` — the skill whose entire job is
rendering views — after being removed from `journey.md`, because the brief that produced
the fix named sections instead of asking for a sweep. Codex found it. Static gates were
green the whole time all seven were live.

The repair that mattered was making the threshold **countable**: two or more connected
parts, a branch, an ownership boundary, or a before → after change. "Substantial" carries
no test a model can fail; a model can call a multi-part proposal small because it is easy
to *describe*.

## Evidence discipline — the part that should survive

Behavioural claims went wrong twice before going right.

1. **No control arm.** The first design would have made a pass meaningless: if a
   treatment worker draws, that could be the rule, the model's disposition, or
   `diagram-design` sitting in its tool list.
2. **Read too early.** The second was scored off disk while workers were still running,
   producing a reported *failure that did not exist* — a recommendation not to ship,
   built on a snapshot. Both "phantom" runs finished at 26 and 37 minutes having produced
   their files all along. Corrected in the work record rather than buried.
3. **Three arms, pre-registered.** The third isolated each rule separately, used
   byte-identical prompts, and fixed the scoring criteria in writing *before* the last
   runs reported — including the note that the scorer is the party who wants the release
   to pass, and that borderline runs score PARTIAL.

Results: visual rule marginal **3/3 vs base 0/3**, no over-fire **0/3**, outside
Conductor **2/2**, explicit request **2/2**. Capsule rule **4/4 vs 0/4** after tightening,
up from 1/3. Nineteen valid runs plus an eight-run re-test; one pilot recorded INVALID.

The capsule tightening is the clearest example of the method working: the five partial
failures failed *identically* — models gave the recommendation early, then asked, leaving
the block above the bubble carrying only the deciding consideration. The word "minimum"
had read as licence to omit what was already said. Telling them to **restate** it moved
the result from 1/3 to 4/4.

## Collaboration, and one collision

At 07:52–07:58 a Codex-side Claude session edited this working tree while nineteen
uncommitted files of 0.132.0 sat in it. Git could no longer separate the two change sets.
It was recoverable only because a byte-exact snapshot of `skills/` existed from an
unrelated test harness. Resolved by explicit sole ownership, an explicit freeze, and a
rule that the other side does not touch the tree until the release is reported
remote-verified. Worth keeping: "wait for the remote-verified report" costs one message
and removes the entire failure mode.

`codex-tui`'s queue ran 4h+ behind once; its reviews were the long pole twice.

**Anthony settled the split at 14:21:** Claude owns build and release, Codex is the pairing
partner for expert-level review and investigation. That supersedes the 08:13 agreement giving
Codex the 0.133.0 implementation, and it matches where each side earned its keep today — every
one of the seven loophole sentences that the builder missed was named by the reviewer.

## Archify

Installed 2026-09-15 21:37 on Anthony's call — `npx skills add tt-a1i/archify -g`, to
`~/.agents/skills/archify`, symlinked for Claude Code. `archify doctor` 15/15. **Zero
runtime dependencies** (its four packages are dev-only, no `node_modules` installed).
Still a dev snapshot, `2.17.0-dev.1`, no stable release. The installer reported **two
Socket alerts that were never identified** — recorded as unknown, not cleared.

## Not observed, not claimed

- 0.131.0's two deferred behaviours: renderer byte-identity with a picker attached, and
  a picked labelled Yes opening direction-setting without approving saved work. The
  0.132.0 Switch In capsule exemption's justification partly rests on the second.
- Whether either 0.132.0 rule holds beyond the one marginal scenario tested.
- The two Archify Socket alerts.
- `diagram-design`'s style guide has `accent` at Krutho blue `#1A6FFF` while
  `accent-tint` still holds the old tangerine — found independently by two sessions.
  Upstream's file, not Kerd's.


---

# Continuation, 14:33 → 18:13 — 0.133.0, and the same error three times

Picked up by the designated successor session: `CONTEXT.md`'s handoff digest matched,
the `kerd-b5-review` role was adopted, and the arrival notice went to `codex-tui`.
This was the first Switch In to run from an installed post-0.129.0 plugin (0.132.0).

**Two contradictions the pickup caught**, both stale records rather than new problems:
`docs/work/visual-communication/work.md` still said 0.132.0 was at the before-push
boundary with nothing pushed, and the installed-plugin note still said the cache
carried nothing after 0.129.0.

## The refusal that shaped the release

Claude proposed a `PreToolUse` hook that would deny a `kerd:effort-*` dispatch with no
explicit `model`. **Anthony refused it: "It turns a missing tool argument into a hook
subsystem."** He set the contract instead — `model` in the call, `subagent_type` for
effort, both named in the grid before dispatch, `job_evidence.py` afterwards — and
ruled out the hook, the matcher framework and a model×effort matrix.

Worth keeping: the proposal had already found two real defects in its own design (the
`hooks.json` nesting trap, the four-alias tool enum) and would have shipped a subsystem
whose tests could pass by doing nothing. Being right about the details of a
disproportionate mechanism is not an argument for building it.

## The finding: 0.132.0's defect class, produced three times while removing it

Four review passes — one Opus job at high effort, then Codex `codex-tui` across three
before-push rounds (five findings, then three, then clean).

**Every defect found was a calibration defect. Not one touched the design.** And three
of them were the *same* error: a rule stated so absolutely that a legitimate situation
could not satisfy it.

1. "Every `Agent` call names both keys — omitting either is a defect" contradicted the
   no-effort-agent route documented two paragraphs earlier.
2. The fix for that, which made both keys absolute, created the contradiction rather
   than resolving the asymmetry the Opus pass had flagged.
3. Adding the `CLAUDE_CODE_SUBAGENT_MODEL_FORCE` fact without saying what to do when
   it fires — a rule unsatisfiable exactly when it binds.

A rule a model cannot satisfy is one it will exempt itself from, and the exemption it
invents is not the one you wanted. This is the mirror image of 0.132.0's seven
unfalsifiable sentences: that release removed rules a model could always pass, and
this one kept writing rules it could never pass.

## Overclaims removed, and two of Kerd's own facts corrected

The draft called its own prose "the guard" and said what ran "is verified". Neither was
true. The honest limit was already written in the work record and had not reached the
guidance.

Checked against the subagent docs rather than argued from memory:

- The resolution order is per-invocation `model` → definition frontmatter →
  `CLAUDE_CODE_SUBAGENT_MODEL` → the caller's model. "Silently inherits the caller's"
  was one branch of two, asserted in eight places **and pinned by a new CI assertion**,
  which would have refused its own correction.
- The `model` field accepts full IDs as well as the four aliases; the enum is this
  host's *tool parameter*.
- A per-invocation `model` persists into a `SendMessage` resume, so the resume is an
  argument for naming it, not the coverage gap the notes claimed.
- `CLAUDE_CODE_SUBAGENT_MODEL_FORCE=1` makes the host ignore the field entirely, and an
  `availableModels` allowlist can substitute another model. So the call *requests*.

## Evidence

One mixed-model fan-out, three jobs in a single dispatch: requested `haiku`/low,
`sonnet`/medium, `opus`/high; observed `claude-haiku-4-5-20251001` (42 records),
`claude-sonnet-5` (28, effort medium), `claude-opus-5` (23, effort high). **The Haiku
job returned no effort records**, so its effort is unverifiable, not confirmed.

730 tests green, `gate.py release` clean, hooks 21/21, CI entry gate green at
`7cf5782`.

## Two process notes

- **Anthony asked why I wait for replies.** There was no good reason: I had parked on a
  Codex reply that had already arrived, and asked him to ping me. Background polling
  re-invokes the session when a reply lands; parking was a mistake, not a limitation.
- **The palette.** A diagram was drawn in Krutho blue because a prior Kerd document
  used it. Anthony: "krutho is not KERD. this skill is for anyone." Kerd's own views
  use diagram-design's neutral default skin; `scope.html` still carries the drift and
  is recorded as separate work.

## Codex's closing judgment, adopted

> three review rounds and increasingly exact prose guards are the limit of useful
> refinement here. Further semantic hardening would be protocol work for its own sake;
> ship this version, observe real dispatches, and only change it again for demonstrated
> behavior.

## Open at close

Anthony's priority, 18:13: records closed (this save), then **update the installed
plugins — Claude 0.132.0, Codex 0.129.0, neither running 0.133.0** — then the
CI/test-path defect as a small standalone fix. Also open: the two 0.131.0 picker
behaviours still unobserved, the palette drift, Archify's two unidentified Socket
alerts, and `kerd-laptop-result.patch` still intentionally untracked.


---

# Continuation, 18:13 → 21:00 — two releases CI asked for, and the palette correction

**Contributions captured:** Claude `kerd-b5-review` (0.133.0 build and release, 0.133.1,
0.133.2, both installations, records) + Codex `codex-tui` (three before-push rounds,
eight findings, all applied) + three native subagents in one mixed-model fan-out (Haiku
sweep, Sonnet release-mechanics check, Opus adversarial read — eleven findings) + a peer
Claude session (relayed Codex's round-1 findings, which were already retrieved; no
unique content). Missing: none.

## What shipped

- **0.133.0** — the explicit-model dispatch contract (`7cf5782`).
- **0.133.1** — `tools/run_tests.py`, the import-order fix in `test_roll_control.py`, and
  CI steps for the 730 tests and the hook tests. **CI had run none of them.**
- **0.133.2** — the defect CI's first run found, because 0.133.1 went red.

## What the three commits touched

Named here because the pickup set must be able to reach them, not as a manifest — `git
show --name-only` reproduces the full inventory.

- **0.133.0, the contract:** the five `agents/effort-low.md`, `effort-medium.md`,
  `effort-high.md`, `effort-xhigh.md` and `effort-max.md` descriptions;
  `skills/conductor/references/model-jobs.md` (the contract itself),
  `orchestration.md` (the dispatch-row rule and the controller exemption) and
  `journey.md` (the grid note); `skills/conductor/SKILL.md`;
  `skills/conductor/scripts/job_evidence.py` (a comment that taught the wrong
  mechanism); `skills/conductor/scripts/tests/test_effort_agents.py` (+8 assertions)
  and `test_job_evidence.py` (a renamed case); README, the three version locations, and
  the new `docs/work/model-dispatch-guard/` record and view.
- **0.133.1, the CI/test-path fix:** `tools/run_tests.py` (new),
  `.github/workflows/gate.yml` (it now runs the suites and the hook tests) and
  `skills/switch/scripts/tests/test_roll_control.py`.
- **0.133.2:** `skills/agent/scripts/tests/test_agent.py`, plus the version and README
  entry.

## Anthony's three priorities, in his order

Closed the records; brought Claude 0.132.0 → 0.133.0 and Codex 0.129.0 → 0.133.0 (a
fresh `output/kerd-codex-0.133.0` build, the `kerd-core` marketplace re-pointed at it,
`codex plugin add` reporting `installed, enabled 0.133.0`); then the CI/test-path fix as
a standalone change.

## The finding: CI earned its keep on its first run

0.133.1 added the CI step and its release note said CI runs the 730 tests. **The first
run went red**, so the claim was false for one commit — which is why 0.133.2 exists.

What it found: a test depending on `agent.RPC` being unpatched global state. Nine sites
in that module patch it; when one leaks, `agent.RPC.__new__(agent.RPC)` becomes
`Mock.__new__(<Mock instance>)`, raising `TypeError: issubclass() arg 1 must be a class`
— an error, not a failing assertion. The test now captures the real class at import.

**Not diagnosed, and recorded as such:** `Mock.__new__(instance)` raises identically on
3.14, so this is not a version quirk. On CI the attribute was patched when that test
ran; locally it was not. Which site leaks, and why only there, is unresolved — and other
tests in the combined run could be passing for the wrong reason. Open in `TODO.md`.

The pattern held to the last commit: **green for as long as the suite only ever ran in
one place.** Same shape as a suite CI never ran, and as the passing test that defended a
wrong ruling for two releases.

## Two corrections from Anthony, both about proportion and audience

- **16:48, the refusal:** *"I would not approve that guard design. It turns a missing
  tool argument into a hook subsystem."* Recorded as a ruling — a countermeasure is the
  size of the defect.
- **16:11, the palette:** *"No. krutho is not KERD. this skill is for anyone."* A
  direction diagram had been drawn in Krutho blue because an existing Kerd document used
  it. Kerd is a plugin other people install; its own views use diagram-design's neutral
  default skin. Recorded as a ruling; `scope.html` still carries the drift.
- **17:45, the process correction:** *"why do you wait to get replies?"* There was no
  good reason — a Codex reply had already arrived while the session sat idle, and the
  session had asked to be pinged. Background polling re-invokes the session when a reply
  lands. Parking was a mistake, not a limitation.

## Deferred behaviour observed, without being asked for

0.131.0 left two behaviours unobserved. **One was observed in this sitting's real Switch
In:** the arrival rendered, a native picker followed the bubble with "Yes — open
direction-setting" and "Not now", Anthony picked Yes, and Conductor opened at
direction-setting **without** approving the saved task — the saved build then needed its
own approval, which he refused and replaced. The 0.132.0 capsule exemption rests partly
on that behaviour, and it held. **The other is still not observed:** no byte comparison
of the renderer's Markdown with a picker attached was ever run.

## Codex's closing judgment, adopted

> three review rounds and increasingly exact prose guards are the limit of useful
> refinement here. Further semantic hardening would be protocol work for its own sake;
> ship this version, observe real dispatches, and only change it again for demonstrated
> behavior.

## Open at close

No task is selected for the next sitting; Anthony's three priorities are all done. The
grounded recommendation, **proposed not agreed**, is the unresolved patch leak, because
it can make other tests pass for the wrong reason. Also open: the renderer byte-identity
check at the next real arrival, the palette drift (two parts), Archify's two unidentified
Socket alerts, the `codex-plugin` behavioural scenario, the 2026-09-13 hold that is
Anthony's call, and the retained launch sequence at 0 of 5.
